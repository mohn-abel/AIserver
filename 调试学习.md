# Linux C++ GDB 调试指南 —— HttpServer 项目实战

> Ubuntu 22.04 · GDB 12.1 · GCC C++17 · Debug 构建

---

## 一、环境 & 命令速查

```bash
apt-get install -y gdb strace valgrind
file build/http_server          # 必须看到 with debug_info, not stripped
grep CMAKE_BUILD_TYPE build/CMakeCache.txt  # 必须 Debug
```

### 启动方式

```bash
gdb ./binary                     # 普通
gdb --args ./binary -p 9090     # 带参数
gdb ./binary core.12345         # 分析 core dump
gdb -p $(pidof process)         # 附加运行中进程
```

### 命令表

| 分类 | 命令 | 作用 |
|------|------|------|
| 断点 | `b func` / `b file:N` | 设断点 |
| | `condition N expr` | 条件断点 |
| | `watch var` | 值改变时暂停（硬件，最多 4 个） |
| | `commands N` → `silent` / `printf` / `continue` / `end` | 自动脚本（不停，只打日志） |
| | `i b` / `d N` | 查看/删除断点 |
| 执行 | `r` / `n` / `s` / `c` / `fin` | 启动/单步跳过/单步进入/继续/跳出 |
| | `Ctrl+C` | 暂停运行中程序 |
| 查看 | `bt` / `frame N` | 调用栈 / 跳到第 N 层 |
| | `p var` / `p *ptr` / `info locals` | 打印变量 / 指针解引用 / 局部变量 |
| | `info threads` / `thread N` | 线程列表 / 切线程 |
| | `thread apply all bt` | **所有线程调用栈（死锁排查必用）** |
| 其他 | `generate-core-file` | 手动生成 core dump |

---

## 二、核心技能（按场景）

### 2.1 Crash 排查 & Core Dump

**三步骤：`bt` → `frame N` → `print`**

```
(gdb) bt                          # 从下往上读：#0 最深（kernel），大号接近 main
#4  __GI_abort
#5  Logger::~Logger               ← muduo FATAL 用析构触发 abort
#7  sockets::bindOrDie            ← ::bind() 返回 -1，根因

(gdb) frame 7                     # 跳到根因层
(gdb) print addr                  # sin_port = 20480 → 端口 80（网络字节序）
```

**Core Dump：** 进程崩溃时 OS 把完整内存快照（所有线程栈/堆/寄存器）序列化为 ELF 文件，事后用 `gdb binary core` 还原现场。

```bash
ulimit -c unlimited
echo "core.%p" > /proc/sys/kernel/core_pattern    # 崩溃自动生成
(gdb) generate-core-file                            # gdb 手动生成
kill -SEGV <PID>                                    # 信号触发

gdb ./http_server core.12345                        # 事后分析
(gdb) bt                                            # 崩溃点
(gdb) thread apply all bt                           # 全部线程
```

### 2.2 源码追踪

```
next  → 执行当前行，不进入子函数                           
step  → 钻入子函数内部                              
finish → 执行完当前函数跳出
```

```
(gdb) b HttpServer::onMessage
(gdb) r                            # curl 触发
(gdb) n                            # 跳过 if (useSSL_)
(gdb) s                            # 钻入 parseRequest()
(gdb) p this->state_               # 追踪状态机：kExpectRequestLine → kGotAll
(gdb) p buf->readableBytes()       # 78 字节
```

### 2.3 条件断点 & Watchpoint & Commands

**条件断点：** 只拦 POST 请求

```
(gdb) b HttpServer::handleRequest
(gdb) condition 1 req.method_ == http::HttpRequest::kPost
```

| ✅ 安全表达式 | ❌ 不安全（多线程 gdb 注入会崩） |
|:--|:--|
| `req.method_ == kPost` | `req.path() == "/chat"` |
| `resp != nullptr` | 任何 `std::string` 成员函数 |

> 不安全原因：gdb 评估条件时在目标线程注入函数调用，`std::string` 涉及堆分配，可能 `this=0x0`。bt 中出现 `<function called from gdb>` 说明是 gdb 崩了，不是程序 bug。

**Watchpoint：** 不知道谁改坏了变量，让 gdb 帮你抓

```
(gdb) d 1                         # 释放硬件槽位
(gdb) watch resp->statusCode_     # 监控 4 字节 enum
(gdb) c
# → Old value = kUnknown  →  New value = k200Ok  at setStatusLine():46
```

限制：x86_64 最多 4 个硬件 watchpoint，每个 ≤8 字节。大对象用 `watch -l`（软件实现，慢但无限制）。

**Commands 自动脚本：** 断点变日志探针，不改代码不重编译

```
(gdb) b HttpServer::handleRequest
(gdb) commands 1
>  silent
>  printf "path=%s\n", req.path_.c_str()
>  continue
>end
```

| 场景 | 方案 |
|:--|:--|
| 根据条件决定停不停 | `condition` |
| 不停，静默打印变量 | `commands` + `silent` + `continue` |

### 2.4 多线程调试

**本项目的 16 个线程布局：**

```
线程      名称            系统调用              对应源码
──────────────────────────────────────────────────────────
1         主线程           epoll_wait            muduo accept 主 EventLoop
2         健康检查         clock_nanosleep       DbConnectionPool::checkConnections
3~10      业务线程池(8)   futex_wait(同地址)    ThreadPool 的 cv_.wait()
11~12     RabbitMQ(2)     poll(500ms)           RabbitMQThreadPool consumer
13~16     I/O reactor(4)  epoll_wait(不同epfd)  muduo one-loop-per-thread
```

> `setThreadNum(4)` → 4 个 I/O 线程各跑独立 EventLoop，epfd 各不相同（113/116/119）。

**三板斧：**

```
(gdb) info threads        # 全局概览 → 每个线程在哪个系统调用
(gdb) thread 2            # 切到线程 2
(gdb) bt                  # 看它在干什么
(gdb) thread apply all bt # 一把梭：所有线程的完整调用栈
```

> 线上卡死不响应：`gdb -p PID` → `thread apply all bt` → 哪个线程卡在 `futex_wait`（等锁）或 `connect`（下游不通），一目了然。

---

## 三、面试速记

| 面试问题 | 回答要点 |
|:--|:--|
| 程序崩溃怎么排查？ | `bt` 看调用栈→`frame N` 定位根因层→`print` 看变量→确认后修代码 |
| 线上崩溃但没有日志？ | `ulimit -c unlimited` 开 core dump → `gdb binary core` 还原现场 → `thread apply all bt` 全线程分析 |
| 程序卡死不响应？ | `gdb -p PID` 附加 → `thread apply all bt` 看哪个线程卡在什么系统调用/锁上 |
| 怎么调试多线程程序？ | `info threads` 看布局 → `thread N` 切线程 → `thread apply all bt` 全局快照 |

---

## 四、本项目的常用调试锚点

| 位置 | 拦截什么 |
|:--|:--|
| `HttpServer::onMessage` (cpp:89) | HTTP 请求入口 |
| `HttpServer::handleRequest` (cpp:177) | 路由分发前 |
| `HttpContext::parseRequest` (cpp:11) | HTTP 解析状态机 |
| `SessionManager::getSession` (cpp:18) | 会话创建/获取 |
| `Router::route` (cpp:21) | 路由匹配 |
| `LLMGateway::call` (cpp:282) | LLM 网关入口 |
