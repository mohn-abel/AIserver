# 基于 Muduo 的 LLM 服务网关系统：校招面试追问清单

项目关键词：C++17、Muduo、Reactor、epoll、HTTP 框架、deferred response、SSE、业务线程池、LLMGateway、限流、超时、fallback、三态熔断、MySQL 连接池、RabbitMQ 异步落库、RAG、工具调用、多会话上下文。

使用方式：先准备“3 分钟项目介绍”，再按每个模块准备“正常链路 + 异常链路 + 为什么这么设计 + 如果重做怎么优化”。

回答标记说明：文中“当前实现”只描述源码已经具备的能力；“边界与演进”用于解释设计取舍，不应在面试中表述为已完成。

## 1. 项目整体介绍

### 1.1 三分钟项目介绍

**面试官：介绍一下你的 LLM 服务网关项目。**

**回答：** 这个项目是在开源 Muduo HTTP 框架和 AI Chat 业务基础上做的工程化改造。最初它能够演示聊天，但压测和真实慢调用暴露了几个问题：LLM 的同步 curl 会阻塞 I/O 线程；Session 共享状态在多线程下会崩溃；长连接复用下 PreparedStatement 服务端句柄泄漏；连接池持锁探活、FIFO 和 shared ownership 也不适合长时间并发运行；模型出口没有限流、超时、熔断和降级。

我的目标因此不是再增加一个聊天接口，而是把它改成位于 Chat 业务和远端模型之间的应用内 LLM 网关。底层是 Muduo HTTP 接入层，负责连接、HTTP 解析、路由、Session、deferred、业务线程池和数据库连接池；中间是 LLMGateway 与 AIStrategy，前者负责限流、错误分类、超时、熔断和 fallback，后者负责不同模型的请求/响应协议；上层是 Chat 业务，负责多会话、上下文、RAG、工具调用、统一 SSE 和 MQ 异步落库。

两个最能体现我贡献的难点是：第一，把秒级 LLM 调用移出 Reactor I/O 线程，并通过 `runInLoop` 保证 SSE 最终在连接所属 I/O 线程发送；第二，从 login 压测的固定失败数定位到 PreparedStatement 泄漏，再把查询资源和连接池所有权改为 RAII，并将连接池重构为 deque 头冷尾热、LIFO 业务复用和锁外保序探活。最终项目有了统一模型出口和更清晰的资源生命周期，但我不会把它描述成完整生产级网关：分布式限流、SSE 背压/取消、MQ 可靠投递和会话淘汰仍是后续方向。

**连环追问 1：这个项目解决的核心问题是什么？**

核心是如何用 C++ 构建一个稳定、可扩展、可治理的 AI 模型接入层：既不让慢模型拖住网络线程，又能在多模型、流式响应和数据库/MQ 等外部资源之间建立清晰的线程边界、所有权与失败边界。

**连环追问 2：为什么叫网关，而不是普通后端？**

因为它承担统一模型出口：业务只表达要调用的模型和会话语义，网关集中处理 URL、Key、限流、超时、错误分类、熔断和 fallback；协议差异由策略层收敛。它目前是单进程应用内网关，不是独立分布式 API Gateway。

**连环追问 3：从客户端到模型结果经过哪些模块？**

浏览器 POST -> Muduo TcpServer/TcpConnection -> HTTP parser -> Session/中间件 -> router/Chat handler -> deferred + 业务线程池 -> 会话级 AIHelper -> AIStrategy -> LLMGateway -> 远端 LLM/RAG；返回方向是上游 chunk -> strategy 解析 -> SSE 编码 -> runInLoop -> 原 I/O EventLoop -> 浏览器。用户与助手消息另走 RabbitMQ -> 消费线程 -> MySQL 旁路。

**连环追问 4：如果只讲一个技术难点，讲哪个？**

优先讲“异步 SSE 的线程边界”，因为它能串起 Reactor、eventfd、pendingFunctors、TcpConnection outputBuffer、慢客户端和断连取消；如果面试官偏 C++ 资源管理，则讲 PreparedStatement 与 ResultSet 生命周期绑定，以及 unique_ptr 连接池借还。路由十万级 QPS 只能作为 HTTP 框架基线，不能作为 LLM 吞吐。

### 1.2 项目真实性追问

**面试官：这个项目是你从 0 到 1 写的吗？参考了哪些实现？**

**回答：** 不是完全从零实现。项目底座来自开源的 Muduo HTTP 框架和 AI Chat 业务，我使用 Muduo、libcurl、MySQL Connector/C++、RabbitMQ 客户端等第三方组件。我的核心工作是先通过压测暴露原系统在多线程 Session、PreparedStatement、连接池和慢 LLM 调用上的问题，再完成异步响应、网关治理、模型策略、统一 SSE 和异步落库等工程化改造。

Muduo 作为库直接使用，我没有把修改 Muduo 源码当作项目贡献；HTTP 层是在原有封装上扩展 deferred 和 SSE。OpenAI 风格响应参考其 `data:` 事件约定，但 RAG 与工具并非 mock：RAG 对接阿里云应用接口，天气等工具调用真实外部接口；工具机制本质是 prompt-based tool routing，不是原生 function calling 或 MCP。压测脚本、服务源码和改动记录都在同一仓库。面试时应把“开源基础、第三方能力、本人改造”明确分开。

连环追问：
- **Muduo 是直接使用库，还是改过源码？**
  答：直接使用 Muduo 库，没有修改其源码；项目工作集中在其上层 HTTP、异步响应和业务治理。
- **HTTP 框架是自己封装的，还是使用 Muduo example 改的？**
  答：底座来自开源项目对 Muduo 的 HTTP 封装，我在此基础上扩展 deferred、SSE、线程池和业务模块，不表述为完全自研。
- **OpenAI 风格 SSE 是否参考官方接口格式？**
  答：参考了 `data: JSON` 和 `[DONE]` 的通用格式，但对浏览器输出由本项目重新统一编码。
- **RAG 和工具调用是真实调用外部服务，还是 mock？**
  答：RAG 对接阿里云应用接口，天气等工具也调用真实外部服务；工具路由是提示词 JSON，不是原生 function calling。
- **多模型配置里接入了哪些真实模型？**
  答：普通策略面向 Qwen、DeepSeek 等 OpenAI-compatible 模型，另有阿里云 RAG 应用策略；实际可用性取决于配置的 URL 和 Key。
- **压测代码和业务代码是否在同一个仓库？**
  答：在同一仓库，README、Lua/压测记录与服务代码可互相对应。

注意：
- 不要把参考开源思想说成完全原创。
- 校招面试更看重你是否理解每个模块，而不是是否“全是自己发明”。

### 1.3 关键 Trade-off 小结

**面试官：这个项目里有哪些你主动做出的设计取舍？**

**回答：** 我不会把所有方案描述成“更好”，而是按当前单机 LLM 网关的风险优先级做取舍：先保证 Reactor 不被慢调用阻塞、资源所有权清晰和后端故障可隔离；再用可接受的最终一致性换主链路稳定；对分布式治理、可靠投递和复杂 Agent 协议保留明确演进边界。

| 选择 | 获得的收益 | 付出的代价 / 边界 | 详见 |
|---|---|---|---|
| LLM 调用转业务线程池，完成后 `runInLoop` 回 I/O 线程发送 | I/O loop 不被秒级调用阻塞，连接状态保持线程归属 | 线程池排队、上下文切换；当前队列仍需有界化 | 3.2、3.3 |
| SSE 而非 WebSocket | 适配 LLM 服务端单向文本流，复用 HTTP 基础设施 | 不适合高频双向交互；心跳、重连和背压需补齐 | 5.1、5.2 |
| 非流式可 fallback，流式已输出后不切换 | 避免不同模型拼接造成语义断裂 | 流中失败只能发流内错误并结束 | 6.1、6.3 |
| 双层惰性令牌桶 | 同时保护用户公平和后端配额，且不额外创建补充线程 | 单机状态，多实例会放大总额度；空闲桶需 TTL 清理 | 6.2、16 |
| 失败计数加时间衰减，而非滑动失败率窗口 | 覆盖典型连续后端故障，状态与并发实现更轻 | 不识别非连续高失败率或慢调用比例 | 7.1 |
| `unique_ptr + deleter`、LIFO、锁外 ping 管理连接池 | 独占会话语义清晰，热连接复用且慢 ping 不阻塞借还 | shutdown 生命周期和冷连接维护更复杂 | 8.1-8.5 |
| MQ 异步落库，而非同步写库 | 模型/SSE 主链路不等待数据库，隔离慢库并削峰 | 最终一致；可靠投递、幂等和故障恢复仍需完善 | 9.1-9.3、16 |
| 策略层隔离厂商协议，当前工具采用 prompt 路由 | Handler 不被协议差异污染，低成本接入工具能力 | 新协议仍需 adapter；工具格式受 prompt 漂移影响 | 10.1、11.2、18.8 |
| 首轮加最近 N 轮构造模型上下文 | 实现简单，限制单次 prompt 的历史范围 | 非 token 精确控制，内存历史仍全量保留，不涉及完整上下文工程 | 11.3、16 |
| 单进程启动加载配置与本地治理状态 | 实现和调试成本低，适合工程原型 | 多实例限流/熔断不一致，热更新不完整 | 6.2、7.2、10.2、16 |

面试时优先展开“线程边界、熔断取舍、连接池、MQ、工具调用”五项；其余作为被追问后的补充。

## 2. 整体架构与请求链路

### 2.1 架构图讲解

**面试官：画一下系统架构。**

**回答：** 架构可以分成主链路与旁路。主链路是“浏览器 -> Muduo TCP/HTTP 接入 -> 中间件与路由 -> Chat handler -> deferred/业务线程池 -> AIHelper 编排 -> AIStrategy 协议适配 -> LLMGateway 治理 -> 远端模型”；响应沿“远端 chunk -> 策略解析 -> SSE 编码 -> 原 I/O EventLoop -> 浏览器”返回。旁路是“AIHelper 生成聊天事件 -> RabbitMQ -> 消费线程 -> MySQL 连接池 -> MySQL”。

新连接先由主 Reactor 的 Acceptor 接受，再由 TcpServer 轮询分给某个 sub Reactor；完整 HTTP 请求的解析、鉴权和路由发生在连接所属 I/O 线程。Chat handler 标记 deferred 后才把阻塞 LLM 调用交给业务线程。`LLMGateway` 管出口治理，`AIStrategy` 管厂商协议差异，二者都不应承担会话业务。MySQL 连接池既服务登录/会话等同步数据库访问，也服务 MQ 消费落库。

连环追问：
- **客户端请求进入后，首先由哪个线程处理？**
  答：新连接先由 main Reactor accept；连接建立后，其 HTTP 读写由轮询选中的 sub Reactor I/O 线程处理。
- **路由匹配在哪里完成？**
  答：I/O 线程解析出完整 HTTP 请求后执行中间件和路由匹配。
- **什么时候进入业务线程池？**
  答：Chat handler 标记 deferred 后，将可能阻塞的 LLM 编排任务提交业务线程池。
- **LLMGateway 负责什么？**
  答：负责出口限流、熔断准入、超时、HTTP 错误分类和非流式 fallback。
- **AIStrategy 负责什么？**
  答：负责不同厂商的请求构造、普通响应解析和流式 chunk 解析，不负责限流与会话。
- **RabbitMQ 异步落库在主链路还是旁路？**
  答：是旁路；模型回答不等待 MySQL 写入完成。
- **MySQL 连接池被哪些模块使用？**
  答：登录/注册等同步数据库接口和 RabbitMQ 落库消费者都会借用连接池连接。
- **SSE 流式响应的 chunk 是在哪个线程产生、哪个线程发送？**
  答：curl/strategy 在业务线程产生，最终由连接所属 I/O 线程发送。

应覆盖的模块：
- Muduo EventLoop / TcpConnection。
- HTTP request parser / router / handler。
- Deferred response。
- Business thread pool。
- LLMGateway。
- AIStrategy。
- MySQL connection pool。
- RabbitMQ producer/consumer。
- Session context manager。

### 2.2 完整链路追问

**面试官：以一次流式对话请求为例，讲完整调用链。**

**回答：** 浏览器把用户问题、会话 ID、模型类型和工具开关放入 POST body。连接所属 I/O 线程从 `TcpConnection` 输入缓冲区增量解析请求行、请求头和 body，经 Session 鉴权与路由进入 Chat handler。handler 调用 `setDeferred()`，保存必要上下文并向业务线程池投递任务，随后 I/O 线程立即回到事件循环。

业务线程按 `userId -> sessionId` 找到会话级 `AIHelper`，读取历史并按当前规则保留首轮和最近窗口；工厂按 `modelType` 选择策略，网关在调用后端前依次执行用户限流、后端限流和熔断准入，再用 curl 发起带连接/总超时的请求。上游每产生完整流片段，strategy 提取文本，handler 编码为 SSE，并用 `runInLoop` 投回连接所属 I/O 线程发送。完成后发送 `[DONE]`，当前实现关闭本次流连接；用户消息在调用前、助手完整结果在结束后分别发布到 MQ。

如果上游在首个数据前失败，可以返回网关错误；已经发送部分 SSE 后只能发送流内错误并结束，不能无缝切模型覆盖已输出文本。客户端断开后，当前发送侧需要检查连接；将断连进一步传递为 curl 取消令牌尚未完整实现，这是当前生命周期管理的缺口。

连环追问：
- **HTTP 请求如何解析出用户 ID、会话 ID、prompt？**
  答：HTTP parser 解析 JSON body；用户身份还应由 Session 鉴权确认，不能只信任 body 中的 userId。
- **会话上下文如何读取和拼接？**
  答：按 userId 和 sessionId 找到 AIHelper，从 messages 读取历史并按首轮加最近窗口构造策略请求。
- **如何选择模型后端？**
  答：前端传 modelType，AIFactory 解析 alias/config 创建策略，Gateway 再按 primary 和 fallback 配置访问后端。
- **限流在哪一步做？**
  答：用户限流在网关入口做，后端限流在每次尝试具体 backend 前做。
- **超时计时从什么时候开始？**
  答：当前 curl 连接/总超时从实际外部调用开始；生产级总 deadline 还应覆盖线程池排队。
- **LLM 返回一段 chunk 后，如何推送给前端？**
  答：curl 回调缓存并拆出完整片段，strategy 提取文本，handler 编成 SSE 后 runInLoop 回投发送。
- **最终对话记录什么时候落库？**
  答：用户消息在模型调用前发布；助手消息在流完成并汇总 fullResult 后发布，由 MQ 消费者异步落库。
- **如果中途模型失败，客户端收到什么？**
  答：未输出前可返回网关错误；已输出 SSE 后发送流内错误并结束。
- **如果客户端主动断开，后台任务怎么处理？**
  答：当前发送侧需检查连接，完整的断连到 curl 取消链路尚未落地；生产上应传播取消令牌。

校招考点：
- 能否讲清楚同步入口和异步出口。
- 能否讲清楚正常流程和失败流程。

## 3. Muduo、Reactor 与线程模型

### 3.1 Muduo 基础

**面试官：为什么选择 Muduo？你用了 Muduo 的哪些能力？**

**回答：** 我使用 Muduo 的核心原因是它把 epoll 事件分发、连接生命周期、缓冲区、定时器以及 one loop per thread 的线程归属封装成了稳定的网络库。本项目使用 `TcpServer` 接收和分配连接，`EventLoop/Poller` 等待事件，`Channel` 绑定 fd 与回调，`TcpConnection/Buffer` 管理连接状态和输入输出数据，并使用 `runInLoop` 完成跨线程任务回投。

主 Reactor 负责监听 fd 和 accept，sub Reactor 负责已连接 socket 的读写。N 个 I/O 线程通常对应 N 个 sub EventLoop。`listenfd` 是监听文件描述符，`connfd` 是 accept 后某条客户端连接的文件描述符，`Channel` 是这个 fd 的事件通道对象。Muduo 降低了手写 epoll 时 fd 生命周期、并发关闭和缓冲区管理的复杂度，但业务仍必须负责 HTTP 解析、慢任务调度、背压、超时与断连取消。

Reactor 是内核通知“已就绪”后应用自己读写；Proactor 是异步 I/O 完成后通知结果。非阻塞 fd 保证单次 read/write 不会卡住 EventLoop；LT 在条件持续满足时重复通知，ET 只在状态变化时通知并要求循环处理到 `EAGAIN`。

连环追问：
- **Muduo 的核心思想是什么？**
  答：Reactor + one loop per thread：I/O 就绪后由固定 EventLoop 串行处理连接事件。
- **one loop per thread 是什么？**
  答：每个 I/O 线程运行一个 EventLoop，同一连接固定归属其中一个 loop。
- **Acceptor、EventLoop、Channel、Poller、TcpConnection 分别做什么？**
  答：Acceptor 接受连接；EventLoop 调度事件；Channel 封装 fd 事件与回调；Poller 封装 epoll；TcpConnection 管连接状态和缓冲区。
- **Muduo 为什么适合高并发网络服务？**
  答：非阻塞 I/O、epoll 和连接线程归属减少线程数及同步竞争，适合大量连接少量活跃事件。
- **Muduo 和你自己手写 epoll 框架相比，优势在哪里？**
  答：它已处理连接生命周期、Buffer、跨线程唤醒、定时器和关闭边界，降低自研出错面。
- **你是否理解 Muduo 的线程模型，而不是只会调接口？**
  答：理解。Muduo 的核心是 one loop per thread：主线程运行 mainLoop，负责监听 `listenfd` 和接受新连接；每个 I/O 线程各自运行一个 subLoop，一条连接一旦分配给某个 subLoop，后续读写、关闭以及 Channel 事件更新都固定由该线程处理。具体可分为三个阶段：

  1. **连接建立与分配：** 内核完成三次握手后令 `listenfd` 可读，mainLoop 中的 Acceptor 调用 `accept` 获得 `connfd`。`TcpServer::newConnection` 通过线程池轮询选择一个 I/O EventLoop，并创建绑定该 loop 的 `TcpConnection`。随后使用 `runInLoop/queueInLoop` 把 `connectEstablished` 投入目标 loop 的 `pendingFunctors`。因为这是跨线程投递，目标 loop 的 `wakeup()` 会向它自己的 `eventfd` 写入计数，唤醒阻塞在 `epoll_wait` 的 I/O 线程。该线程处理 wakeup Channel 后执行 `doPendingFunctors`，在 `connectEstablished` 中调用 `Channel::enableReading()`，最终通过 `epoll_ctl` 将 `connfd` 的读事件注册到该 subLoop 的 Poller。

  2. **连接数据到达：** 客户端数据进入该 socket 的内核接收缓冲区后，是 **`connfd` 自身变为可读**，内核将其加入 epoll 就绪集合，`epoll_wait` 返回对应的 active Channel；这里不需要 `eventfd`，也不是先读取 `pendingFunctors`。EventLoop 调用 `Channel::handleEvent`，再触发绑定的 `TcpConnection::handleRead`；`Buffer::readFd` 把字节从内核 socket 接收缓冲区读入用户态 `inputBuffer_`，随后调用 messageCallback，把数据交给上层 HTTP 协议解析器做增量解析。

  3. **跨线程业务回写：** 如果业务线程完成 LLM 调用后要发送 SSE，它不能直接修改连接的 outputBuffer 和 Channel，而是再次通过 `conn->getLoop()->runInLoop()` 投递发送任务。此时才会复用 `pendingFunctors + eventfd` 的跨线程唤醒机制，让发送最终回到连接所属 I/O 线程执行。

  因此需要修正一种混淆：`eventfd` 唤醒是“其他线程给 EventLoop 投递任务”；`connfd` 可读唤醒是“客户端网络数据到达”。两者都可能让 `epoll_wait` 返回，但对应不同的 Channel 和后续处理路径。

八股延伸：
- **Reactor 模式是什么？**
  答：等待 I/O 就绪事件，再把事件分发给注册的回调，由应用执行实际读写。
- **主 Reactor 和从 Reactor 分别负责什么？**
  答：主 Reactor 监听并 accept；从 Reactor 处理已建立连接的读写、关闭和回投任务。
- **Reactor 和 Proactor 区别是什么？**
  答：Reactor 通知就绪后应用读写；Proactor 由异步 I/O 完成后通知结果。
- **epoll LT 和 ET 区别是什么？**
  答：LT 在条件仍满足时持续通知，调用 epoll_wait 就会继续返回该事件，支持部分读写与阻塞/非阻塞IO，但是还是非阻塞更推荐使用；ET 只在状态变化时通知，必须循环处理到 EAGAIN，因此强制要求使用非阻塞IO，否则循环读会将当前 EventLoop 阻塞在某个客户端上，等待该客户端发生下一次事件，无法处理其他客户端事件。
  可写事件通常只在 outputBuffer 有剩余数据时注册，数据全部写完后立即取消，避免因为 socket 长期可写导致 epoll 持续唤醒。
- **为什么网络 fd 要设置非阻塞？**
  答：防止一次 accept/read/write 因对端慢而卡住整个 EventLoop。

### 3.2 I/O 线程不能阻塞

**面试官：为什么不能在 Muduo I/O 线程里直接调用 LLM？**

**回答：** libcurl 的同步调用会等待 DNS、建连、后端排队、首 token 和推理完成，延迟可能从数秒到数十秒。一个 EventLoop 通常同时管理很多连接；它一旦被某个 LLM 请求阻塞，同 loop 上其他连接的读、写、关闭回调和定时器都无法及时执行，表现为整批连接延迟尖峰而不是只慢一个用户。

所以 I/O 线程只完成短小的协议解析和任务投递，LLM 调用交给有界业务线程池。业务完成后再回到原 EventLoop 发送。线程池也不是无限容量的解决方案：需要队列上限、排队超时和拒绝策略，否则慢下游只会把阻塞从 I/O 线程搬到无限任务队列。

连环追问：
- **LLM 调用为什么是阻塞或高延迟操作？**
  答：其本质上是一次外部服务调用，同步 curl 要等待 DNS、连接、远端排队、首 token 和模型推理，延迟不可控。
- **如果 I/O 线程阻塞，会影响哪些连接？**
  答：影响归属同一个 EventLoop 的全部连接，不只当前请求。
- **一个 EventLoop 通常管理多少连接？**
  答：没有固定值，由 fd 上限、内存、业务负载和活跃率决定；一个 loop 通常管理很多连接。
- **I/O 线程阻塞后，读写事件还能及时处理吗？**
  答：不能，epoll 返回的事件和 pending functors 都要等阻塞调用结束。
- **定时器、超时检测会不会被影响？**
  答：会，TimerQueue 回调也依赖 EventLoop 获得执行机会。
- **如果模型调用偶尔 5 秒才返回，系统会出现什么现象？**
  答：同 loop 连接出现成片延迟、超时和发送积压，吞吐明显下降。

建议回答：
- I/O 线程应该只做短小、非阻塞的网络事件处理。
- LLM 调用属于外部 I/O，延迟不可控，应交给业务线程池。
- 业务完成后再通过 `runInLoop` 或 `queueInLoop` 回到连接所属 EventLoop 发送响应。

### 3.3 跨线程发送响应

**面试官：为什么业务线程处理完后，要通过 `runInLoop` 回到 EventLoop 发送响应？**

**回答：** 一条 `TcpConnection` 归属固定 EventLoop，它的 `Channel`、`outputBuffer_`、是否监听 `EPOLLOUT` 和关闭状态都应由该 I/O 线程串行修改。业务线程直接 `send` 时，如果内核发送缓冲区满而返回 `EAGAIN`，发送逻辑要将未发送完的数据追加至 Muduo outputBuffer 并修改 Channel 的可写事件；此时 I/O 线程可能同时处理该连接的可读、可写或关闭事件，产生数据竞争如缓冲区竞争、事件掩码错乱和关闭后写入。

`runInLoop` 在异线程调用时把 lambda 放入目标 loop 的 `pendingFunctors`，通过 eventfd 唤醒阻塞在 `epoll_wait` 的 I/O 线程；若调用者本来就在目标 loop，则立即执行。`queueInLoop` 总是排队到后续循环。当前代码在长时间业务任务中捕获 `TcpConnectionPtr`，会在客户端断开后继续延长连接对象生命周期。更合理的方式是长任务保存 `weak_ptr` 和独立的请求上下文，真正发送时再将 `weak_ptr` 临时提升为局部 `shared_ptr`，并检查连接与请求状态；close callback 负责触发上游取消。

连环追问：
- **TcpConnection 是否线程安全？**
  答：不能理解为所有方法和内部状态都可任意并发操作；其 I/O 状态设计上归属固定 EventLoop，在使用时可以无锁，这也是为什么不要让除了IO线程以外的其他线程再对TCPconnection对象进行读写操作。
- **如果业务线程直接调用 send，会有什么风险？**
  答：可能与 I/O 线程并发修改 outputBuffer、Channel 事件和关闭状态。
- **连接可能已经关闭了怎么办？**
  答：长时间业务任务不应强持有 `TcpConnection`，而应保存 `weak_ptr` 和独立的 `RequestContext`。发送回调真正执行时，先确认请求未取消，再通过 `weak_ptr::lock()` 获得仅在本次回调期间存活的 `shared_ptr`，并检查 `conn->connected()`。锁定失败或连接已断开时，只执行取消、记录和资源收尾，不再尝试向客户端发送失败响应。请求的成功、失败、取消和资源回收由独立 `RequestContext` 管理，不能依赖延长连接对象生命周期。weak_ptr解决连接生命周期问题，而RequestContext解决从连接断开到发送的这部分资源开销收敛的问题
- **如何避免悬空指针？**
  答：不捕获裸连接指针。长期保存时使用 `weak_ptr`；需要访问连接时临时 `lock()` 成局部 `shared_ptr`，保证对象在这一次检查和操作期间不会析构。
- **回调里应该捕获 `shared_ptr` 还是 `weak_ptr`？**
  答：应按“强引用需要持有多久”和“是否拥有连接”区分，而不是按长连接、短连接区分。LLM 调用、deferred 任务等长时间业务任务捕获 `weak_ptr`，避免客户端断开后仍因强引用保留无用连接对象；已回投到原 EventLoop 的短发送回调，可以在执行时从 `weak_ptr` 临时提升为局部 `shared_ptr`，保证本次回调内对象稳定。直接捕获 `shared_ptr` 只适合确定极短且有意延长对象生命周期的操作，但仍必须检查 `connected()`。普通的短任务依旧是最好不使用shared_ptr延长其生命周期，统一原则是：长期弱持有，使用时临时强持有。
- **`runInLoop` 和 `queueInLoop` 的区别是什么？**
  答：runInLoop 在目标线程内立即执行，否则排队；queueInLoop 无条件排到后续循环。
- **如果当前线程就是连接所属 EventLoop，`runInLoop` 会怎么执行？**
  答：直接执行回调，不经过 eventfd 唤醒。

风险点：
- 要能说清“连接归属线程”和“跨线程操作连接对象”的问题。
- 校招不一定要求背 Muduo 源码，但要理解线程安全边界。

### 3.4 线程与连接池容量

**面试官：当前项目的 I/O 线程、业务线程和数据库连接数如何设置？为什么不能只套 CPU 核数公式？**

**回答：** 当前运行参数是：主 Reactor 1 个、Muduo I/O 子线程 4 个（`main.cpp` 的 `setThreadNum(4)`）、聊天业务线程池 8 个（`ChatServer.cpp` 的 `ThreadPool(8)`）、RabbitMQ 消费线程 2 个、MySQL 连接池启动时预建 100 条连接。前四项分别承担网络事件循环、LLM/工具等慢业务、异步落库；连接池大小是可用数据库会话数，不等同于 CPU 线程数。

当前环境是 8 核 8G VM。4 个 Muduo I/O 子线程是网络层的保守起点：它们使用非阻塞 epoll，只处理收发、HTTP 解析和任务投递，不能将“阻塞 I/O 线程取 2 倍核心数”的经验直接套到这里。真正等待远端 LLM 的是业务线程池中的同步 curl；它属于外部 I/O 等待型任务，当前 8 是安全起点。若压测显示业务队列等待明显、CPU 仍有余量、模型限额未触顶，可按 12、16 做阶梯压测；生产建议先以 12 为候选，而不是声称当前已经验证出唯一最优值。

常见估算 `N = cores x (1 + W/C)` 中，C 是本地计算时间，W 是等待外部 I/O 的时间。CPU 密集任务 W/C 接近 0，线程数通常接近核心数；阻塞 I/O 任务可更多。2 倍核心数只隐含 W/C 约为 1，不是通用结论。LLM 调用的 W/C 可能非常高，但不能据此创建几百个阻塞线程，还要受后端并发/配额、内存、上下文切换、有界队列和请求 deadline 限制。更实用的是先用 Little 定律估算在途需求：目标 QPS x 平均 LLM 耗时，再取下游许可与本地资源可承受范围内的较小值。

MySQL 的 100 是此前登录压测时为排除“连接池获取等待”而设置的实验参数；当前实现会启动即预建全部 100 条，不是按需增长到 100。它不应直接作为聊天运行配置：聊天主链路不长期持有数据库连接，稳定写库主要由 2 个 MQ 消费线程完成。8 核 8G 的聊天运行基线可先取 16 条，再按活跃连接、获取等待、SQL P99、MySQL `Threads_connected/Threads_running` 和 `max_connections` 做 8 -> 16 -> 32 -> 64 的阶梯压测。对于当前同步登录 Handler，数据库查询还会阻塞所属 I/O 线程，因此把连接池从 16 增至 100 不必然提高 QPS。

连环追问：
- **CPU 密集和 I/O 密集如何从数据判断？**
  答：CPU 接近满核、运行队列增长且火焰图集中在本地计算时偏 CPU 密集；CPU 尚有余量但业务线程大量等待 curl/MySQL、线程池队列和外部调用时延上升时偏 I/O 等待密集。
- **什么时候增加 Muduo I/O 线程？**
  答：只在 EventLoop CPU、事件循环延迟、就绪事件积压或网络读写成为瓶颈时增加；LLM 首 token 慢不能靠增加 I/O loop 解决。
- **什么时候增加业务线程？**
  答：业务线程持续满载、队列等待增加，而 CPU、内存和后端限流仍有余量时；增加后应比较吞吐、P99、拒绝率和上下文切换，而不是只看 QPS。
- **为什么不把 DB 连接池长期设为 100？**
  答：预建连接会占 MySQL 的连接、线程和内存预算，多实例时会放大；若活跃并发远小于 100，只会浪费资源，且无法解决 I/O 线程中同步 SQL 的阻塞问题。
- **当前线程池还有什么生产级缺口？**
  答：当前任务队列无界，尚未实现队列容量、排队超时、拒绝策略和按 CPU/阻塞 I/O 任务分池；扩容前应先补这些背压机制。

## 4. HTTP 框架与异步响应

### 4.1 HTTP 路由

**面试官：你的 HTTP 框架路由是怎么设计的？**

**回答：** 路由以 HTTP method 和 path 为匹配条件。静态路由依次查询 Handler 表和 Callback 表；静态未命中后，才线性遍历正则 Handler 和正则 Callback。动态路径会在注册时将路径模式编译为 `std::regex`。对象 Handler 分支命中后会把捕获组写入复制的 `HttpRequest`；正则 Callback 分支虽然也构造了 `newReq`，但当前源码误将原始 `req` 传给 callback，导致路径参数没有真正传入，这是现有 bug。框架实现了正则路由能力，但当前 ChatServer 的业务路由全部通过 `Get/Post` 静态注册，没有调用 `addRoute` 注册动态正则路由，因此当前没有真实业务正则路由及其压测结果。当前注册接口也没有完整冲突检测：相同静态 key 在同一 map 中会被覆盖，而 Handler 表和 Callback 表可能同时保留且 Handler 优先；重复正则规则会继续追加并由先注册者先命中。路由查找是短小的内存计算，可以在 I/O 线程执行；命中后的阻塞业务必须转出 I/O 线程。

连环追问：
- **静态路由和正则路由分别怎么匹配？**
  答：静态路由直接按 method/path 查找；正则路由运行预编译规则并提取捕获组。
- **正则路由是否预编译？**
  答：是。`addRegexHandler/addRegexCallback` 在注册时调用 `convertToRegex` 构造 `std::regex` 并保存在 vector 中，请求时只执行匹配；但当前业务层没有实际注册正则路由。
- **路由冲突如何处理？**
  答：当前没有显式冲突检测。同一静态 map 的相同 key 会覆盖；Handler 与 Callback 两张表的相同 key 可同时存在且 Handler 优先；正则重复注册会追加，先注册的规则先匹配。生产级应在注册阶段拒绝歧义或定义明确优先级。
- **HTTP method 是否参与匹配？**
  答：参与，同一路径可分别注册 GET、POST 等 handler。
- **路径参数如何提取？**
  答：对象 Handler 分支会把捕获组写入 `newReq`；Callback 分支当前错误地调用原 `req`，路径参数会丢失，需要改为 `callback(newReq, resp)`。
- **404、405、500 如何返回？**
  答：当前 `Router::route` 只返回是否命中，HttpServer 未命中统一返回 404，没有实现 405 区分；handler 异常会被转换为 500。若要支持 405，需要在 method+path 未命中后再判断该 path 是否存在于其他 method。
- **路由匹配在 I/O 线程里做是否安全？**
  答：短小且规则只读时安全；阻塞业务和动态修改路由不能直接占用 I/O 线程。

压测追问：
- **README 中约 12.25W 和 11.24W QPS 是静态路由与正则路由的对比吗？**
  答：不是。约 12.25W 测的是静态 `/entry`；约 11.24W 测的是同样通过 `Get` 注册的静态 `/menu`，后者额外经过 Session 鉴权。源码中没有业务 `addRoute` 调用，也没有正则路由压测脚本，因此不能把两组数据的差异归因于正则匹配。
- **正则路由是否一定会比静态路由慢很多？**
  答：理论上静态哈希查找平均 O(1)，当前正则路由则在静态表未命中后线性扫描规则并执行 `std::regex_match`，规则越多、命中越靠后或全部不命中，成本越高。但端到端差距未必必然很大：当规则很少且 HTTP 解析、网络写回和 handler 占主要成本时，差距可能被稀释。必须使用相同 handler、相同响应体、相同中间件和连接参数，并测试不同正则规则数量与命中位置，才能得出结论。
- **wrk 参数是什么？**
  答：典型为 `-t4 -c200 -d30s`，分别是客户端线程、并发连接和持续时间。
- **压测接口是否真的调用 LLM？**
  答：`/entry` 和 `/menu` 都没有调用 LLM；`/login` 访问 MySQL，但也不调用 LLM。这些数据不能代表模型吞吐。
- **CPU 是否打满？**
  答：原记录不足以给出可靠结论；未同步采集 CPU 时不能声称已找到饱和瓶颈。
- **P99 延迟是多少？**
  答：原记录未形成可信 P99，应明确缺失并在重测时补充分位数和错误率。

### 4.2 Deferred response

**面试官：你说设计了 deferred 异步响应机制，具体是什么？**

**回答：** 普通响应即同步要求 handler 返回前填好 `HttpResponse`，`HttpServer::onRequest()` 随即发送；deferred 则由 handler 显式标记“当前栈帧不发送”，把完成响应的责任转交给后台任务。Chat handler 标记 deferred 后提交业务线程池，业务完成时通过连接所属 loop 发送 header、chunk、error 和 end。

当前 `HttpResponse` 保存连接引用，handler 捕获必要请求数据和连接对象。这个机制解决了 I/O 线程阻塞，但当前还不是完整的生产级异步请求状态机：应增加原子 `completed` 状态，确保正常完成、超时和断连只有一个路径能收尾；连接关闭要通知请求取消；后台异常要转为一次错误响应；排队超过 deadline 的任务应在执行前取消。deferred 析构只应释放本地状态，不能隐式再发送一次响应。

连环追问：
- **普通同步响应和 deferred 响应有什么区别？**
  答：同步 handler 返回即发送；deferred 让当前栈帧不发送，由后台任务后续完成。
- **handler 返回时，请求对象和连接对象谁持有？**
  答：任务复制所需请求字段，并通过连接智能指针/响应对象维持连接引用；不能依赖栈上 request。
- **deferred 标志所在的 HttpResponse 已在 handler 返回后析构，后台异步任务靠什么继续完成、取消和收尾？**
  答：当前 deferred_ 只是同步发送路径的跳过标志，后台任务值捕获请求必要字段和 TcpConnectionPtr，完成后自行通过 runInLoop 发送 SSE。生产级应额外创建由后台任务、超时定时器和连接 close callback 共享的 RequestContext，用于管理异步请求的 deadline、取消状态和唯一终态；它管理的是请求生命周期，不是管理已经析构的 HttpResponse。
- **业务线程处理完后如何触发响应？**
  答：调用连接所属 loop 的 runInLoop，发送 SSE chunk 或结束事件。
- **超时后如何避免重复响应？**
  答：用原子 completed/状态机做 compare-exchange，只有第一个且唯一终态执行收尾。
- **客户端断开后 deferred 如何感知？**
  答：通过 TcpConnection close callback 标记 RequestContext cancelled。

异常追问：
- **后台任务完成时连接已关闭怎么办？**
  答：weak_ptr 锁定失败或状态为 disconnected 时丢弃发送，并取消后续上游工作。
- **后台任务抛异常怎么办？**
  答：线程池边界捕获异常，转换为一次错误响应和日志，不能让 worker 退出。
- **业务线程池排队太久，请求是否应该超时？**
  答：应该；总 deadline 应包含排队时间，执行前发现过期则直接取消。
- **如果同一个 deferred 被响应两次，如何防止？**
  答：使用一次性完成标志或明确状态机，发送前原子抢占终态。

## 5. SSE 流式响应

### 5.1 SSE 基础

**面试官：SSE 是什么？和 WebSocket 有什么区别？**

**回答：** SSE 是基于 HTTP 响应的服务端单向事件流，客户端发起一个请求，响应类型是 `text/event-stream`，一条事件通常是 `data: 内容\n\n`，需要保证当前连接在响应生成过程中不断开，并不是一次请求多次响应，而是一个响应拆分为多个事件流。WebSocket 通过 HTTP Upgrade 建立全双工通道，适合客户端和服务端都持续高频推送，且支持二进制帧；LLM 场景主要是服务端单向输出，所以 SSE 更简单且更容易经过常规 HTTP 基础设施，并不需要 websocket 的客户端主动推送消息功能，所以其双向全双工能力在当前闲置，SSE实现就已经足够覆盖需求。

具体区别：
SSE 是应用层事件格式，TCP 只提供可靠有序字节流。HTTP/1.1 keep-alive 表示同一 TCP 连接可承载多个 HTTP 请求；chunked 是未知总长度响应体的 HTTP 传输分帧；二者都不等同于 SSE。支持自动重连，数据格式主要是文本。
websocket 由HTTP请求发起握手升级协议，成功后不再按照HTTP请求和响应格式传输，而是切换至websocket的全双工帧协议；客户端和服务端都可以随时主动发送文本、二进制数据以及心跳等，但是需要手动处理重连问题。

连环追问：
- **SSE 基于什么协议？**
  答：基于 HTTP 响应和 TCP 字节流。
- **SSE 是单向还是双向？**
  答：服务端到客户端单向；客户端提问走独立 HTTP 请求。
- **SSE 的 `Content-Type` 是什么？**
  答：`text/event-stream`。
- **SSE 每条消息格式是什么？**
  答：字段按行书写，常用 `data: ...`，以空行结束一条事件。
- **浏览器断线后如何重连？**
  答：EventSource 可自动重连并携带 Last-Event-ID；本项目 POST 流需额外应用层重连设计。
- **SSE 是否适合 LLM 流式输出？为什么？**
  答：适合，因为 LLM 主要是服务端单向持续输出文本，协议简单且浏览器支持好。
- **WebSocket 在这个场景下有什么优势和劣势？**
  答：优势是全双工和帧控制；劣势是连接与代理管理更复杂，而当前不需要高频客户端推送。

八股延伸：
- HTTP/1.1 keep-alive。
- chunked transfer。
- 代理缓冲。
- 长连接资源占用。

### 5.2 项目中的 SSE 链路

**面试官：你的 OpenAI 风格 SSE 是怎么统一封装的？**

**回答：** 网关的 curl 写回调先缓存可能被 TCP/curl 拆开的半行，取得完整上游 `data:` 片段后交给 `AIStrategy::parseStreamChunk`。策略只负责从厂商格式提取统一文本，Chat handler 再编码成面向浏览器的 OpenAI风格 SSE，并回投原 I/O 线程。普通模型和 RAG 各自解析协议；工具调用的第一轮决策不对前端流式输出，工具执行后第二轮最终回答才走统一 SSE。

上游自然结束后发送 `[DONE]`。非流式后端可以等完整结果后作为一个 SSE 事件发送，但不是真正 token 流。已经发送部分内容后发生错误，可以发送专门的 error event 再结束，不能再改 HTTP 状态或安全 fallback。当前未形成心跳、断连取消和慢客户端背压的完整闭环。若客户端很慢，Muduo outputBuffer 可能无限增长；生产上需要高水位、最大流字节数、取消上游和 token 微批发送。

连环追问：
- **普通问答、RAG、工具调用结果如何统一成 SSE？**
  答：各策略先归一化文本 chunk，工具只流式输出第二轮最终答案，再由 handler 统一编码。
- **模型返回 chunk 后如何转换成 `data: ...`？**
  答：strategy 解析厂商 JSON，handler 对统一文本做 JSON/SSE 转义并加 `data:` 与双换行。
- **`[DONE]` 什么时候发送？**
  答：上游正常完成且所有已产生 chunk 已排入原 I/O loop 后发送。
- **如果模型不是流式返回，如何适配成统一格式？**
  答：等待完整结果后作为一个 SSE data 事件发送，再发送 DONE；这不是真正增量流。
- **中途失败时还能发送错误事件吗？**
  答：可以，若响应头已发就用 SSE error/data 表达，随后结束连接。
- **客户端断开后，模型请求是否继续？**
  答：当前可能继续；生产级应由 close callback 取消 curl。
- **是否做心跳？不做有什么问题？**
  答：当前没有完整心跳；长时间无 token 时代理可能按空闲超时关闭连接。
- **不同策略的具体差异在哪？详细说说请求和响应的格式差距**
  答：普通模型：请求包含： messages + stream=true；响应解析 choices[0].delta.content；
  RAG：input.prompt + parameters.incremental_output=true，额外请求头X-DashScope-SSE: enable；响应解析 output.text；
  
重点准备：
- SSE 不是简单 `while write`，必须处理连接生命周期、超时、断开、缓冲刷新。
- 要能说明“流式数据生成”和“网络发送”所在的线程边界。

## 6. LLMGateway 治理能力

### 6.1 为什么需要网关治理

**面试官：为什么模型调用需要限流、超时、fallback 和熔断？**

**回答：** 模型后端是不可控的远程资源，可能出现 DNS/连接失败、429、5xx、鉴权错误、长时间无首包或中途断流。在没有网关进行治理时是在请求构造完成后，直接使用curl调用LLM，限流解决“请求进入得太快”，超时解决“单次请求占用太久”，熔断解决“已知故障后端仍被持续请求”，fallback 解决“主后端失败时是否有可替代路径”。它们保护的是本地业务线程、供应商配额和故障隔离，不是四个重复功能。

单用户突发会占满业务线程和模型额度；单模型异常若没有按 backend 隔离，会拖累所有请求。超时太短会误伤正常长推理，太长会使线程和连接长期占用。非流式 fallback 可以重新调用备用模型并返回其完整结果；流式一旦吐出 token 就无法回滚，因此当前不做 fallback。降级可能带来模型能力、延迟和答案风格变化，所以应在配置中明确优先级并记录 `fallbackUsed`。

连环追问：
- **LLM 后端可能出现哪些故障？**
  答：DNS/TLS/连接失败、429、5xx、鉴权错误、首包慢、中途断流、非法 JSON 和业务 error。
- **单个用户请求过多会造成什么问题？**
  答：占满业务线程、模型配额和 SSE 连接，影响其他用户公平性。
- **单个模型后端异常会影响全局服务吗？**
  答：按 backend 独立限流熔断后不应影响其他模型；没有隔离时会拖累共享线程池。
- **超时设置过短或过长分别有什么问题？**
  答：过短误杀正常长推理，过长会长期占用线程、连接和供应商额度。
- **fallback 返回什么内容？**
  答：返回备用模型的完整业务响应，并记录实际 backend 与 fallbackUsed；不能返回内部路由细节。
- **熔断和限流的区别是什么？**
  答：限流按速率做准入保护容量；熔断依据失败健康度隔离故障后端。
- **降级是否会影响用户体验？**
  答：会，模型能力、风格和延迟可能变化，因此要有优先级、可观测性和必要提示。

### 6.2 双层令牌桶限流

**面试官：用户级和后端级双层令牌桶分别解决什么问题？**

**回答：** 用户桶按 userId 限制单个用户请求速率，避免滥用并保证公平；后端桶按 backend/model 限制全体用户对某个下游的进入速率，保护供应商配额与容量。当前顺序是入口先检查用户桶，每次尝试具体后端时再检查后端桶。令牌桶按经过时间惰性补充 `elapsed * rate`，不超过 burst 容量，允许有限突发；漏桶更强调固定速率流出和平滑。

用户桶通过但后端桶拒绝时，当前应返回网关限流错误，通常不归还用户令牌。令牌代表请求速率许可，请求已经到达并消耗网关资源，归还会使高频重试绕过削峰。远端模型拒绝时后端令牌通常也不归还，因为调用尝试已消耗本地/下游资源。这里必须把“请求限流”与“用户计费额度”分开：账务额度应由独立 QuotaManager 在得到实际 usage 或成功结果后结算。

多线程下每个桶内部用锁保护补充和扣减，桶映射的创建也要同步。`rate` 来自后端承诺吞吐或压测安全值，`burst` 表示允许的瞬时峰值。当前用户桶缺少闲置淘汰，长期运行会随 userId 增长；可记录最后访问时间，定时清理或使用分片/LRU。多实例部署时本地桶会把全局额度放大，需要 Redis/Lua 或集中限流服务。

连环追问：
- **令牌桶原理是什么？**
  答：按时间以固定速率补令牌且不超过容量，请求取得令牌才放行，允许有限突发。
- **令牌桶和漏桶区别是什么？**
  答：令牌桶允许容量范围内突发；漏桶强调按固定速率平滑流出。
- **用户级限流防什么？**
  答：防单用户滥用并保证多用户公平。
- **后端级限流防什么？**
  答：防全体流量超过某模型配额和承载能力。
- **两层限流的判断顺序是什么？**
  答：入口先检查用户桶，每次准备访问具体 backend 时再检查其后端桶。
- **如果用户桶通过、后端桶失败，返回什么？**
  答：返回可识别的网关限流错误；请求令牌通常不归还，计费额度不扣。
- **令牌桶多线程下怎么实现？**
  答：桶内锁保护惰性补充和扣减，桶映射的创建/淘汰另做分片同步。
- **为什么采用请求到来时惰性补充，而不是后台线程定时补充？**
  答：限流只在请求到来时需要判定。当前以 `elapsed * rate` 结算理论应补令牌，语义等价于持续补充，但不需要后台线程、周期唤醒和扫描所有用户/后端桶；空闲桶没有 CPU 成本。代价是空闲期的 tokens 不实时更新，需用 steady_clock 防时钟回拨，且若要展示实时余额或回收空闲桶仍需额外机制。
- **后台定时补充的优缺点是什么？**
  答：它使内存中的余额持续更新，便于展示或统一调度；但会产生定时唤醒、全量桶扫描，并与请求扣减路径增加同步竞争，周期边界还可能形成批量令牌释放。当前按需限流场景下收益不足。
- **参数如何设置：容量、生成速率、突发流量？**
  答：rate 取安全持续 RPS，capacity/burst 取允许短时峰值，再根据 429、队列和延迟回调参。

手撕延伸：
- **手写一个线程安全令牌桶。**
  答：使用 steady_clock、tokens/rate/capacity/lastRefill 和 mutex，获取时先补充再扣减。
- 支持按用户 ID 维护多个令牌桶。
- **如何清理长期不用的用户桶？**
  答：按 lastAccess 做周期扫描、TTL/LRU 淘汰，删除时与并发获取同步。

### 6.3 超时控制

**面试官：模型调用超时怎么做？**

**回答：** 当前在 `LLMGateway` 的 curl 调用中设置连接超时和请求总超时，超时发生在业务线程，不会阻塞 I/O EventLoop。curl 返回超时后网关把它分类为后端失败，必要时计入熔断；非流式可尝试 fallback，流式已输出后只能结束当前流。

更完整的超时应该有统一 deadline，并拆成业务线程池排队超时、连接超时、首包超时、流式空闲超时和总超时。假设当前请求的总预算是60s，所有阶段共享这个预算，并检查各阶段超时。

超时后需要具备取消机制并修改当前请求的状态。只在上层“认为超时”但不取消 curl，会让业务线程继续占用，所以底层必须可中断。生产级可用 curl progress/write callback 检查取消标记，超时、客户端断开以及服务停机都可使用requestContext的取消标记传播到各阶段流程中取消任务；同时用原子请求状态保证超时响应和迟到的正常结果只能一个终态成功收尾；迟到结果只能丢弃，不能重复发送响应造成竞态。
当前主要具备 curl 级超时，首包/包间超时、统一取消机制与状态机尚未完全落地。


连环追问：
- **超时由业务线程控制，还是 HTTP 框架控制？**
  答：当前主要由业务线程中的 curl 控制；生产级还应由请求级 deadline 统一覆盖排队和网络。
- **超时后底层请求能否取消？**
  答：curl 自身超时会中止；主动取消可在 progress/write callback 检查取消标记。
- **如果底层请求不能取消，业务线程会不会被占住？**
  答：会，直到 curl 自己返回，因此只在上层丢弃结果不够。
- **超时后模型又返回了结果，如何避免重复写响应？**
  答：请求状态用原子 CAS 从进行中转终态，迟到结果发现终态后丢弃。
- **超时是否计入熔断失败？**
  答：后端连接/响应超时通常计入；本地排队超时不应污染后端健康。
- **SSE 流式响应中，首包超时和两包之间超时是否区分？**
  答：生产上应区分首包 timeout 和 idle timeout；当前主要是连接/总超时，尚未细分完整。

### 6.4 HTTP 状态码业务失败识别

**面试官：你说会识别 HTTP 状态码业务失败，具体怎么识别？**

**回答：** `curl_easy_perform == CURLE_OK` 只说明 HTTP 传输完成，不代表业务成功，因此请求结束后还要读取响应状态码，并检查响应 JSON 的 error 结构。当前网关将错误归为客户端参数、鉴权、远端限流、后端不可用和未知错误，再决定是否计入熔断、是否可 fallback。

429 是远端限流；500 是被调服务内部错误；502 表示网关收到无效上游响应；503 表示服务不可用/过载；504 表示网关等待上游超时。400 等请求错误不应熔断或重试；401/403 通常是 Key/权限配置错误，应快速失败和告警，不能靠熔断掩盖；429、5xx、网络失败和超时可计入后端熔断计数并在非流式时 fallback。HTTP 200 但 body 含 error 或缺失必需字段也不是成功。对客户端应返回稳定、脱敏的网关错误，原始供应商状态和 body 只进入安全日志。

连环追问：
- **429、500、502、503、504 分别代表什么？**
  答：429 远端限流；500 服务内部错误；502 网关收到无效上游响应；503 服务不可用；504 网关等待上游超时。
- **401/403 是不是应该触发熔断？**
  答：通常不按瞬时健康故障熔断，应快速失败并告警配置/权限问题。
- **4xx 和 5xx 是否都算失败？**
  答：都不是成功，但治理语义不同：大多数 4xx 是请求/配置问题客户端侧问题，5xx 才体现后端服务可用性。
- **模型返回 200 但 JSON 里有 error，算不算失败？**
  答：算业务失败，不能被 HTTP 200 掩盖。
- **限流错误和服务异常错误是否应该区别处理？**
  答：应该，前者可提示稍后重试/Retry-After，后者进入可用性治理。
- **哪些错误应该 fallback，哪些应该直接返回？**
  答：网络、超时、429、5xx 可在未输出时 fallback；参数和鉴权错误通常直接返回。

## 7. 三态熔断

### 7.1 熔断状态机

**面试官：三态熔断是哪三态？状态如何转换？**

**回答：** `CLOSED` 正常放行并累计失败；达到阈值转 `OPEN`。`OPEN` 在恢复时间内快速拒绝，不访问下游；冷却到期后由请求触发进入 `HALF_OPEN`。半开只放受限探测请求，失败立即回 OPEN，成功达到 `successThreshold` 后回 CLOSED。

当前不是失败率窗口，而是失败计数加时间衰减：每次失败递增计数；若距离上一次失败已超过 `failureResetTimeoutMs`，下一次失败会从零重新累计；成功也只有在该失败窗口过期后才清零。因此它不等价于“成功就清零”的严格连续失败计数，实际效果是避免“失败、成功、失败”交替出现时熔断长期打不开。项目配置可支持有限个半开探测，单探针最保守，多个探针恢复更快但会增加恢复期冲击。

**为什么当前不采用滑动窗口失败率：** 滑动窗口可以表达“最近 10 秒至少 50 个请求、失败率超过 50% 才熔断”，能避免小样本误判，也能识别大量非连续失败；还可叠加慢调用比例。但当前网关主要面对外部 LLM 的连续 429、5xx、网络错误和超时，失败计数加时间衰减已覆盖典型故障。滑动窗口还需要定义窗口长度、桶粒度、最小样本数、失败阈值和慢调用阈值，参数不当会造成误熔断或漏熔断；在当前单机原型、请求规模和测试覆盖下，额外复杂度没有足够收益。它是后续高流量场景的演进方向，不是当前缺失的基础正确性。

**当前策略的缺点：** 它没有分母，不能表达“最近 100 次里失败 40 次但并不连续”的慢性不稳定；同一个阈值在低流量和高流量下含义不同，低流量的少量失败可能显得过于敏感，高流量下分散失败又可能迟迟不触发。时间衰减只按相邻失败间隔重置，窗口内偶发成功不会消除已有失败计数，可能在恢复但仍有零星错误时偏保守。它也不统计慢调用比例，后端长期高延迟但尚未报错时不会主动隔离。阈值、reset timeout 与 recovery timeout 只能依赖经验和压测配置，无法像滑动窗口那样通过最小样本数与失败率更直观地表达 SLO。

**滑动窗口的多线程难点：** 它不是只多加一个失败率除法。共享状态会扩展为多个时间桶的总请求数/失败数、当前桶位置、窗口推进与过期清理、最小样本判断，以及原有的三态、半开名额和 generation。若“推进或清空桶”“写入本次结果”“汇总失败率并切换状态”分开加锁，多个线程可能同时轮转同一桶而重复清零，或一个线程基于过期快照打开熔断、另一个线程已更新统计，导致计数丢失或错误状态迁移。最稳妥的首版仍是用当前同一把 mutex 将这些操作和 HALF_OPEN 许可发放作为一个复合临界区；只有压测证明这把锁成为热点，才考虑将桶计数原子化、读取统计快照并用版本号/epoch 校验。无论如何，状态切换、半开探测和迟到结果隔离仍需要互斥或等价的原子状态机，不能因为统计使用 atomic 就认为熔断器天然线程安全。

当前熔断器的多线程安全已经落地：`allowRequest`、`reportSuccess`、`reportFailure` 和 `state` 都通过同一把互斥锁保护。这样状态判断、失败计数、OPEN/HALF_OPEN/CLOSED 转换以及半开探测名额的“检查并递增”会作为一个不可分割的操作执行，多个线程不会重复把熔断器切入半开，也不会突破 `halfOpenMaxCalls`。每次放行都会返回一个带 `generation` 的 permit；状态代际发生变化时递增 generation，旧代请求迟到的成功或失败上报会被忽略，避免旧的 CLOSED 请求误关闭新的 HALF_OPEN，或污染新一轮探测。半开阶段只要任一已放行探测失败就重新 OPEN；只有达到成功阈值且已放行的探测全部成功才回到 CLOSED。

**半开多轮探活的取舍：** 当前 `half_open_max_calls = success_threshold`，是一轮有限探针全部成功即恢复，状态简单但阈值不能设得过高或过低。可演进为“并发上限 2、累计成功阈值 K”的同一 generation 多轮探活：始终最多 2 个在途探针，成功后补发下一个，累计 K 次成功才关闭；任一有效探针失败或超时则重新 OPEN。严格等待一整批返回再放下一批验证更强，但一个慢探针会让大量请求在半开期被拒绝；滑动并发窗口恢复更快，代价是状态与超时管理更复杂。半开未获许可的请求可快速失败或走备用后端，不能无限排队。

相关并发测试覆盖了半开名额上限、半开成功/失败并发时失败优先、关闭态并发失败达到阈值，以及旧代请求迟到回报等场景。

连环追问：
- **closed 状态下做什么？**
  答：正常放行并根据结果更新失败计数。
- **open 状态下请求怎么处理？**
  答：冷却期内快速拒绝，不访问后端；到期后竞争半开许可。
- **half-open 状态允许多少探测请求？**
  答：由 `half_open_max_calls` 配置；单探针最保守，多探针恢复更快但冲击更大。
- **探测成功几次后恢复？**
  答：达到配置的 `success_threshold` 后恢复 CLOSED。
- **恢复阈值能否大于单轮探针数？**
  答：可以，需将一个 HALF_OPEN generation 设计为多轮累计探活，并配合单探针 deadline；当前实现为单轮策略，二者相等。
- **多轮探活遇到慢探针怎么办？**
  答：严格分批会等待该批完成，保守但恢复慢；更实用的是维持有限在途探针、成功即补发，同时让慢探针超时按失败处理。
- **探测失败后怎么办？**
  答：立即回 OPEN，并重新开始恢复冷却计时。
- **失败计数什么时候清零？**
  答：当前成功且距离最近失败超过 reset timeout 后清零，半开恢复时也重置。
- **熔断窗口是固定窗口还是滑动窗口？**
  答：当前是失败计数加时间衰减，不是完整滑动失败率窗口；它优先覆盖连续或短时间密集失败。
- **为什么不直接使用滑动窗口失败率？**
  答：滑动窗口能识别非连续失败，但需定义桶、窗口、最小样本和慢调用阈值，还增加并发轮转与统计复杂度。当前 LLM 后端典型故障多为连续 429、5xx 或超时，失败计数加时间衰减的收益与复杂度更匹配。
- **当前失败计数加时间衰减有什么缺点？**
  答：它不统计总请求数，难以识别非连续的高失败率；阈值在不同流量下意义不同，也不能识别长期慢调用。高流量生产场景可演进为带最小样本数、失败率和慢调用比例的滑动窗口。
- **改成滑动窗口后如何保证线程安全？**
  答：首版仍用同一把 mutex 包住桶轮转、结果写入、失败率汇总、状态切换和半开许可；不能拆成多个独立加锁步骤。高并发下可将桶计数原子化，但状态机与 generation 仍需保持复合操作的一致性。

### 7.2 熔断维度

**面试官：你的熔断器按什么维度维护？用户、模型还是后端？**

**回答：** 当前按 `backendId/modelId` 建独立熔断器，而不是按用户或全局。按全局太粗，一个模型故障会挡住所有模型；按用户太细，不能表达下游共享故障，也会产生大量状态。按后端可以让主模型故障时只隔离该节点，其他模型和 fallback 仍可工作。

如果同一 provider 的多个模型共享 API Key、限额和物理集群，仅按 model 又可能过细，生产上可叠加 provider、credential、model/instance 多级熔断。当前状态在进程内通过熔断器自己的并发状态保护；多实例间不共享。熔断状态通常不必持久化到磁盘，因为它是短时健康判断，但多实例若要一致治理可通过服务发现/控制面共享健康信号。

连环追问：
- **如果按全局熔断，会有什么问题？**
  答：一个模型故障会阻断所有健康模型，隔离粒度过粗。
- **如果按用户熔断，会不会过细？**
  答：会，状态量大且无法反映共享后端的整体故障。
- **如果按模型后端熔断，如何隔离单个故障模型？**
  答：每个 backendId 持有独立 CircuitBreaker，路由时只检查当前候选后端。
- **多个后端实例如何处理？**
  答：理想上按实例熔断并在模型/provider 层聚合；当前主要按配置 backend ID。
- **熔断器状态多线程下如何保护？**
  答：当前实现使用 `mutex_` 串行保护状态、计数、时间戳和半开名额；`allowRequest` 中对“检查状态、冷却判断、切换 HALF_OPEN、检查并递增探测数”一次完成，避免多个线程重复进入半开或超过探测上限。
- **为什么还需要 generation/permit？**
  答：`allowRequest` 为每次放行返回当前代际的 permit。熔断器从 CLOSED 打开、从 OPEN 进入 HALF_OPEN 或半开探测失败重新 OPEN 时都会递增 generation；旧代请求即使之后才返回，`reportSuccess/reportFailure` 发现 permit 已过期也会忽略，避免迟到结果污染新状态。
- **半开成功和失败同时返回时怎么办？**
  答：所有上报在同一把锁下串行化，任一有效半开探测失败都会优先把状态改回 OPEN；成功只有在达到阈值且所有已放行探测都成功时才能关闭熔断。
- **熔断状态是否需要持久化？**
  答：通常不落磁盘，它是短期健康状态；多实例一致性可由共享控制面处理。

校招建议：
- 可以回答按模型后端或 backend 维度更合理，避免一个后端异常拖垮整体。
- 如果项目里是简单实现，要诚实说明当前粒度和改进方向。

## 8. MySQL 连接池与 RAII

### 8.1 连接池基本设计

**面试官：你的 MySQL 连接池怎么设计？**

**回答：** 连接池预先维护有限的 MySQL 会话，避免每个请求重复 TCP 建连和认证。当前池内用 `deque<ConnEntry>` 保存空闲连接，条目包含连接、`lastReturned` 和 `lastChecked`；业务通过带 `PoolDeleter` 的 unique_ptr 独占借用，析构自动归还。没有空闲连接时用条件变量带超时等待，当前默认约 5 秒，超时抛异常，使资源耗尽变成可观测失败而非无限挂起。

最小/最大连接数应由 MySQL `max_connections`、服务实例数、业务线程并发和数据库承载能力共同决定，不能只按 CPU 核数。MySQL 重启后旧连接在借用 ping 或后台健康检查中被发现，失活连接绕过归还 deleter 销毁，并尝试创建替代连接。连接池 shutdown 的难点是借出对象仍引用 PoolDeleter 中的池指针；当前依赖服务生命周期保证池后析构，生产级应停止新借用、等待/超时回收借出连接，或让 deleter 指向独立共享状态。

连环追问：
- **为什么需要连接池？**
  答：复用 TCP/认证后的 MySQL 会话，限制总连接数并降低每请求建连开销。
- **最大连接数、最小连接数怎么设置？**
  答：结合数据库 max_connections、实例数、业务线程并发和压测等待时延设置。
  连接不能过大，连接本身消耗数据库资源，每条 MySQL 连接占内存、会话状态、线程/调度资源；连接池不能提升数据库处理能力，当数据库只能稳定处理 20 条活跃 SQL 时，放开 100 条并发只会让更多请求同时争 CPU、磁盘、Buffer Pool 和锁，P99 反而更差；锁竞争和死锁风险增加，写操作并发过高会加剧行锁等待、事务堆积和连接占用。
  连接不能过小，会形成业务请求的排队，空闲连接很快耗尽，请求阻塞在“获取连接”阶段，最终触发获取超时；数据库可能仍有余力，但是业务却被连接上限限制排队。
- **获取连接时没有空闲连接怎么办？**
  答：在条件变量上有界等待，有连接归还时被唤醒。
- **获取连接超时怎么处理？**
  答：抛出资源获取异常，由接口返回服务繁忙并记录池等待指标。
- **连接归还在哪里发生？**
  答：借用句柄析构时由 PoolDeleter 自动放回 deque 尾部。
- **连接池析构时还有连接借出怎么办？**
  答：应先停止新借用并等待/超时回收；当前依赖整体服务销毁顺序。
- **MySQL 重启后旧连接如何失效和重连？**
  答：借用检查或后台 ping 识别失败，销毁旧连接并创建替代连接。
  **当发生连接排队时如何排查是因为连接池过小还是慢 SQL**
  答：首先需要针对连接池借出与归还进行埋点，记录借出与归还时间，连接池借出数和剩余数，业务线程排队数量，连接获取超时数量等数据；
  当连接池配置过小，借连接等待时间高，但连接归还速率正常，持有连接时间偏短且稳定，MySQL 的 CPU、IO 仍有余量，小幅增加连接池大小 QPS 上升且 P99 没有恶化；
  当发生慢 SQL/锁等待信号，连接持有时间较长，慢查询日志、锁等待或事务列表异常，大量线程处于等待锁、IO 的情况，增加连接数量只是暂时缓解，甚至可能加剧排队；
  连接泄露的信号，连接长期借出不归还，空闲连接长期归0，即使业务下降，问题也并没有解决；
  需要根据借出数、归还数、等待时长、持有时长、业务排队数量、连接池剩余数量、锁等待进行判断并进行问题定位处理。


### 8.2 LIFO 热连接复用

**面试官：为什么连接池采用 LIFO，而不是 FIFO？**

**回答：** 当前约定 deque 头部是冷连接、尾部是热连接，业务 `pop_back` 借出并 `push_back` 归还。LIFO 优先复用刚用过的 MySQL 会话，降低空闲超时导致的失活和重连概率；连接对象及相关内存近期被访问，也可能有缓存局部性，但 CPU cache 不是主要论据。每次借用仍需必要的活性判断，LIFO 只是降低拿到失活连接的概率，不会消除检查成本。

FIFO 会让所有连接轮流被使用，连接从归还到下次借出间隔更长，更容易整体变冷。LIFO 的反面是底部连接可能长期闲置、失活并占容量，因此健康线程扫描冷端，未来也可优先缩容冷端。当前用 `lastReturned` 表示业务热度、`lastChecked` 表示探活节奏；探活不能更新 lastReturned，也不能把检查后的冷连接放到尾部，否则后台线程会破坏冷热分层。

连环追问：
- **什么是热连接？**
  答：最近被业务使用并归还、空闲时间短的连接。
- **LIFO 对 CPU cache 或连接活跃性有什么好处？**
  答：主要降低空闲失活概率，近期对象也可能仍在缓存；后者只是辅助收益。
- **LIFO 会不会导致某些连接长期不用？**
  答：会，冷连接沉在 deque 头部，因此需要冷端探活和可选缩容。
- **长期不用连接如何保活？**
  答：健康线程按 lastReturned/lastChecked 从冷端摘出并锁外 ping。
- **如果服务端关闭空闲连接，连接池如何发现？**
  答：后台 ping 或业务借用时的活性检查返回失败。

### 8.3 `unique_ptr + custom deleter`

**面试官：为什么用 `unique_ptr + 自定义 deleter` 管理连接？**

**回答：** 数据库连接带事务、临时变量、statement 和 result set 状态，同一时刻只应由一个业务调用方独占。池借出时把 unique_ptr move 给调用者，不能复制；调用结束后 deleter 将裸指针重新包装并放回池尾。RAII 把“借出必须归还”写进类型，而不是依赖每条异常路径手工 returnConnection。

`shared_ptr` 的问题不只是控制块和原子引用计数：它可以被随意复制到 lambda 或异步任务，实际归还时间失去控制；甚至让两个逻辑请求并发使用同一数据库会话，造成事务/结果集状态污染和连接池耗尽。自定义 deleter 通常只增加一个池指针和一次普通函数调用；真正风险是池先析构后 deleter 再访问它，所以要保证关闭顺序。deleter/析构函数必须 noexcept，归还失败只能记录、销毁连接或进入安全回收路径，不能抛异常。

连环追问：
- **`unique_ptr` 的所有权语义是什么？**
  答：同一时刻资源只有一个unique_ptr所有者，禁止复制、允许移动。
- **自定义 deleter 里做什么？**
  答：将健康连接重新包装入池尾；池关闭或连接失效时直接销毁。
- **deleter 捕获连接池指针是否安全？**
  答：只有连接池生命周期覆盖全部借出句柄时安全，否则会悬空。
- **如果连接池先析构，连接后归还，会不会崩？**
  答：会有访问已析构池的风险，必须通过 shutdown 顺序或共享 pool state 解决。
- **为什么不用 `shared_ptr`？**
  答：一是连接是独占会话资源，shared_ptr 会模糊归还时机并允许意外并发共享；二是shared_ptr存在额外控制块开销。
- **自定义 deleter 是否有运行时开销？**
  答：有一次普通 deleter 调用并可能多存一个池指针，相比数据库 I/O 可忽略。

八股延伸：
- **RAII 是什么？**
  答：资源获取即初始化，把释放绑定到对象析构，覆盖正常与异常路径。
- **智能指针区别。**
  答：unique_ptr 独占；shared_ptr 引用计数共享；weak_ptr 不拥有资源并用于观察 shared_ptr 生命周期。
- **移动语义。**
  答：转移资源所有权而不复制底层连接，源对象转为空状态。
- **析构函数为什么不建议抛异常？**
  答：栈展开中再次抛异常会触发 terminate，资源清理路径必须 noexcept。

### 8.4 PreparedStatement 资源泄露的定位与修复

**面试官：你是如何定位并修复预编译语句资源泄露的？**

**回答：** 问题是在修复 Session 线程安全后做 login 压测时暴露的：并非 QPS 自然下降，而是成功请求数异常地稳定接近 `16382`，随后日志报 `Can't create more than max_prepared_stmt_count statements`。`16382` 正好是 MySQL 常见的 `max_prepared_stmt_count` 默认上限，因此我没有把它归因为连接池不足或压测脚本问题，而是沿登录查询链路检查每次 `prepareStatement` 之后的释放路径。

根因有两层。第一，原实现每次查询都创建 `PreparedStatement`，但局部 `unique_ptr` 使用默认 deleter，只做 C++ 对象 `delete`，没有显式调用 Connector 的 `stmt->close()`；在本项目使用的 Connector 行为下，`close()` 才会释放 MySQL 连接上下文中的服务端 prepared statement 句柄。连接池使 Connection 长期复用，因而服务端句柄在同一批连接上持续累积，最终触顶。第二，查询接口返回裸 `sql::ResultSet*`，但它依赖创建它的 Statement；若局部 Statement 在函数返回时析构，调用方继续遍历 ResultSet 就是悬垂访问。这是资源泄露和 use-after-free 两个独立但同源的生命周期问题。

修复时我没有只在某个 handler 补 `close()`，而是把依赖关系编码进类型。`StmtPtr` 是带 `StmtDeleter` 的 `unique_ptr<PreparedStatement>`，deleter 固定执行 `close()` 再 `delete`。`executeQuery()` 把 Statement 所有权 move 给不可拷贝、可移动的 `DbQueryResult`，它同时持有 `StmtPtr` 与 `ResultSet*`；析构时先 delete ResultSet，再由成员 `StmtPtr` 的 deleter 释放 Statement 和服务端句柄。外层 `QueryResult` 又按成员析构逆序，确保查询结果先销毁，最后才将池化 Connection 归还。`executeUpdate()` 也统一使用 `StmtPtr`，使正常返回、异常抛出和早退都走同一释放路径。

验证不应只看“服务不报错”。应在重复执行超过原故障阈值的 login/查询压测时确认不再出现该报错，并观察 MySQL 的 `Prepared_stmt_count` 不再随请求数单调增长、连接池借还与错误率正常；必要时用 ASan/Valgrind 或 Connector 诊断确认 ResultSet 没有悬垂访问。要如实说明：该修复解决的是当前每次创建再释放的语句生命周期，并没有实现跨请求 Statement cache；是否做缓存是独立的性能优化，也需要处理按 Connection 分片、失效和容量淘汰。

连环追问：
- **为什么 `delete` 还不够，必须显式 `close()`？**
  答：`delete` 回收的是客户端 C++ 对象；本项目使用的 Connector 需要 `close()` 向 MySQL 释放服务端 prepared statement 句柄。二者是不同地址空间、不同生命周期的资源。
- **为什么把 Statement 与 ResultSet 绑定，而不是只返回 ResultSet？**
  答：ResultSet 不能独立于创建它的 Statement 存活。让结果对象拥有 Statement，调用方无法在依赖已释放后继续使用裸结果集。
- **为什么不把 Statement 放进 ResultSet？**
  答：不能修改第三方 `ResultSet` 类型；组合一个 `DbQueryResult` 包装器能保留 ResultSet 的访问接口，同时显式表达所有权和析构顺序。
- **如果 `executeQuery()` 在绑定参数或执行时抛异常怎么办？**
  答：局部 `StmtPtr` 自动运行 deleter，仍会 close 再 delete；没有成功构造结果对象也不会泄露。
- **这个 bug 和连接池有什么关系？**
  答：连接池不是根因，但它让 Connection 活得很久，使服务端句柄不能随短连接断开而被动回收，从而把泄露放大成稳定可复现的容量故障。

### 8.5 锁外 ping 与时间戳保护期

**面试官：健康检查为什么采用锁外 ping？**

**回答：** `ping()` 是可能阻塞的数据库网络 I/O，若持池锁执行，所有业务线程都无法借还连接，数据库抖动会放大成连接池全局延迟尖峰。当前流程是在锁内从 deque 冷端保序摘出候选，使其暂时不在可借集合中；解锁后 ping；再加锁按结果销毁/补充或插回冷区，只更新 `lastChecked`。

摘出动作同时解决了“多个线程是否会检查同一连接”：连接不在池中，其他业务和检查线程都拿不到。`lastReturned` 保护冷热语义，`lastChecked` 控制同一冷连接最小探活间隔。旧的 `vector + swap/pop_back` 会把尾部热连接换到冷端并打乱顺序；改用 deque 后不再用这种交换技巧。ping 失败时必须绕过 PoolDeleter 的自动归还逻辑，否则坏连接会重新入池。

连环追问：
- **如果在锁内 ping，会造成什么问题？**
  答：慢网络 I/O 持有池全局锁，阻塞所有业务借还并制造周期性 P99 尖峰。
- **ping 是网络 I/O 吗？延迟是否可控？**
  答：是与 MySQL 的往返，延迟受网络和数据库状态影响，不可假设为常数。
- **锁外 ping 期间连接状态可能变化吗？**
  答：数据库侧可能断开，但本地连接已从池摘出，不会被其他线程并发使用；结果回来后再判定。
- **时间戳保护期保护什么？**
  答：lastReturned 保护业务冷热，lastChecked 防同一冷连接被过度频繁探活。
- **如何避免多个线程同时 ping 同一个连接？**
  答：锁内先从可借 deque 摘出候选，其他线程无法再取得它。
- **ping 失败后连接如何移除或重建？**
  答：绕过自动归还 deleter 销毁坏连接，再按池策略创建 replacement。

这块是项目亮点，也最容易被深挖。建议准备一个具体流程：
- 加锁取出候选连接。
- 释放锁后做 ping。
- 根据 ping 结果和时间戳判断是否可用。
- 可用则返回给业务，不可用则销毁或重建。

## 9. RabbitMQ 异步落库

### 9.1 为什么引入 RabbitMQ

**面试官：为什么对话记录要异步落库？直接写 MySQL 不行吗？**

**回答：** 同步写库可以实现，但它会把连接池等待、SQL 往返、慢库和重试放到用户流式完成路径，并继续占用业务线程。即使正常只需几十毫秒，MQ 的主要价值也不是省这几十毫秒，而是把数据库波动与模型主链路隔离、吸收瞬时峰值，并让消费者独立扩容、限速和重试。聊天消息是追加型事件，本次回答不依赖同步读回刚写的数据，天然适合最终一致。

只用同一个业务线程池异步写库仍与模型任务争线程和队列，进程崩溃时内存任务也会丢；MQ 提供独立缓冲与消费语义。当前 RabbitMQ 故障时的可靠降级不完整。如果消息不可丢，应使用 publisher confirm、持久化队列/消息以及 outbox/WAL；如果允许“聊天成功、记录可能延迟或丢失”，必须把这个产品边界讲清并告警，不能无限同步重试拖住主链路。

连环追问：
- **同步写库会增加什么延迟？**
  答：增加连接池等待、SQL 网络往返和提交时延，异常时还可能叠加重试。
- **异步落库对主链路有什么收益？**
  答：把数据库抖动从 SSE 收尾路径隔离，并削峰和独立扩展消费者。
- **消息队列宕机时，请求还能成功吗？**
  答：设计目标是聊天可继续，但当前会有记录丢失风险，取决于可靠性策略。
- **对话记录丢失是否可以接受？**
  答：是产品/SLA 决策；若不可接受就必须增加 outbox/WAL，而不是口头保证。
- **如果不能丢，怎么保证可靠性？**
  答：从消费者生产者模型开始分析；1、生产者部分：启用本地事务(几乎不使用，性能很差)/使用pubilsher confirm发送确认机制，成功后异步返回ACK；2、消息队列部分：进行队列/消息持久化，但是未落盘成功还是会丢失，一般配合pubilsher confirm使用，落盘成功后返回ACK；3、消费者部分：取消自动ACK并使用手动ACK，消费成功返回ACK；4、异常处理：有限次重试队列/手动消费死信队列，幂等性设计。
- **为什么不用业务线程池直接写库？**
  答：会与 LLM 任务争线程，进程崩溃时内存任务丢失，也缺少 broker 缓冲和重投。

### 9.2 RabbitMQ 基础

**面试官：RabbitMQ 里 exchange、queue、routing key、binding 分别是什么？**

**回答：** producer 把消息发给 exchange；exchange 根据 binding 规则和 routing key 路由到 queue；consumer 从 queue 获取消息。direct 做 routing key 精确匹配，topic 支持点分主题和通配符，fanout 忽略 key 向所有绑定队列广播。当前聊天落库只有简单队列用途，复杂交换机不是主线。

publisher confirm 保护“生产者到 broker 是否确认接收”；durable queue 与 persistent message 保护 broker 重启；manual ack 保护“消费者处理成功后才从队列确认”。三者保护不同链路，消息进入 MQ 不等于已落库。积压排查看队列深度、publish/ack 速率、unacked 数、消费时延和失败率；永久失败或超过重试次数的消息进入 DLQ，避免毒消息阻塞主队列。

连环追问：
- **direct、topic、fanout 区别是什么？**
  答：direct 精确 key，topic 通配主题，fanout 向所有绑定队列广播。
- **生产者确认是什么？**
  答：broker 异步确认消息已接收，保护 producer 到 broker 这一段。
- **消费者 ack 是什么？**
  答：消费者处理成功后确认，broker 才删除/标记完成消息。
- **消息持久化怎么做？**
  答：durable exchange/queue 加 persistent message，并理解落盘确认策略。
- **重复消费怎么处理？**
  答：messageId 唯一索引或幂等表，重复 insert 视为已完成。
- **消息积压怎么排查？**
  答：看 ready/unacked、生产与 ack 速率、最老消息年龄、消费者错误和 DB 时延。
- **死信队列有什么用？**
  答：隔离超过重试次数、过期或被拒绝的毒消息，避免阻塞主队列。

### 9.3 落库幂等与失败处理

**面试官：如果消息重复消费，如何保证数据库幂等？**

**回答：** RabbitMQ 可靠消费通常按“至少一次”设计，消费者可能在数据库提交后、ack 前崩溃，消息会重投。
因此幂等性需要区分出防止重复执行和防止重复落库；防止重复执行：业务幂等键 + 状态机更新，服务端首次处理记录状态和结果，完成后更新状态，重复请求直接检查幂等键和状态；防止重复落库：每条聊天事件需要全局唯一 `message_id`，数据库对它建唯一索引；

当前代码发布的是 SQL 字符串，消费者在 handler 返回后显式 `BasicAck`，但没有稳定 message_id、唯一键、显式事务边界、publisher confirm、持久化投递、重试和 DLQ，仍属于可靠性缺口。生产设计应发布结构化事件，包含 session、role、sequence、timestamp 和状态；在显式事务提交后 ack，失败指数退避，超过次数进 DLQ。顺序要求可按 session 分区/单消费者或使用 sequence 在读取时排序；会话内存与数据库是最终一致，MQ 不应反向持有 `AIHelper` 或 `TcpConnection`。

连环追问：
- **每条对话消息是否有唯一 ID？**
  答：当前 SQL 中有消息 id，但生产级应把稳定 messageId 放入结构化 MQ 事件并建唯一约束。
- **数据库唯一索引如何设计？**
  答：以 message_id 唯一；会话读取另建 `(session_id, sequence)` 索引。
- **插入失败后是否重试？**
  答：瞬时连接/死锁错误可有限重试，永久约束/格式错误进入 DLQ。
- **重试会不会造成乱序？**
  答：会，因此消息带 sequence，按 session 分区串行或读取时排序。
- **消费失败后消息如何处理？**
  答：不 ack，按退避重新投递；超过次数进入 DLQ 并告警。
- **异步落库和会话上下文一致性如何保证？**
  答：内存上下文提供即时一致，数据库最终一致；事件不可变并带 session/sequence，恢复时按序重建。

## 10. AIStrategy 与多模型接入

### 10.1 策略抽象

**面试官：AIStrategy 抽象了什么？为什么需要它？**

**回答：** 当前策略接口抽象 `buildRequest`、`parseResponse`、`parseStreamChunk`、是否流式和额外请求头，把厂商的请求 JSON、普通响应和流式 delta 解析从 handler 与网关中移走。`GenericAIStrategy` 处理 OpenAI-compatible 模型，`AliyunRAGStrategy` 处理阿里云 RAG 的特殊字段与响应。工厂按配置 type 创建策略，网关仍专注限流、熔断、超时和 HTTP 执行。

它还不够协议中立。当前 messages 是 `vector<pair<string,long long>>`，角色在一定程度上依赖顺序，结果被压成 string，说明 OpenAI chat 格式已渗入领域层。新增兼容模型可只改配置；新增格式差异大的供应商必须新增策略，甚至应重构为中立 `AIRequest/AIResponse/AIStreamEvent/ChatMessage{role,content}`，由 provider adapter 双向转换。配置化解决数据差异，抽象类解决行为差异，两者不能互相替代。

连环追问：
- **请求构造如何统一？**
  答：业务形成统一消息，具体 strategy 转成厂商 JSON。
- **响应解析如何统一？**
  答：strategy 把普通响应/流事件归一为文本或内部事件，再交给业务层。
- **流式参数如何统一？**
  答：策略暴露 streaming 能力并负责厂商 stream 字段，网关只执行流式 HTTP。
- **OpenAI 兼容模型和阿里云 RAG 差异在哪里？**
  答：请求字段、应用 ID/参数、stream 开关和响应层级不同，因此 RAG 使用独立策略。
  OpenAI兼容模型：请求包含： messages + stream=true；响应解析 choices[0].delta.content；
  阿里云 RAG：请求包含：input.prompt + parameters.incremental_output=true，额外请求头X-DashScope-SSE: enable；响应解析 output.text；
- **新增模型只改配置，是否真的不改代码？**
  答：OpenAI-compatible 模型通常只改配置；新协议类型仍要增加 adapter/strategy。
- **如果新模型返回格式完全不同怎么办？**
  答：新增 provider adapter；若现有接口承载不了，应先重构中立请求/响应事件而不是继续堆 if。

八股延伸：
- **策略模式是什么？**
  答：把可替换算法封装为统一接口，运行时选择具体实现。
- **工厂模式是否适合创建不同 strategy？**
  答：适合集中按 type/config 构造；进一步可用注册表避免工厂 switch 扩张。
- **配置化和抽象类边界在哪里？**
  答：URL、模型名、阈值是数据差异放配置；请求构造和解析是行为差异放策略。

### 10.2 配置化

**面试官：配置文件里有哪些字段？**

**回答：** 模型配置包括默认模型、alias、模型 ID/type、URL、实际 model name、API Key、stream、上下文窗口和额外 header；网关配置还包括后端列表、连接/请求超时、用户与后端限流、熔断阈值/恢复时间和 fallback 路由。启动加载时应校验必填字段、重复 ID、未知 alias 和 fallback 引用，尽早失败。

API Key 不应提交仓库或打印日志，应从环境变量、挂载 secret 或密钥服务读取并脱敏。当前以启动时加载为主，没有完整热更新。若增加热更新，适合原子替换不可变配置快照，让进行中请求继续持有旧快照；Key/URL/阈值可更新，但线程数、连接池结构等通常需要谨慎重建。配置刷新不应无意清空已有熔断/限流状态，应按稳定 backend ID 迁移。

连环追问：
- **base URL、model name、API key、timeout、stream 参数如何配置？**
  答：模型文件配置 URL/name/secret 引用/stream，网关文件配置连接与总 timeout；启动校验后生成快照。
- **API key 如何保护？**
  答：从环境变量或 secret 服务读取，不提交仓库、不输出日志。
- **配置错误如何启动时发现？**
  答：做 schema、必填字段、重复 ID、alias 和 fallback 引用校验，失败即停止启动。
- **是否支持热更新？**
  答：当前主要启动加载，没有完整热更新。
- **如果配置热更新，已有请求怎么处理？**
  答：请求持有启动时取得的不可变旧快照，新请求使用原子替换后的新快照。
- **配置变更是否影响限流和熔断状态？**
  答：阈值应更新但状态按稳定 backend ID 保留；删除后端时再安全回收。

## 11. RAG、工具调用与上下文管理

### 11.1 RAG

**面试官：RAG 在项目里怎么接入？**

**回答：** 当前 RAG 是前端/业务显式选择 `aliyun-rag` 模型类型，工厂创建 `AliyunRAGStrategy`，由它构造阿里云应用接口的请求并解析特殊流式响应。知识库检索在远端 RAG 服务内部完成，本项目并没有自己取回文档再拼 prompt；HTTP 接入、业务线程池、网关治理、SSE 与落库仍和普通模型共享。

所以 RAG 和普通模型是“协议策略处分流、基础链路共享”，不是完全独立系统。当前也没有自动判断是否需要 RAG。更合理的演进是在 `AIOrchestrator` 中根据意图、知识域或模型决策选择 Direct/RAG/Tool/RAG+Tool。检索失败是否回普通模型取决于事实可靠性要求；若允许 fallback，应明确告诉模型/用户未命中知识库。检索和生成共同受统一 deadline，而不是各自无限等待。

连环追问：
- **RAG 请求和普通问答请求有什么区别？**
  答：RAG 使用阿里应用接口和特殊字段，普通模型使用 OpenAI-compatible messages/model。
- **检索结果如何拼接进 prompt？**
  答：当前由远端阿里 RAG 服务内部检索拼接，本项目不直接拿文档拼 prompt。
- **检索失败是否 fallback 到普通问答？**
  答：当前没有通用自动 fallback；是否回退取决于事实可靠性产品要求。
- **RAG 响应如何统一成 OpenAI 风格 SSE？**
  答：AliyunRAGStrategy 解析厂商 chunk，handler 再编码统一 data 事件。
- **检索耗时如何纳入超时控制？**
  答：远端 RAG 调用整体受 curl deadline；更细可拆检索与生成阶段指标。

### 11.2 工具调用

**面试官：工具调用结果如何处理？**

**回答：** 当前不是原生 function calling，而是 prompt-based tool routing。启用工具后，第一轮非流式 LLM 被提示输出固定 JSON，AIHelper 解析 `need_tool/tool_name/args`；命中注册工具后由 `AIToolRegistry` 执行，再把工具结果和原问题构造成临时 prompt，第二轮 LLM 流式生成最终答案。前端只看到最终回答，数据库也只保存原始用户问题和最终助手答案。

第一轮 JSON 可能格式错误，代码只有一次修复/降级逻辑；工具不存在、参数错误、超时或异常要转换为受控结果，不能执行任意函数，进行一次的LLM纠正逻辑，识别到格式错误、字段错误、参数错误等将错误结果与问题描述再次输入约束LLM进行纠正。

当前实现仅完成了上述流程，工具调用的安全边界包括白名单、JSON Schema、参数长度、网络目标限制、权限、超时、审计和副作用确认；多个独立只读工具可并行，有依赖/副作用则串行还未落地。

改进方向：使用原生 function calling，流程：用户问题 + tool_schema -> 模型流式增量返回 tool_calls 结构化对象 -> 服务端执行增量解析工具参数并执行工具 -> 以 tool_role 回传调用结果 -> 二次调用LLM生成最终回答；依旧是两阶段LLM调用或循环的模式，由API协议约定的tool_schema/tool_calls结构，虽然还需要进行校验，但是相对 prompt-base 更稳定，同时支持多个tool_calls 结构化对象，支持多工具、多参数的多次循环执行。

连环追问：
- **模型如何表示需要调用工具？**
  答：当前第一轮返回提示词约束 JSON，包含是否调用、工具名和参数。
- **工具调用失败怎么办？**
  答：捕获不存在、参数、超时和异常，生成受控失败结果或退回普通回答。
- **工具调用结果是否再次喂给模型？**
  答：是，作为临时最终回答 prompt 进入第二轮模型调用。
- **工具调用和普通问答如何统一响应？**
  答：普通问答单次流式；工具路径只把第二轮最终答案走相同 SSE。
- **工具调用是否会带来安全问题？**
  答：会，需要白名单、schema、权限、超时、网络目标限制、审计与副作用确认。

### 11.3 多会话上下文

**面试官：多会话上下文怎么管理？**

**回答：** ChatServer 以 `userId -> sessionId -> shared_ptr<AIHelper>` 隔离会话；`AIHelper` 是会话窗口级对象，`chat()` 才是单次提问。当前 messages 主要在内存中，消息同时异步落 MySQL。session ID 由创建会话接口生成并由前端后续请求传回，服务端还应以登录身份校验它确实属于该用户，不能只信任 body。

AIHelper 用锁保护 messages 与 strategy，锁覆盖模型调用全过程，因此同一 session 不会同时修改上下文或让两段回答交错；代价是前一个 LLM 慢调用会阻塞同会话下一问。**但这只是互斥串行，不是服务端 FIFO 顺序保证：** Chat handler 先把每个请求投进全局业务线程池，再由不同 worker 竞争 AIHelper 的 mutex；两个脚本/多标签页请求即使按 A、B 到达，B 也可能先获得锁并先进入上下文，等待 mutex 的任务还会占住业务 worker。浏览器在流式期间禁用输入只是体验约束，不是服务端防御。当前模型配置的 `max_context=20` 表示轮次窗口：超过阈值后，`buildRequestWithContext()` 临时构造“首轮问答 + 最近 20 轮问答 + 中间已裁剪提示”发送给模型；原始 `messages` 向量并不删除历史。于是模型输入在超限后长期接近固定满窗口，每一轮都要扫描、复制并发送约 21 轮问答；同时单会话历史和 AIHelper 本身会持续占用服务端内存。

这里要把两个问题拆开：**工作上下文**服务于本次推理，目标是控制 token、保留连续语义；**聊天记录**服务于页面展示、审计和恢复，目标是完整持久化且不全量常驻内存。不能因为模型不再带早期消息，就把用户可见的历史直接删除。

工作上下文的下一步是检查点式滚动摘要：保留 system/首轮中的稳定约束、一个 `summary` 和最近 K 轮原文。累计 token 即将超过预算时，对较早的一段已确认问答生成结构化摘要，摘要应保留用户偏好、已确定事实、未完成任务、工具/RAG 关键结论和时间线；将其与旧摘要合并，替换已压缩的原文，只保留最近 K 轮继续对话。原始全文仍在 MySQL，内存工作窗口只保存“摘要 + 最近 K 轮”，而不是清空唯一历史。摘要本身也要有版本、覆盖消息区间和 token 上限，避免多次摘要造成事实漂移；重要事实可做结构化长期记忆，必要时按当前问题检索相关历史，而不是盲目把所有摘要塞回 prompt。摘要生成会额外消耗一次模型调用，应在异步任务中按阈值或会话空闲时触发，并设失败时回退到最近窗口。

窗口单位也应从轮次演进为 tokenizer token 预算：不同语言、代码块、工具结果和模型 tokenizer 的长度差异很大，同样 20 轮并不代表同样成本。请求预算还要预留 system prompt、工具/RAG 内容和模型输出 token；当不能精确使用供应商 tokenizer 时，可先采用保守估算并记录实际 usage 校准。轮次窗口作为校招项目的简化方案可以接受，但不能宣称它是严格的上下文长度控制。

聊天记录层面，创建或打开会话时只从 MySQL 分页加载最近一页消息，向上滚动再按 `sessionId + sequence` 或时间游标加载更早记录，不能把一段会话全文重建为常驻 vector。热会话可缓存最近窗口或页面，设置单用户和全局内存上限、TTL 和 LRU；缓存命中直接返回，未命中再查库。即使用户请求查看全部历史，也应分页或流式传输，不能因此永久突破单会话内存上限。异步落库需有稳定 sequence 和幂等保证，才能让懒加载、摘要覆盖区间与内存窗口在重启后正确恢复。

连环追问：
- **session id 如何生成和传递？**
  答：创建会话时服务端生成，前端后续请求携带；服务端结合登录 userId 校验归属。
- **上下文保存在内存、MySQL 还是 Redis？**
  答：当前 AIHelper 内存保存即时上下文，消息异步落 MySQL，未使用 Redis。
- **上下文长度过长怎么裁剪？**
  答：当前临时保留首轮和最近 N 轮，原始 vector 不裁剪。后续以 token 预算为准，将较早历史压缩成带覆盖区间的摘要，内存只保留摘要和最近 K 轮；全文聊天记录持久化后按需加载。
- **摘要后是否会丢失早期关键信息？**
  答：有风险，所以摘要应保留事实、偏好、未完成任务和关键结论，并记录它覆盖的 sequence 范围；原始消息不能删除。对精确历史问题再按关键词或向量检索回填原文，摘要失败则回退最近窗口。
- **用户翻看很早的聊天记录时是否把整个会话读进内存？**
  答：不应这样做。接口按 sequence/time cursor 分页返回，热数据缓存最近页并受用户级和全局 LRU/TTL 限制；缓存未命中才查 MySQL。
- **多用户之间如何隔离？**
  答：用 userId 到 sessionId 到 AIHelper 的两级映射，并校验会话所有权。
- **会话过期怎么清理？**
  答：当前缺少自动 TTL/LRU；生产上定期淘汰并从持久化层按需恢复。
- **并发请求同一个 session 会不会乱序？**
  答：当前 AIHelper 锁只保证不并发修改上下文，不保证按客户端到达顺序执行；脚本或多标签页可让后到任务先抢到锁。浏览器禁用输入不是服务端保证。
- **如何在服务端保证单会话多轮请求顺序？**
  答：接入时为 `(userId, sessionId)` 分配单调 sequence，进入有界 FIFO mailbox；同一会话只允许一个 worker FIFO 队列，完成或取消当前请求后再执行下一条。队列满、排队超时或请求取消时明确拒绝/移除；消息落库携带相同 sequence，恢复时按 sequence 排序。若产品不允许排队，也可在会话已有活动流时直接返回“上一轮未完成”。

## 12. 压测、性能与排查

### 12.1 wrk 压测

**面试官：你的 12W QPS 是怎么压测的？**

**回答：** README 中的典型命令是 `wrk -t4 -c200 -d30s`：4 是 wrk 客户端线程，不是服务端 I/O 线程；200 是并发连接；30 秒是持续时间。约 12.25W QPS 对应静态 `/entry`，约 11.24W QPS 对应“静态 `/menu` + Session 鉴权”，两者都不是正则路由，也没有调用 LLM。前者可作为当前机器和同机 loopback 环境下的 HTTP 静态短路径基线；后者只能说明加入 Session、不同 handler 与不同响应体后的整条 `/menu` 链路结果，不能单独量化 Session 开销，更不能量化正则匹配开销。

同机部署存在观察者效应：wrk 与服务端争 CPU/缓存，同时绕过真实网卡、代理和 TLS。若当时没有完整记录 CPU、内存、机器配置、P99 和错误率，就应明确说这是阶段性相对数据，而不是完整 benchmark。面试材料更适合同时展示 entry 基线和 login 真实业务链路：login 不是取 HTML，而是 POST/JSON、Session、连接池、PreparedStatement 查询和密码校验，QPS 低很多是合理的。早期 session/menu 数据若响应不完整或资源泄漏后大量失败，应删除或只作为发现 bug 的证据。

连环追问：
- **wrk 命令参数是什么？**
  答：典型命令是 `wrk -t4 -c200 -d30s URL`，必要时用 `-s script.lua` 构造 login POST。
- **机器配置是什么？**
  答：VM 8C8G，进行同机部署压测。
- **测试持续多久？**
  答：典型记录为 30 秒；严谨测试还应预热并多轮重复。
- **200 连接是否 keep-alive？**
  答：wrk 默认复用 HTTP 连接，除非服务端响应显式关闭；应结合响应头和连接统计确认。
- **接口是否访问 MySQL、RabbitMQ、LLM？**
  答：`/entry` 不访问这些外部系统；`/menu` 会做 Session 鉴权但不访问 LLM；`/login` 会访问 MySQL，但不调用 RabbitMQ 或 LLM。
- **平均延迟和 P99 延迟是多少？**
  答：现有记录未提供足够可信的完整分位数，不能编造；重测时同时记录平均、P50/P95/P99 和错误率。
- **CPU、内存、网卡是否到瓶颈？**
  答：未同步采集就不能下结论；需结合 mpstat/pidstat、RSS、网卡吞吐和软中断判断。

必须说清：
- 12.25W 是静态 `/entry` 短路径；11.24W 是静态 `/menu` 加 Session 鉴权，不是正则路由数据。
- 当前没有可复现的动态正则路由压测结果，不能声称“正则路由约 11.2W”。
- 真实模型调用吞吐由下游 LLM 延迟和并发限制决定，不能用这个 QPS 表示。

### 12.2 性能瓶颈定位

**面试官：如果压测 P99 很高，你怎么排查？**

**回答：** 先按阶段拆分而不是直接猜：I/O 线程 CPU 与事件循环延迟、业务线程池活跃数/队列长度/排队时间、Session 锁等待、MySQL 连接池等待和 SQL 时延、curl DNS/建连/首包/总耗时、MQ 队列深度，以及 SSE outputBuffer 高水位。CPU 满且火焰图集中在解析/路由是计算瓶颈；线程大量停在 mutex/condition variable 是锁或资源池瓶颈；网卡满/重传高是网络问题；本地空闲而 curl 首包长是下游慢。

`perf record/report` 与火焰图看采样最宽栈；gdb `thread apply all bt` 看线程停在 epoll、锁、curl 还是数据库；ASAN 查越界、UAF、double free 和泄漏，TSAN 更适合数据竞争。只有 P99 高而平均正常说明少量长尾，常见原因是连接重建、健康检查持锁、偶发慢 SQL、下游抖动或慢客户端，而不是平均路径全面过载。

连环追问：
- **先看哪些指标？**
  答：先看吞吐、错误率、延迟分位，再看 EventLoop、业务队列、连接池、下游时延和输出缓冲。
- **如何区分 CPU 瓶颈、锁竞争、网络瓶颈、下游慢？**
  答：CPU 满看火焰图；锁竞争看 futex/线程栈；网络看带宽重传；本地空闲而外调首包慢说明下游。
- **perf 火焰图怎么看？**
  答：横向宽度代表采样占比，沿调用栈向上定位最宽热点及其调用来源。
- **gdb 如何看线程堆栈？**
  答：attach 后用 `info threads` 和 `thread apply all bt` 判断线程停在 epoll、锁、curl 还是数据库。
- **ASAN 能帮助排查什么？**
  答：堆栈越界、Use-after-free、double free 和部分泄漏；数据竞争应使用 TSAN。
- **如果只有 P99 高，平均延迟正常，说明什么？**
  答：少量长尾路径异常，常见是锁等待、连接重建、慢 SQL、下游抖动或慢客户端。

## 13. 故障场景

### 13.1 下游模型故障

**面试官：模型服务超时或 500，系统怎么处理？**

**回答：** 网关将超时、网络错误和 5xx 分类为后端失败并报告熔断器。非流式且未向用户输出时，可以按配置尝试 fallback；是否先重试取决于错误是否瞬时、请求是否安全和剩余 deadline。重试必须有限次数、指数退避并带抖动，否则后端故障时会形成重试风暴。

客户端收到的是稳定的网关 502/504 或 SSE error，而不是不加处理的供应商 body。流式已经发出部分 token 时，不能切换模型拼接另一答案，也无法更改 HTTP status，只能发错误事件并结束。超时/5xx 计熔断；400、401/403 等调用方或配置问题不应与服务不可用混为一谈。

连环追问：
- **先重试还是先 fallback？**
  答：先按错误和剩余 deadline 判断；同后端瞬时错误可少量退避重试，健康备用后端可直接 fallback。
- **重试会不会放大故障？**
  答：会，必须限次、退避、抖动并受总 deadline/熔断器约束。
- **失败是否计入熔断？**
  答：超时、网络、429 和 5xx通常计入；本地参数、排队与鉴权配置错误不应同等计入。
- **用户收到什么响应？**
  答：收到稳定脱敏的 429/502/503/504 或 SSE error，并带可关联 requestId。
- **SSE 已经发出部分内容后失败，怎么办？**
  答：发送流内错误/结束事件，保留 partial 状态，不切换模型拼接。

### 13.2 MySQL 故障

**面试官：MySQL 挂了，对系统有什么影响？**

**回答：** 登录、注册和需要数据库读取的接口会直接受影响；聊天记录写入通过 MQ 在旁路，数据库短暂故障不应中断已经进行的 LLM/SSE 主链路。连接池在借用 ping 或健康检查中发现失活连接，销毁并尝试补充；持续失败要让获取连接超时、快速返回和告警，而不是无限重连占满线程。

当前会话上下文主要在内存，所以已加载会话仍可聊天，但历史恢复和持久化会受影响。如果未来上下文迁移 Redis/MySQL，需要定义降级为最近内存窗口还是明确返回会话不可用。异步落库失败必须保留消息重试；当前可靠性链路不完整，不能宣称数据库恢复后一定零丢失。

连环追问：
- **主链路是否依赖 MySQL？**
  答：登录等入口依赖；已建立的聊天生成不等待聊天记录落库，但会话恢复可能依赖持久化。
- **异步落库失败是否影响对话？**
  答：设计上不影响当前回答，但影响记录可靠性，需要重试与告警。
- **连接池如何发现连接不可用？**
  答：借用活性检查和冷端健康线程 ping。
- **是否有重试或降级？**
  答：坏连接会销毁并补充；数据库持续故障时应获取超时和快速失败，不能无限重试。
- **会话上下文如果依赖 MySQL，如何处理？**
  答：可降级最近内存窗口或明确返回不可恢复；需按产品一致性要求选择。

### 13.3 RabbitMQ 故障

**面试官：RabbitMQ 挂了，对话还能继续吗？**

**回答：** 从业务解耦目标看，对话和 SSE 应能继续，但聊天记录可能无法持久化。当前项目没有完整本地 outbox，publish 失败主要依赖错误处理/告警，存在丢记录风险。是否同步降级写 MySQL 取决于产品优先级：它能降低丢失但会重新把数据库耦合到主链路，并可能在 MQ 和 DB 同时异常时拖垮线程。

若要求记录不可丢，更合理的是先写本地事务 outbox/WAL，再由后台投递 MQ；或把业务数据与 outbox 放在同一数据库事务。响应成功与数据可靠性必须有明确 SLA，不能一边说异步解耦、一边默认消息绝不会丢。

连环追问：
- **生产消息失败如何处理？**
  答：记录失败并有限重试；严格可靠场景先写 outbox/WAL。
- **是否本地暂存？**
  答：当前没有完整本地暂存；这是不可宣称零丢失的原因。
- **是否直接同步落库？**
  答：可作为有界降级，但会重新耦合主链路，不能无限等待。
- **数据丢失和响应成功之间如何权衡？**
  答：由 SLA 决定：体验优先可最终一致，审计不可丢则响应成功前必须先落可靠 outbox。

### 13.4 客户端断开

**面试官：客户端 SSE 连接断开后，后台模型调用怎么办？**

**回答：** Muduo 通过连接 close/error callback 感知 socket 关闭，之后回投发送任务必须检查连接是否仍有效。当前代码通过连接指针维持对象生命周期，但“对象还活着”不等于“TCP 仍连接”；正在执行的 curl 不一定因浏览器断开自动停止，可能继续占业务线程和模型额度。

生产级应给每次请求一个共享取消状态：close callback 标记 cancelled；业务线程取队列任务前检查；curl progress/write callback 周期检查并返回中止；所有资源在 `Completed/Failed/TimedOut/Cancelled` 之一的终态统一释放。用户消息应保留，助手消息按产品语义记录 cancelled/partial，不能把半截答案标为正常完成。当前这套端到端取消尚未全部落地，应如实说明。

连环追问：
- **如何感知连接关闭？**
  答：Muduo 的 close/error callback 感知，并更新请求取消状态。
- **已排队任务能否取消？**
  答：任务执行前检查 cancelled/deadline 即可跳过；需队列任务携带 RequestContext。
- **正在执行任务能否取消？**
  答：curl progress/write callback 检查取消标记并主动中止。
- **资源如何释放？**
  答：由请求状态机收敛到终态，RAII 释放 curl、缓冲、连接引用和临时上下文。
- **是否还落库？**
  答：用户消息保留，助手按 cancelled/partial 状态决定是否保存部分内容。

## 14. 项目相关手撕题

### 14.1 令牌桶

**面试官：手写一个线程安全令牌桶。**

**回答思路：** 保存 `tokens`、`capacity`、`ratePerSecond`、`lastRefill` 和 mutex。`tryConsume(n)` 加锁后先用 steady_clock 计算 `elapsed * rate`，令 `tokens = min(capacity, tokens + refill)` 并更新 lastRefill；若 tokens 足够则扣减并返回 true，否则 false。使用 monotonic 的 steady_clock 避免系统时间回拨。

按用户维护时外层 `unordered_map<userId, TokenBucket>` 的查找/创建也要加锁或分片，桶本身再负责内部原子性。记录最后访问时间并定期淘汰闲置桶；如果所有操作都用一把全局锁，用户数高时会形成热点。

追问：
- **如何根据时间补 token？**
  答：用 steady_clock 计算距 lastRefill 的秒数乘 rate，再加到当前 token。
- **如何限制最大 token 数？**
  答：补充后执行 `min(capacity, tokens + added)`。
- **多线程并发获取 token 怎么做？**
  答：同一桶的补充和扣减放在同一临界区，避免超发。
- **如果要按用户维护多个桶，怎么组织数据结构？**
  答：分片 unordered_map 保存 userId 到桶，配 lastAccess 做 TTL 淘汰。

### 14.2 简单 HTTP 路由器

**面试官：实现一个简单 HTTP router，支持 method + path 匹配。**

**回答思路：** 静态路由可用 `unordered_map<MethodPath, Handler>`，key 由 method 与规范化 path 组成，平均 O(1)。动态路径在注册时把 `/users/:id` 转为预编译 regex 或使用 trie，匹配后将捕获组写入 path params。先查静态再查动态，避免动态规则遮蔽精确路由。

判断 404/405 需要区分“path 完全不存在”和“path 存在但当前 method 没 handler”，因此不能只做一次复合 key 查询。正则列表线性扫描是 O(R)，路由多时可按 method/前缀分桶或 trie 降低候选数。

追问：
- **如何支持路径参数？**
  答：注册时把参数段编译为捕获组或 trie 参数节点，匹配后写入 request params。
- **如何支持正则路由？**
  答：注册时预编译 regex，按 method/前缀缩小候选集。
- **路由匹配复杂度是多少？**
  答：静态哈希平均 O(1)，线性正则是 O(R × matchCost)，trie 接近 O(path length)。
- **如何处理 404 和 405？**
  答：先判断路径是否存在，再判断 method；不存在 404，存在但 method 不支持 405。

### 14.3 线程池

**面试官：手写一个线程池。**

**回答思路：** 线程池包含有界任务队列、mutex、`notEmpty/notFull` 条件变量、stop 标志和 worker 数组。submit 在锁内检查停止和队列容量，可选择阻塞、超时或拒绝；入队后 notify_one。worker 用带谓词 wait，取出任务后立即解锁再执行，捕获异常避免 worker 线程退出。

shutdown 先停止接收新任务，再选择 drain 队列或取消剩余任务，notify_all 并 join。LLM 场景必须有界队列和 deadline；任务抛异常要由 future/统一异常处理传递，不能让 `std::terminate` 杀进程。

追问：
- **任务队列怎么实现？**
  答：用 mutex 保护的有界 deque，生产者入队、worker 出队。
- **条件变量如何使用？**
  答：wait 必须带谓词防虚假唤醒，状态变化后在锁外/合适位置 notify。
- **如何停止线程池？**
  答：停止接收、设置 stop、notify_all，选择 drain 或取消后 join。
- **任务抛异常怎么办？**
  答：worker 边界捕获，或通过 packaged_task/future 传给提交方。
- **队列满了怎么办？**
  答：采用拒绝、调用方运行或带超时等待；LLM 服务不应无限扩容队列。

### 14.4 RAII 连接池句柄

**面试官：写一个自动归还连接的 RAII wrapper。**

**回答思路：** 使用禁止拷贝、允许移动的句柄，内部保存 `Connection*` 与安全的 pool state；析构时 noexcept 地归还，移动构造把源指针置空。也可以直接使用 `unique_ptr<Connection, PoolDeleter>`，其类型天然表达独占和移动。

连接池 shutdown 要先停止新借用，等待借出句柄归还或让 deleter 识别 pool 已关闭后直接销毁连接；不能让 deleter 访问悬空池指针。归还过程中发生异常不能从析构抛出，只能记录并销毁该连接。

追问：
- **禁止拷贝还是允许拷贝？**
  答：连接句柄禁止拷贝，防止一个连接出现多个独立归还责任。
- **是否支持移动？**
  答：支持，移动后源句柄置空，所有权转给目标。
- **连接池析构顺序如何处理？**
  答：先停止借用和后台线程，再等借出句柄，最后销毁空闲连接与 pool state。
- **归还连接失败怎么办？**
  答：析构不能抛；记录错误并直接销毁连接，不把未知状态连接放回池。

## 15. 面试模拟追问链

### 链路 A：从 Muduo 到异步响应

**串联理解：** main Reactor accept 后将连接交给 sub EventLoop；I/O 线程解析 HTTP，但不执行秒级 LLM；handler deferred 后投业务线程，完成时通过 runInLoop 回原 loop。连接断开需要 weak reference/状态检查与取消信号；请求状态机防止正常完成、超时和断连重复响应；SSE 已输出后失败只能流内报错并结束。

1. **Muduo 的线程模型是什么？**
   答：主 Reactor accept，sub Reactor 采用 one loop per thread 处理连接 I/O。
2. **为什么 LLM 调用不能在 I/O 线程执行？**
   答：同步外调延迟不可控，会阻塞同 EventLoop 上所有连接。
3. **业务线程处理完如何发送响应？**
   答：将发送 lambda 投到连接所属 EventLoop。
4. **为什么要 `runInLoop`？**
   答：保证 TcpConnection 缓冲和 Channel 状态只在归属线程修改。
5. **如果连接已经断开怎么办？**
   答：weak_ptr/连接状态检查失败后停止发送并传播取消。
6. **deferred response 怎么避免重复响应？**
   答：原子终态标记保证只有一个完成路径。
7. **SSE 流式响应中途失败怎么处理？**
   答：发送流内 error 并结束，不切换模型拼接。

### 链路 B：从连接池到 C++ RAII

**串联理解：** 连接池减少建连成本；LIFO 复用热连接并让冷连接沉到 deque 头部；unique_ptr + deleter 表达独占借还。deleter 的池指针要求严格 shutdown 顺序；健康线程锁内摘出、锁外 ping、冷区保序放回，双时间戳分别表达业务热度和检查节奏；获取超时后业务快速失败而不是无限挂起。

1. **MySQL 连接池为什么需要？**
   答：复用昂贵数据库会话并限制并发连接总量。
2. **为什么用 LIFO？**
   答：优先复用热连接，减少空闲失活，并让冷连接沉到头部便于探活/缩容。
3. **`unique_ptr + deleter` 如何自动归还连接？**
   答：借出时移动 unique_ptr，析构时 deleter 将连接放回池尾。
4. **deleter 捕获连接池指针安全吗？**
   答：仅在池生命周期覆盖借出句柄时安全。
5. **锁外 ping 有什么并发风险？**
   答：数据库侧状态仍可能变化，但候选已摘出，不会被本地线程并发使用。
6. **时间戳保护期如何解决？**
   答：lastReturned 维持冷热，lastChecked 限制探活频率。
7. **获取连接超时后业务怎么返回？**
   答：转换为服务繁忙/数据库不可用错误并记录等待指标。

### 链路 C：从网关治理到熔断

**串联理解：** 用户桶保护公平，后端桶保护下游；限流是速率准入而非计费。请求通过后执行带超时 curl，按 HTTP/业务错误分类报告后端熔断器；CLOSED 达阈值转 OPEN，冷却后半开探测。fallback 只用于未向客户端输出的可替代调用，流式中途失败不能安全切换。

1. **为什么需要限流？**
   答：保护入口公平和下游配额，避免队列与线程被突发耗尽。
2. **用户级和后端级限流区别是什么？**
   答：前者约束单用户，后者约束所有用户对某后端的总速率。
3. **令牌桶怎么实现？**
   答：按 elapsed 惰性补 token，在锁内判断并扣减。
4. **超时算不算失败？**
   答：后端响应超时算；本地排队超时不应计入后端熔断。
5. **三态熔断状态如何转移？**
   答：CLOSED 达阈值到 OPEN，冷却后 HALF_OPEN，探测成功关闭、失败重开。
6. **half-open 放多少请求？**
   答：按配置；单探针稳妥，多探针恢复更快。
7. **fallback 返回什么才合理？**
   答：返回备用后端完整业务结果并记录实际路由，不泄露内部配置。

### 链路 D：从压测到性能分析

**串联理解：** 12.25W 是静态 `/entry` 的 HTTP 短路径基线，不代表 LLM；11.24W 是静态 `/menu` 加 Session 鉴权，不是正则路由。当前没有动态正则路由压测，不能从现有数据判断正则开销。P99 要按 EventLoop、线程池、锁、连接池和下游分段；perf 看 CPU 栈宽度，gdb 看阻塞点。I/O CPU 打满时先确认热点，再考虑增加 I/O 线程、减少解析/日志/拷贝或把误放的业务移出 loop，不能盲目扩线程。

1. **12W QPS 测的是什么链路？**
   答：同机环境下 HTTP 解析、路由和短响应链路。
2. **为什么不能代表真实 LLM 调用吞吐？**
   答：没有外调模型，真实吞吐主要受秒级推理和后端配额限制。
3. **现有数据能说明正则路由只比静态路由低一点吗？**
   答：不能。11.24W 对应 `/menu` 静态路由和 Session 鉴权，项目没有正则路由实测。若要比较，必须注册专用动态路由并控制 handler、响应体、中间件和 wrk 参数完全一致。
4. **P99 高怎么排查？**
   答：按 EventLoop、线程池、锁、连接池、下游和输出缓冲分段定位。
5. **perf 火焰图怎么看？**
   答：看最宽调用栈及其上游来源，宽度代表 CPU 采样占比。
6. **如果 I/O 线程 CPU 打满，怎么优化？**
   答：先定位热点，再减少日志/解析/拷贝或误放业务，必要时按核数增加 I/O loop。

## 16. 当前实现边界与后续演进

这一节统一回答项目距离生产级还有什么差距。边界不等于项目没有价值：当前已经完成了 Reactor 与慢 LLM 调用解耦、跨线程回投、策略协议适配、网关限流/熔断/超时、RAII 数据库资源管理和 MQ 异步落库。下面列出的是在单机工程原型范围之外，不能表述为已完成的能力。

**面试官：你认为这个项目当前最大的实现边界是什么？如果继续演进，优先级如何排？**

**回答：** 我把边界分为资源收敛、流式可靠性、调用治理、持久化一致性、单机状态和可观测性六类。最高优先级不是立刻做分布式，而是先保证慢客户端、断连和超时不会继续无界占用本地线程、内存和上游配额；其次保证异步落库可恢复、可去重；最后再扩展多实例治理和更复杂的模型协议。

| 优先级 | 当前实现边界 | 真实影响 | 下一步动作 | 详细说明 |
|---|---|---|---|---|
| P0 | 聊天长任务仍强持有 TcpConnectionPtr；尚无完整 RequestContext、统一 deadline 和 close callback 到 curl 的取消链路 | 客户端断开或逻辑超时后，业务线程和上游 LLM 仍可能继续工作；正常、超时和断连缺少统一终态仲裁 | 长任务改持有 weak_ptr + RequestContext；用原子终态与取消标记收敛结果；在 curl progress/write callback 中检查取消 | 见 3.3、6.3、13.4 |
| P0 | 业务线程池任务队列无界；SSE 没有高水位背压、最大 outputBuffer 或 token 微批策略 | 下游慢、客户端慢或突发流量时，任务和输出缓冲可能堆积，带来延迟放大和 OOM 风险 | 有界队列、排队 deadline、拒绝策略；高水位暂停或取消上游，并按时间或字节对 token 批量发送 | 见 3.3、5.2、6.3 |
| P1 | 当前 curl 只有连接超时和单次请求总超时 | 排队、工具路由、主后端和 fallback 的时间可能叠加；没有首包和流式空闲超时 | 用 steady_clock 在请求进入时创建统一 deadline，并区分排队、连接、首包、idle 和总超时 | 见 6.3 |
| P1 | 已实现 [DONE] 结束本次响应并保留 HTTP keep-alive 连接供后续请求复用，但没有 SSE 心跳、POST 流重连、事件 ID 或断点续传 | 代理可能缓冲或按空闲超时断流；流中断后无法从已发送位置恢复 | 关闭代理缓冲、增加注释心跳；以请求 ID 和事件序号设计应用层重连与恢复协议 | 见 5.1、5.2、18.3 |
| P1 | MQ 解耦已完成，但没有 publisher confirm、持久化队列/消息、outbox、DLQ 和稳定 messageId 幂等约束 | 进程或 broker 故障下聊天记录可能丢失或重复；MQ 成功不等于数据库已提交 | 使用结构化事件代替原始 SQL，DB commit 后手动 ack；增加 confirm、持久化、幂等唯一键和失败重试/DLQ | 见 9.2、9.3、13.3、18.6 |
| P1 | 同一会话仅靠 AIHelper mutex 互斥；任务先进入全局线程池，未保证服务端 FIFO，脚本可并发提交 | 后到请求可能先进入上下文；等待会话锁的任务占用 worker，MQ 落库顺序也无严格约束 | 接入时分配 session sequence，使用有界 FIFO mailbox/单消费者串行执行；队列满或活动流时拒绝，取消与落库按 sequence 收敛 | 见 11.3、9.3 |
| P2 | 限流和熔断状态仅在单进程内；配置主要启动时加载 | 多实例时总额度被放大、各实例健康判断不一致；路由和阈值无法安全动态调整 | Redis/Lua 或集中限流；按 provider/model/instance 聚合健康；不可变配置快照和受控热更新 | 见 6.2、7.1、10.2 |
| P2 | AIHelper 只做部分编排：RAG 由前端 modelType 选择，工具由 enableTools 触发，三类链路没有统一执行计划 | 前端耦合底层路径，普通问答、RAG、工具难以组合 | 拆出 Orchestrator 生成直连、RAG、工具或组合计划；会话、协议适配与执行治理各自分层 | 见 10.1、11.1、11.2、18.8 |
| P2 | 工具调用依赖 prompt 输出自定义 JSON，只做一次格式修复；未接入原生 tools/tool_calls/tool_call_id | 格式受提示词漂移影响，不利于多工具、多轮和调用关联 | provider adapter 适配原生协议；编排层完成校验、执行、结果回传和 deadline/轮数控制 | 见 11.2、18.8 |
| P2 | AIHelper 全量持有会话消息；模型仅临时保留首轮加最近 N 轮，未做 token 摘要或历史懒加载 | 长会话增加内存、token 成本与数据库压力 | 推理窗口采用 token 预算、摘要加最近 K 轮；完整历史按 sequence 分页加载并受 TTL/LRU 限制 | 见 11.3 |
| P2 | 连接池固定预建 100 条，shutdown 仍依赖借出句柄先释放 | 资源基线偏高，停机时 deleter 存在生命周期约束 | 支持最小/最大容量、停止新借用、等待归还或独立共享 pool state | 见 3.4、8.1、8.3、13.2 |
| P3 | 缺少统一 metrics、trace/requestId 传播和可复现的真实 LLM 端到端压测 | 无法以 P50/P99、首 token、token 间隔、队列等待、后端错误率快速定位瓶颈 | 建立分阶段指标和脱敏结构化日志；用可控 mock provider 或固定脚本测 TTFT、流式 idle、取消和 fallback | 见 12.1、12.2 |

还有几个需要如实说明的框架与安全边界：动态正则 Callback 目前没有将携带路径参数的 newReq 传入回调；路由注册缺少完整冲突检测；数据库和模型密钥仍应迁移为环境变量或密钥服务引用，日志必须避免输出 Authorization、明文密钥和完整隐私 body。

连环追问：
- **为什么把断连取消和背压放在最高优先级？**
  答：它们直接决定资源是否有上界。没有取消，断开的客户端仍会占业务线程和模型额度；没有背压，慢客户端会令 Muduo outputBuffer 和任务队列持续增长，最终影响健康请求。
- **SSE 已经能发送 [DONE]，为什么还不能称为可靠流式服务？**
  答：[DONE] 只表示本次应用流正常结束。可靠流还需要处理代理空闲超时、心跳、客户端中断、重连游标、慢客户端背压和上游取消；当前只完成了基本传输与 HTTP keep-alive 复用。
- **RabbitMQ 已经异步落库，为什么还可能丢消息或重复？**
  答：异步只实现主链路解耦。没有 publisher confirm 和 durable/outbox 时 producer 到 broker 可能丢；没有稳定 messageId 与幂等约束时消费者重试可能重复；数据库提交和 ack 之间也需要明确事务顺序。
- **为什么不立刻把限流和熔断做成分布式？**
  答：当前先解决单进程内的调用治理和状态正确性。分布式需要共享存储、故障模型、一致性与成本设计；在单实例尚未补齐取消、背压和观测前，先做分布式不会解决最直接的资源泄漏风险。
- **面试时如何避免把演进方向说成项目成果？**
  答：使用固定句式：当前已实现什么、它解决了什么、边界在哪里、生产上下一步如何做。例如当前有 curl 级超时；统一 deadline 和主动取消是下一步，不能说项目已实现请求级取消。

## 17. 这个项目最该提前准备的答案

这一节不是额外背诵清单，而是理解验收标准：每个主题都应能从“对象由谁拥有、代码在哪个线程执行、失败后状态如何收敛、方案引入什么代价”四个方向继续回答。

- 3 分钟项目介绍。
- 一张架构图。
- 一条普通请求完整链路。
- 一条 SSE 流式请求完整链路。
- 一条下游模型失败链路。
- Muduo I/O 线程和业务线程的边界。
- deferred response 的生命周期。
- MySQL 连接池的 RAII 和并发安全。
- 限流、超时、fallback、熔断的状态流转。
- wrk 压测范围和指标解释。

## 18. 第三方库与协议：应达到的工程理解层

这一节的目标不是阅读源码，而是对每项都能说清：为什么用、系统里怎么用、关键机制、失败时怎么办。对于没有在项目中实际使用的库或功能，面试时应明确说“了解原理，未在当前项目落地”，不要把学习内容包装成项目实现。

### 18.1 JSON 库：`nlohmann/json` 或等价 JSON 库

**面试官：LLM 的请求和响应 JSON 是怎么处理的？为什么选择这个 JSON 库？**

**回答：** 项目用 nlohmann/json 的 DOM API。请求阶段由 strategy 把统一消息构造成 object/array/string/bool/number/null 等 JSON 值，再 `dump()` 序列化为 curl body；普通响应由网关获得完整 body 后 parse，流式响应则先按 SSE 行切分，再逐个 parse chunk。这个库接口直观、适合 LLM 这种字段频繁变化且重可读性的控制协议，代价是 DOM 构建、字符串分配和拷贝比 Protobuf/专用流式解析更重。

解析不能直接写一长串嵌套 `j["choices"][0]["delta"]["content"]`。应先判断 object/array、`contains` 与类型，再逐层读取并产生带 provider/backend 的错误上下文。`at()` 缺字段会抛异常且不会插入；非 const `operator[]` 可能创建缺失字段，容易掩盖错误。HTTP 200 但包含 error、缺必需字段或非法 JSON仍是后端协议失败；是否熔断应看它是否说明后端异常，稳定的本地适配 bug 不应靠 fallback 掩盖。超大 JSON 可改 SAX/流式解析，但当前 token chunk 较小，不是首要优化。

连环追问：
- **JSON 的基本类型有哪些？对象、数组、字符串、数值、布尔、null 如何表示？**
  答：nlohmann/json 直接用 object、array、string、number、boolean 和 nullptr/null 表示。
- **序列化和反序列化分别发生在请求链路的哪一步？**
  答：strategy 构造 JSON 后 dump 为请求 body；网关/strategy 收到 body 或 chunk 后 parse。
- **OpenAI 兼容接口的普通响应和流式 chunk 的 JSON 结构有哪些差异？**
  答：普通响应一次包含完整 message/content；流式通常是多个 choices[].delta.content 和 finish_reason。
- **缺字段、字段类型不对、响应不是合法 JSON 时如何处理？**
  答：逐层校验并抛协议错误，记录脱敏上下文，不能让异常穿透线程边界。
- **`at()` 和 `operator[]` 取字段时的行为差异是什么？**
  答：at 缺失会抛；非 const `[]` 可能创建缺失键，容易掩盖错误。
- **如何避免用一串嵌套下标直接取字段导致异常难以定位？**
  答：分层 contains/type 检查，封装带字段路径和 provider 信息的解析函数。
- **JSON 解析失败应计入后端失败、触发 fallback 或熔断吗？**
  答：远端偶发非法响应可计后端协议失败并 fallback；稳定本地适配 bug 不应靠熔断掩盖。
- **JSON 很大时，DOM 解析的内存和延迟代价是什么？**
  答：会复制并构建完整树，峰值内存可数倍于文本；超大数据可用 SAX/流式解析。

建议掌握：
- JSON 是文本格式，便于调试和兼容，但解析、字符串分配通常比 Protobuf 更重。
- 请求构造使用明确字段；响应解析先校验错误结构和必需字段，再读取业务字段。
- 要区分 HTTP 成功但 JSON 内携带业务错误，与真正可用的业务响应。
- `nlohmann/json` 的易用性很适合控制面和 LLM 协议适配；如需极端性能或超大响应，可评估流式/SAX 解析，但不是当前项目必须项。

### 18.2 HTTP 与 OpenAI 兼容协议

**面试官：调用模型后端时，HTTP 请求包含哪些关键内容？**

**回答：** 网关从策略和配置得到 URL、POST method、JSON body、额外 header、API Key 和超时。`Authorization: Bearer` 用于供应商身份认证，`Content-Type: application/json` 声明请求体格式；curl 分别配置连接超时、总超时和写回调。策略负责厂商字段，HTTP 执行层负责发送和收集状态，职责不要混在 handler。

错误分三层：curl code 是 DNS/连接/TLS/超时等传输错误；HTTP status 是远端网关/服务状态；HTTP 200 内的 error 或结构异常是业务/协议错误。4xx 参数与鉴权通常重试无效，429 可按 Retry-After/退避处理，5xx/网络瞬时错误可有限重试或 fallback。所有重试共享总 deadline、限制次数并加抖动，避免放大故障。API Key 从 secret/env 读取，日志只记录 backend ID、耗时和脱敏错误，禁止打印 Authorization 或完整敏感 body。

OpenAI-compatible 只表示主要请求字段和 SSE 事件相似，不表示所有供应商的错误、usage、工具和结束原因完全相同；差异应收敛在 provider adapter。

连环追问：
- **URL、method、header、body、timeout 分别如何构造？**
  答：配置给 URL/Key/timeout，strategy 给 JSON body/附加头，HTTP 层统一 POST 与 curl 选项。
- **`Authorization: Bearer`、`Content-Type: application/json` 分别解决什么问题？**
  答：前者携带认证凭据，后者声明请求体媒体类型。
- **HTTP 状态码、网络错误、JSON 业务错误三者如何区分？**
  答：分别读取 CURLcode、HTTP response code 和解析后的 error/必需字段。
- **重试哪些错误比较合理？为什么 4xx 通常不应盲目重试？**
  答：网络瞬断、部分 429/5xx 可重试；大多数 4xx 是确定性参数/权限问题，重试不会改变结果。
- **如何避免重试放大后端故障？**
  答：限制次数、指数退避加抖动、总 deadline、熔断和重试预算。
- **API key 如何从配置中读取并避免打印到日志？**
  答：配置只存 secret 引用，从环境/密钥服务解析；日志对 Authorization 和 body 脱敏。

需要能说明：
- OpenAI 兼容是“请求/响应字段和 SSE 事件格式尽量兼容”，不等于所有供应商的语义完全相同。
- 适配层应把供应商差异收敛在请求构造、响应解析和错误映射中，不能泄漏到路由或业务层。

### 18.3 SSE 协议边界

**面试官：SSE 协议本身有哪些约束？**

**回答：** 服务端返回 `Content-Type: text/event-stream`，每条事件由若干字段行组成并以空行结束。`data` 可有多行并由客户端合并；`event` 指定事件类型；`id` 更新客户端 Last-Event-ID；`retry` 建议浏览器重连间隔。心跳通常用注释行 `: ping`，用于穿过代理空闲超时，不应伪造业务空数据。

自动重连是 EventSource 客户端行为，服务端要支持 Last-Event-ID 才能断点续传；本项目用 POST 发聊天，不能直接假设原生 EventSource 的全部重连语义。Nginx 等代理可能缓冲响应，部署时要关闭缓冲并及时 flush。SSE 是单向 HTTP 流，WebSocket 是 Upgrade 后的双向帧协议。项目重点是连接线程归属、错误收尾和背压，当前没有完整 id/retry/断点续传实现。

连环追问：
- **响应头为什么是 `Content-Type: text/event-stream`？**
  答：让客户端和代理按 SSE 事件流而非普通完整文本处理。
- **一条事件为什么通常以空行结束，例如 `data: {...}\n\n`？**
  答：SSE 规范用空行作为事件边界，多行 data 在边界处组合分发。
- **`event`、`id`、`retry` 字段分别有什么作用？**
  答：event 指事件类型，id 设置重连游标，retry 建议客户端重连等待。
- **SSE 的自动重连由谁发起？服务端需要如何处理 `Last-Event-ID`？**
  答：EventSource 客户端发起；服务端若支持续传，应从该 id 后恢复或明确不支持。
- **为什么反向代理可能缓冲 SSE？如何避免长时间不刷新给客户端？**
  答：代理默认优化普通响应会聚合 body；需关闭 buffering、禁用缓存并及时 flush。
- **心跳消息解决什么问题？**
  答：防止代理/负载均衡按空闲超时断开，并帮助发现死连接。
- **SSE 和 WebSocket 的边界是什么？**
  答：SSE 是 HTTP 单向文本事件；WebSocket 是升级后的双向帧通道。

回答边界：
- SSE 是基于 HTTP 的服务端单向推送；浏览器原生支持重连，适合“客户端发起一次请求，服务端持续返回 token”的 LLM 场景。
- WebSocket 是基于 HTTP Upgrade 建立的双向应用层协议，适合客户端也需持续主动推送的实时交互；它不是“TCP 层协议”。
- 你项目的重点是连接生命周期和线程归属，不是自行实现浏览器重连协议。

### 18.4 Muduo：库的使用边界

**面试官：Muduo 帮你屏蔽了哪些复杂性，你仍然需要自己负责什么？**

**回答：** Muduo 屏蔽 epoll_ctl/epoll_wait、非阻塞 socket、accept 分发、Buffer 读写、连接回调、TimerQueue 和跨线程 wakeup 等通用网络细节。`EventLoop` 是线程事件循环，`Channel` 是 fd 与事件回调的绑定，`TcpConnection` 管连接状态，`Buffer` 管字节，`TimerQueue` 管定时任务。Channel 必须归属一个 loop，才能让事件掩码和回调串行化。

Muduo 不替业务解决 HTTP 语义、路由、LLM 阻塞调度、业务 deadline、慢客户端背压、客户端断开后取消上游、幂等和错误映射。输出缓冲积压会吃光应用内存；高水位回调只是观测入口，还要有暂停/取消策略。`runInLoop` 用于“在 loop 线程可立即执行，否则排队”，`queueInLoop` 用于无条件延后。连接关闭时必须区分对象生命周期和 socket 连接状态，持有 shared_ptr 只能防析构，不能保证仍可发送。

连环追问：
- **Muduo 中 `EventLoop`、`Channel`、`TcpConnection`、`Buffer`、`TimerQueue` 各自承担什么职责？**
  答：分别负责循环调度、fd 事件、连接状态、字节缓冲和定时任务。
- **`Channel` 为什么只能归属一个 EventLoop？**
  答：保证事件注册和回调在一个线程串行，避免同 fd 并发状态修改。
- **Muduo 处理 socket 事件，不代表业务回调可以阻塞，对吗？**
  答：对；业务回调阻塞会卡住该 EventLoop 的所有连接。
- **`TcpConnection` 的生命周期和关闭回调怎么处理？**
  答：由 shared ownership 覆盖回调执行期，close callback 移除 Channel 并通知上层取消。
- **输出缓冲区积压时有什么风险？**
  答：内存无界增长、延迟扩大和 OOM，需要高水位与背压。
- **什么时候用 `runInLoop`，什么时候用 `queueInLoop`？**
  答：希望目标线程内立即执行用 runInLoop；希望无条件延后一轮用 queueInLoop。

掌握标准：
- 说清 Muduo 封装了 epoll 事件分发、连接生命周期、定时器和线程归属。
- 说清业务仍需负责协议解析、业务调度、超时、背压、断连后的任务清理和跨线程对象引用安全。

### 18.5 MySQL Connector / C API 与预编译语句

**面试官：PreparedStatement 相比拼接 SQL 有什么价值？**

**回答：** PreparedStatement 把 SQL 结构与参数值分开，驱动按类型绑定和转义，避免用户输入改变 SQL 语法，是防 SQL 注入的主要手段；也提升可读性。是否复用执行计划取决于 Connector 和 MySQL 行为，不应绝对承诺性能一定更高，一般情况下预编译语句如果缓存至内存中并复用，能直接省去语法/词法分析的开销，直接命中参数替换之后的执行计划。

生命周期上 Connection 拥有数据库会话，PreparedStatement 依赖 Connection，ResultSet 又依赖 Statement 和 Connection。项目用池化连接 RAII handle，同时用 `DbQueryResult` 绑定 statement/result set，保证结果使用期间依赖仍活着，并由自定义 deleter 显式 close 服务端 statement。连接错误应使该连接退出池，SQL 语法/约束错误返回业务失败，超时需要取消并判断连接是否还能复用。单条聊天 insert 可独立事务；多条必须原子提交时才需要事务边界。MQ 消费应在数据库事务 commit 后 ack。

连环追问：
- **参数绑定如何避免 SQL 注入？**
  答：SQL 结构固定，驱动按参数类型编码值，输入不能变成 SQL 语法。
- **Statement、ResultSet、Connection 之间的生命周期关系是什么？**
  答：ResultSet 依赖 Statement，Statement 依赖 Connection，销毁顺序必须反向。
- **为什么 ResultSet 不能在其依赖的 Statement/Connection 已释放后继续使用？**
  答：它可能仍读取驱动缓冲、游标和服务端会话资源，依赖statement释放后再访问会悬垂导致未定义。
- **连接池返回的是裸连接、RAII handle 还是智能指针？**
  答：返回带 PoolDeleter 的 unique_ptr RAII handle。
- **MySQL 错误分为连接错误、SQL 错误、超时错误时，分别如何处理？**
  答：连接错误淘汰连接；SQL 错误返回业务/开发错误；超时取消并重新评估连接是否可复用。
- **事务边界如何确定？异步落库是否需要事务？**
  答：一个不可分割业务动作一个事务；单 insert 可自提交，DB commit 必须先于 MQ ack。

回答边界：
- PreparedStatement 的主要价值是参数化、可读性和避免反复拼接 SQL；是否有执行计划复用取决于驱动和数据库行为，面试时不要绝对化。
- 你使用 RAII 的核心是严格表达借用连接和查询资源的释放顺序。

### 18.6 RabbitMQ 与 AMQP 0-9-1

**面试官：RabbitMQ 的消息从生产者到落库消费者，可靠链路如何设计？**

**回答：** 可靠链路分段保证：生产者用 confirm 确认 broker 接收；exchange/queue durable 且消息 persistent 保护 broker 重启；consumer 手动 ack 并且只在数据库事务成功后确认；失败按次数退避重试，永久失败进入 DLQ。任何单一措施都不是端到端“恰好一次”。

常见语义是至少一次，所以消费者必须用 messageId 唯一索引或幂等表处理重复。消息发到 MQ 只表示进入 broker，不表示已落库；消费成功后 ack 才能在消费者崩溃时重投。消息积压监控 ready/unacked 数、生产/ack 速率、最老消息年龄、DB 写时延和错误率，并对生产端限流或扩消费者。当前项目直接发布 SQL 字符串，并在 handler 返回后显式 ack；它实现了解耦，但没有 confirm、持久化、事务闭环、幂等键、重试和 DLQ，因此尚未形成完整可靠链路。

连环追问：
- **exchange、queue、binding、routing key 分别是什么？**
  答：exchange 路由入口进行消息分发，queue 存消息由消费者进行消费，binding 是绑定关系路由规则，routing key 是消息携带的路由标签。producer通过将携带routing key 的消息发布给 exchange 交换机，交换机通过选择的模式进行消息投放，direct 模式下，需要精准匹配 routing key 和 队列的 binding key，其他模式下 routing key 和 binding key 有不同的匹配规则；
- **direct、topic、fanout 交换机适用什么场景？**
  答：direct 需要精确匹配，所以精确任务路由用 direct；topic 支持通配符模糊匹配，所以主题订阅用 topic；fanout 直接无视消息中的routing key，对绑定当前 exchange 的队列进行消息投放，所以广播通知用 fanout。
- **publisher confirm、持久化消息、durable queue、消费者手动 ack 分别保护哪一段？**
  答：依次保护 producer 到 broker、消息数据重启恢复、队列元数据重启恢复、consumer 处理完成。
- **为什么“消息发到了 MQ”不等于“一定已经落库”？**
  答：broker 接收后还要排队、投递、消费和 DB commit，任一段都可能失败。
- **消费成功后为什么再 ack？重复投递怎么做幂等？**
  答：commit 后 ack 防止处理前丢失；重投用 messageId 唯一约束去重。
- **重试何时应该进入死信队列（DLQ）？**
  答：超过次数/年龄，或确定是永久格式和约束错误时。
- **消息积压、消费变慢时如何通过指标和限流发现？**
  答：监控 ready/unacked、最老年龄、publish/ack 速率和 DB 时延，必要时限产或扩容。

掌握标准：
- 正确说出 RabbitMQ 常见语义通常是“至少一次投递”，因此消费者要靠唯一消息 ID、数据库唯一键或幂等表处理重复。
- 异步落库提升主链路响应速度，但把一致性从同步调用转为最终一致性，需要明确可接受的数据丢失/延迟边界。

### 18.7 配置、日志与 CMake

**面试官：多模型配置和第三方依赖如何管理？**

**回答：** 配置区分模型协议配置和治理配置：URL/model/stream/header/secret 引用由策略与 HTTP 层使用，限流/熔断/fallback/timeout 由网关使用。阈值、路由和 Key 可以通过不可变快照热更新；线程池大小、连接池结构和依赖库版本通常需要受控重启或专门迁移。配置启动时做 schema 与引用校验。

日志不得记录 Authorization、明文 Key、完整用户隐私和未脱敏供应商 body；使用 requestId/backendId/errorClass/latency 等结构化字段。CMake 中头文件通过 `target_include_directories` 传递编译搜索路径，`target_link_libraries` 声明目标级链接关系及 PUBLIC/PRIVATE 传播，运行时还需动态库搜索路径。目标级配置比全局 include/link 更少污染。Debug 通常保留符号、低优化并可开 sanitizer；Release 高优化、可能关闭断言，仍应保留可用于线上定位的符号文件。

连环追问：
- **配置中哪些字段可以热更新，哪些字段不应热更新？**
  答：路由、阈值、Key 引用可快照更新；线程池/连接池结构和 ABI 依赖不宜无迁移热改。
- **API key 如何脱敏，日志中哪些内容不能记录？**
  答：只显示尾部少数字符；禁止 Authorization、完整 Key、用户隐私和未脱敏响应体。
- **CMake 如何区分头文件、链接库、运行时动态库？**
  答：include path 供编译找声明，link library 供链接解析符号，运行时 loader 再按 rpath/系统路径找 so。
- **`target_link_libraries` 为什么优于全局 include/link 设置？**
  答：目标级声明依赖及 PUBLIC/PRIVATE 传播，避免污染无关 target。
- **Debug 和 Release 构建的差异是什么？**
  答：Debug 通常低优化、有符号/断言；Release 高优化，线上仍应单独保存调试符号。

建议：
- 这些不是项目主线，但属于简历写有 CMake/Linux 工具后可能出现的补充题，准备到能解释日常工程实践即可。

### 18.8 原生 Function Calling

**面试官：原生 function calling 是什么？模型会直接调用你本地的 C++ 函数吗？**

**回答：** 不会。原生 function calling 是模型与应用之间的结构化工具协商协议：服务端在请求中通过 tools 声明工具名称、描述和参数 JSON Schema；模型返回 tool_calls，包含工具名、参数和 tool_call_id；服务端仍需校验参数、执行白名单工具，并以携带 tool_call_id 的 tool 结果再次调用模型生成最终答案。模型只负责提出调用意图，执行权、权限和安全边界始终在服务端。

相比当前 prompt-based 路由，原生协议不依赖提示词强约束模型输出自定义 JSON，工具调用与结果可通过 tool_call_id 稳定关联，并可一次返回多个 tool_calls。它通常仍是两阶段或多轮循环，不是一次 API 调用自动完成所有事情。多个独立只读工具可并行；有依赖或副作用的工具必须串行，并受最大轮数、总 deadline、权限和审计约束。

当前项目尚未接入 tools/tool_calls/tool_call_id，只实现了 prompt JSON 路由、工具白名单执行和第二轮流式回答。后续应由 AIStrategy 或 provider adapter 隔离不同厂商的 tools 请求和响应字段，AIHelper 上层的编排负责循环执行、超时和结果回传，LLMGateway 仍只负责远端调用治理。

连环追问：
- **有 JSON Schema 后还需要本地校验吗？**
  答：仍需要。Schema 约束模型交互格式，不是安全边界；服务端仍要校验工具白名单、参数范围、用户权限、网络目标和副作用。
- **为什么不把 function calling 放进 LLMGateway？**
  答：网关负责限流、熔断、超时和 HTTP 传输；工具调用属于会话业务编排，厂商协议字段由策略或 provider adapter 适配。
