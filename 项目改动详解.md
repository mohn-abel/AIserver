# 项目改动详解（相对开源原项目）

> 本项目基于开源教学项目 **Kama-HTTPServer / CppAIService**（youngyangyang04）二次开发。
> 本文逐文件记录**我在原项目基础上做出的全部改动**：改了什么、为什么改、改动后的效果。
> 对比基准：原项目 `AIApps/ChatServer` 与 `HttpServer` 目录的原始实现。
>
> **改动性质图例**：🆕 全新模块 ｜ ♻️ 实质重构/修复 ｜ 🔒 线程安全加固 ｜ 🧹 工程化/格式化（不计入能力证明）
>
> **术语说明**：文档中沿用项目里的 “MCP/工具” 命名，但当前实现更准确地说是 **prompt-based tool use / 工具路由**：第一次 LLM 调用输出工具决策 JSON，后端执行工具，再进行第二次最终回答生成；它不是原生 MCP 协议，也不是 OpenAI 原生 function calling。

---

## 目录

- [一、数据库访问层（HttpServer/utils/db）](#一数据库访问层)
- [二、连接池性能修复（DbConnectionPool）](#二连接池性能修复)
- [三、异步响应与线程池（HttpResponse / ThreadPool / HttpServer）](#三异步响应与线程池)
- [四、会话层线程安全（Session）](#四会话层线程安全)
- [五、LLM 接入网关（LLMGateway）🆕](#五llm-接入网关)
- [六、网关测试（tests）🆕](#六网关测试)
- [七、AI 模型接入层重构（AIStrategy / AIFactory）](#七ai-模型接入层重构)
- [八、AIHelper 对话核心改造](#八aihelper-对话核心改造)
- [九、ChatServer 主类与 main 装配](#九chatserver-主类与-main-装配)
- [十、请求处理器（Handlers）](#十请求处理器)
- [十一、本次流式/MCP/RAG 整合改造](#十一本次流式mcprag-整合改造)
- [十二、安全与工程化](#十二安全与工程化)
- [十三、沿用原项目、未实质改动的部分](#十三沿用原项目未实质改动的部分)
- [十四、深度追问与改进边界](#十四深度追问与改进边界)

---

## 一、数据库访问层

**文件**：`HttpServer/include/utils/db/DbConnection.h`、`HttpServer/src/utils/db/DbConnection.cpp` ♻️

### 1.1 预编译语句返回值的悬垂指针 + 服务端句柄泄漏

**改动前（原项目）**：

```cpp
template<typename... Args>
sql::ResultSet* executeQuery(const std::string& sql, Args&&... args) {
    std::lock_guard<std::mutex> lock(mutex_);
    std::unique_ptr<sql::PreparedStatement> stmt(conn_->prepareStatement(sql));
    bindParams(stmt.get(), 1, std::forward<Args>(args)...);
    return stmt->executeQuery();   // ← stmt 在函数返回时析构
}
```

**存在两个缺陷**：

1. **悬垂 ResultSet（use-after-free）**：MySQL Connector/C++ 中 `ResultSet` 的生命周期**绑定在创建它的 `PreparedStatement` 上**。函数返回时局部 `stmt` 被析构，返回的 `ResultSet*` 立即失效，调用方遍历结果即未定义行为。
2. **服务端预编译句柄泄漏**：`std::unique_ptr` 默认析构只调用 `delete`，**不会调用 `stmt->close()`**。Connector/C++ 必须 `close()` 才能释放 **MySQL 服务端**的 prepared statement 句柄。连接是池化、长生命周期的，句柄只增不减，最终触发 `max_prepared_stmt_count`（默认 16382）上限，报 *"Can't create more than max_prepared_stmt_count statements"*，服务不可用。

**改动后**：

```cpp
// 自定义 deleter：析构时先 close() 释放服务端资源，再 delete
struct StmtDeleter {
    void operator()(sql::PreparedStatement* stmt) const {
        if (stmt) { stmt->close(); delete stmt; }
    }
};
using StmtPtr = std::unique_ptr<sql::PreparedStatement, StmtDeleter>;

// 结果持有者：同时持有 stmt 与 ResultSet，保证结果集存活期间 stmt 不被释放
class DbQueryResult {
public:
    DbQueryResult(StmtPtr stmt, sql::ResultSet* rs) : stmt_(std::move(stmt)), rs_(rs) {}
    ~DbQueryResult() { delete rs_; }
    DbQueryResult(const DbQueryResult&) = delete;            // 禁拷贝
    DbQueryResult(DbQueryResult&&) noexcept;                 // 允许移动
    sql::ResultSet* operator->() const { return rs_; }
    explicit operator bool() const { return rs_ != nullptr; }
private:
    StmtPtr stmt_;            // 持有 PreparedStatement，析构时自动 close
    sql::ResultSet* rs_;     // 生命周期依赖 stmt_
};

template<typename... Args>
DbQueryResult executeQuery(const std::string& sql, Args&&... args) {
    std::lock_guard<std::mutex> lock(mutex_);
    StmtPtr stmt(conn_->prepareStatement(sql));
    bindParams(stmt.get(), 1, std::forward<Args>(args)...);
    sql::ResultSet* result = stmt->executeQuery();
    return DbQueryResult(std::move(stmt), result);   // stmt 所有权转移给结果对象
}
```

`executeUpdate` 同样改用 `StmtPtr`，函数结束时自动 `close()` 释放服务端资源。

**为什么**：用 RAII 把"相互依赖的两个资源（stmt 与 ResultSet）"捆绑成一个对象，让它们的生命周期一致；用自定义 deleter 区分"客户端内存释放（delete）"与"服务端资源释放（close）"。

**效果**：
- 彻底消除悬垂指针导致的崩溃风险；
- 杜绝服务端预编译句柄泄漏，长跑服务不再因句柄耗尽而挂掉；
- 调用方按 `auto res = executeQuery(...); res->next();` 使用，结果对象析构时自动回收全部资源。

### 1.2 深层机制：这不是“补一个析构函数”这么简单

这次修复的核心是把 **资源依赖关系** 显式编码到类型里。原实现的问题不是单纯忘记 `delete`，而是 `PreparedStatement`、`ResultSet` 和 MySQL 服务端句柄之间存在三层生命周期：

| 资源 | 生命周期归属 | 如果处理不当 |
|---|---|---|
| `PreparedStatement` C++ 对象 | 客户端进程内存 | 局部变量析构后，依赖它的 `ResultSet` 可能悬垂 |
| MySQL 服务端 prepared statement 句柄 | MySQL 服务端连接上下文 | 只 `delete` 不 `close()` 会让服务端句柄继续占用 |
| `ResultSet` | 依赖创建它的 statement | statement 先释放时，遍历结果集可能未定义行为 |

所以 `DbQueryResult` 的意义不是“包装返回值”，而是把 `stmt` 和 `rs` 的依赖关系收敛为一个不可拷贝、可移动的结果对象。调用方拿到的是一个带所有权语义的对象，而不是一个裸结果集指针。

### 1.3 异常安全与移动语义

`DbQueryResult` 禁止拷贝，是为了避免两个结果对象同时认为自己拥有同一个 `ResultSet*`，导致二次释放。允许移动，是为了让函数可以返回结果对象，同时把所有权从临时对象转移给调用方。

移动构造/移动赋值里会把源对象的 `rs_` 置空，这个细节很关键：

```cpp
DbQueryResult(DbQueryResult&& other) noexcept
    : stmt_(std::move(other.stmt_)), rs_(other.rs_) {
    other.rs_ = nullptr;
}
```

这样旧对象析构时不会再释放已经转移出去的结果集。

### 1.4 当前实现边界

这次修复解决了资源生命周期和服务端句柄泄漏，但仍有几个边界需要清楚：

- `executeQuery()` 内部仍然以连接级 mutex 串行化同一条 MySQL 连接上的操作，这是合理的，因为单个连接本身不应该被多个线程同时执行 SQL。
- 参数绑定目前对通用类型使用 `std::to_string`，并单独特化 `std::string`；如果未来传入更复杂类型，比如时间、二进制、optional，需要继续扩展绑定逻辑。
- `StmtDeleter::operator()` 中 `stmt->close()` 默认不捕获异常。析构路径中抛异常在 C++ 中风险很高，生产级可以考虑在 deleter 内部吞掉并记录日志，避免析构期间异常传播。

---

## 二、连接池性能修复

**文件**：`HttpServer/include/utils/db/DbConnectionPool.h`、`HttpServer/src/utils/db/DbConnectionPool.cpp` ♻️

### 2.1 健康检查持锁执行慢 IO → 周期性 QPS 抖动

**改动前（原项目）**：健康检查线程**持锁**遍历并 `ping()` 全部连接。`ping()` 是一次到 MySQL 的网络往返（毫秒级），持锁期间所有业务线程阻塞在 `getConnection()`，造成周期性 QPS 抖动甚至雪崩。

**改动后**：改为"摘出—锁外探测—放回"三段式，并加时间戳过滤与采样上限：

```cpp
void DbConnectionPool::checkConnections() {
    while (!stop_) {
        // ...每 60s 一轮...
        size_t i = 0;
        while (i < connections_.size() && checked < maxCheckPerRound_ && !stop_) {
            ConnEntry entry;
            {
                std::lock_guard<std::mutex> lock(mutex_);
                auto idle = now - connections_[i].lastReturned;
                if (idle < idleThreshold_) { ++i; continue; }   // 热连接跳过
                // swap+pop 把待检连接摘出池（持锁，O(1)）
                entry = std::move(connections_[i]);
                if (i != connections_.size() - 1) connections_[i] = std::move(connections_.back());
                connections_.pop_back();
            }
            // 锁外 ping，不阻塞业务线程
            if (!entry.conn->ping()) { /* reconnect 或 spawnReplacement */ }
            {
                std::lock_guard<std::mutex> lock(mutex_);
                entry.lastReturned = std::chrono::steady_clock::now();
                connections_.push_back(std::move(entry));
                cv_.notify_one();
            }
            ++checked;
        }
    }
}
```

**为什么**：慢 IO（网络 ping）绝不能在持锁状态下执行；同时只检查空闲超过阈值的连接、每轮限量，避免一次扫全池。

**效果**：业务线程不再被健康检查阻塞，消除周期性延迟尖峰。

### 2.2 队列 FIFO → LIFO 热连接复用

**改动前**：先进先出，每个连接轮流使用。
**改动后**：用 `std::vector` 尾进尾出（LIFO 栈），`getConnection` 总是取栈顶最近归还的"热连接"：

```cpp
conn = std::move(connections_.back().conn);
connections_.pop_back();
```

**为什么/效果**：热连接持续被复用，冷连接逐渐空闲并被健康检查回收，降低连接因长期空闲被 MySQL `wait_timeout` 断开的概率。

### 2.3 获取连接由无限等待改为超时等待

**改动前**：

```cpp
while (connections_.empty()) {
    if (!initialized_) throw DbException("Connection pool not initialized");
    cv_.wait(lock);          // ← 连接耗尽时业务线程永久阻塞
}
```

**改动后**：

```cpp
while (connections_.empty()) {
    if (!initialized_) throw DbException("Connection pool not initialized");
    if (cv_.wait_for(lock, acquireTimeout_, [this]{ return !connections_.empty(); }))
        break;               // 谓词为真：有可用连接
    throw DbException("Timeout acquiring database connection from pool");  // 超时快速失败
}
```

（头文件新增成员 `std::chrono::milliseconds acquireTimeout_{5000};`）

**为什么**：连接全部被占用且迟迟不归还时，无限 `wait` 会让请求线程永久挂起、堆积。
**效果**：超时（默认 5s）后快速失败抛异常，避免线程无限堆积，把故障"快速暴露"而非"无声拖死"。

### 2.4 配套：QueryResult 自动归还连接

**文件**：`HttpServer/include/utils/MysqlUtil.h` ♻️

**改动前**：

```cpp
QueryResult(std::shared_ptr<db::DbConnection> conn, db::DbQueryResult result)
    : conn_(std::move(conn)), result_(std::move(result)) {}
// ...
std::shared_ptr<db::DbConnection> conn_;   // 持有连接，防止提前归还
```

**改动后**：

```cpp
using PooledConn = std::unique_ptr<db::DbConnection, db::PoolDeleter>;
QueryResult(PooledConn conn, db::DbQueryResult result)
    : conn_(std::move(conn)), result_(std::move(result)) {}
// ...
PooledConn conn_;   // 析构时通过 PoolDeleter 自动归还连接到池
```

**为什么/效果**：查询结果对象持有的连接由 `shared_ptr` 改为带 `PoolDeleter` 的 `unique_ptr`，**结果对象析构时连接自动归还连接池**，所有权语义清晰（独占），杜绝连接泄漏与提前归还导致 ResultSet 失效。

### 2.5 连接池重构的关键细节

#### 2.5.1 LIFO 的价值不只是“取热连接”

FIFO 会让池内连接轮流被使用，每个连接都看起来“最近活跃”。这会带来一个问题：低负载时也难以判断哪些连接是真正多余的冷连接。

LIFO 则不同：

- 最近归还的连接会被优先复用，命中热连接概率高；
- 一部分连接会长期沉在 `vector` 底部，成为明确的冷连接；
- 健康检查从底部扫描时，更容易优先处理真正空闲较久的连接；
- 后续如果实现连接池缩容，LIFO 的“连接收敛”特性也更容易识别可回收连接。

因此 LIFO 在这里不仅是性能优化，也是连接生命周期管理策略。

#### 2.5.2 为什么健康检查要“摘出、锁外 ping、放回”

`ping()` 是网络 I/O，耗时不可控。如果持锁 `ping()`，连接池 mutex 会从“保护容器一致性”变成“串行化所有业务线程和数据库网络往返”。

新的三段式把临界区缩短到只保护容器结构：

1. 持锁：用 `swap + pop_back` 摘出待检查连接；
2. 解锁：对连接执行 `ping()` / `reconnect()`；
3. 持锁：把连接放回池中并 `notify_one()`。

这里 `swap + pop_back` 的意义是避免在 `vector` 中间删除导致大量元素移动，同时保持取出操作接近 O(1)。

#### 2.5.3 `PoolDeleter` 的借还模型

`getConnection()` 返回的不是普通 `unique_ptr<DbConnection>`，而是带 `PoolDeleter` 的 `unique_ptr<DbConnection, PoolDeleter>`。当业务侧持有的连接对象析构时，不会直接 `delete conn`，而是调用：

```cpp
void PoolDeleter::operator()(DbConnection* conn) const {
    pool->returnConnection(conn);
}
```

这相当于把“连接归还”绑定到指针析构路径上，避免业务层忘记归还连接。真正释放连接只发生在连接池析构阶段，此时会 `release()` 绕过 deleter，再手动 `delete` 裸指针。

#### 2.5.4 故障路径：坏连接如何处理

`getConnection()` 从池里取出连接后，会先 `ping()`。如果连接失效：

- 尝试 `reconnect()`；
- `reconnect()` 失败则 `spawnReplacement()` 创建新连接；
- 如果整个过程抛异常，会把连接重新放回池中并继续向上抛。

这保证了连接池不会因为一次取连接失败就永久丢失池容量。

#### 2.5.5 当前边界

当前连接池仍然是固定容量池，并没有实现动态扩缩容。LIFO 和时间戳为缩容提供了条件，但本次改动主要目标是：

- 消除健康检查持锁慢 I/O；
- 修复连接借还所有权语义；
- 让连接耗尽快速失败；
- 降低冷连接被业务线程频繁取出的概率。

如果继续演进，可以增加最小连接数、最大连接数、空闲回收和连接创建限速，避免数据库异常时大量线程同时创建新连接。

---

## 三、异步响应与线程池

### 3.1 通用线程池 🆕

**文件**：`HttpServer/include/utils/ThreadPool.h`、`HttpServer/src/utils/ThreadPool.cpp`（全新）

- 基于 `std::packaged_task` + `std::future` 的任务提交，`enqueue` 返回 future；
- `std::condition_variable` 调度，析构时 `stop_=true` + `notify_all` + join，优雅退出；
- 停止后再 `enqueue` 抛 `std::runtime_error`。

```cpp
template<class F>
auto ThreadPool::enqueue(F&& f) -> std::future<typename std::invoke_result_t<F>> {
    using return_type = typename std::invoke_result_t<F>;
    auto task = std::make_shared<std::packaged_task<return_type()>>(std::forward<F>(f));
    std::future<return_type> res = task->get_future();
    { std::lock_guard<std::mutex> lock(mutex_);
      if (stop_) throw std::runtime_error("ThreadPool is stopped");
      tasks_.emplace([task]{ (*task)(); }); }
    cv_.notify_one();
    return res;
}
```

**为什么/效果**：为"把阻塞的 LLM 调用从 I/O 线程卸载出去"提供通用执行器。

### 3.2 HttpResponse 异步响应支持 ♻️

**文件**：`HttpServer/include/http/HttpResponse.h`、`HttpServer/src/http/HttpResponse.cpp`

新增（原项目无）：

```cpp
// 异步响应：保存连接 + 标记延迟发送
void setConnection(const muduo::net::TcpConnectionPtr& conn) { conn_ = conn; }
muduo::net::TcpConnectionPtr getConnection() const { return conn_; }
void setDeferred() { deferred_ = true; }
bool isDeferred() const { return deferred_; }

// I/O 线程发送响应的静态便捷方法
static void sendJsonResponse(const TcpConnectionPtr&, const std::string& body, ...);
// SSE 流式辅助方法
static void sendSSEHeaders/ sendSSEChunk / sendSSEError / sendSSEEnd(const TcpConnectionPtr&, ...);
```

其中 `conn_` 为 `muduo::net::TcpConnectionPtr`（即 `shared_ptr<TcpConnection>`）。

**为什么**：HTTP 框架原本是"同步返回响应"模型，无法承载秒级阻塞的 LLM 调用。
**效果**：Handler 可标记 `deferred`，先不发响应，等业务线程算完再异步发；SSE 辅助方法支撑逐 token 流式输出。

### 3.3 HttpServer 接入 deferred 短路 ♻️

**文件**：`HttpServer/src/http/HttpServer.cpp`

```cpp
HttpResponse response(close);
response.setConnection(conn);          // 新增：把连接交给 response
httpCallback_(req, &response);

if (response.isDeferred())             // 新增：异步处理则不在当前栈帧发送
    return;
// ...否则照常 appendToBuffer + send
```

**为什么/效果**：让框架识别"异步响应"，配合线程池实现业务逻辑与网络 I/O 解耦——单个慢请求不再阻塞 Reactor 的 I/O 线程。

> **完整异步链路**（见第十节 ChatSendHandler）：I/O 线程 `setDeferred()` → 业务线程池执行 LLM 调用 → chunk/end 通过 `conn->getLoop()->runInLoop()` 投递回连接所属 I/O 线程发送；`conn` 为 shared_ptr 值捕获，延长连接生命周期，规避客户端提前断开的 use-after-free。当前实现中 SSE header 在业务线程内直接发送，生产级可进一步统一投递回 I/O 线程，收敛跨线程发送边界。

### 3.4 异步响应的几个关键边界

#### 3.4.1 deferred 解决的是“响应时机”，不是“业务并发控制”

`deferred` 的作用是告诉 HTTP 框架：这个请求暂时不要在 `onRequest()` 当前栈帧里发送响应。它本身不负责限流、取消、超时，也不负责保证业务线程池不被打满。

所以完整链路需要同时依赖：

- handler 捕获必要上下文，避免引用栈上对象；
- 业务线程池承载慢调用；
- 网关层做超时、限流、熔断；
- 回到 event loop 发送最终响应或 SSE chunk。

#### 3.4.2 `TcpConnectionPtr` 值捕获的取舍

当前 handler 捕获 `conn` 的 `shared_ptr`，这样即使客户端中途断开，回调执行时连接对象也不会被提前释放，避免 use-after-free。

代价是：长耗时任务会延长连接对象生命周期。生产级更稳妥的处理可以是捕获 `weak_ptr`，回到 I/O 线程后 `lock()` 判断连接是否仍然有效，再决定是否发送。

#### 3.4.3 业务线程池的剩余风险

线程池把慢 LLM 调用从 I/O 线程挪走，但不代表系统不会被慢请求拖垮。如果请求量超过线程池处理能力，任务队列仍会积压。当前项目主要通过网关限流和连接池超时来降低风险；更完整的生产实现还可以增加：

- 线程池队列长度上限；
- 请求排队超时；
- 客户端断开后的任务取消；
- SSE 流式发送失败后的上游调用中断。

---

## 四、会话层线程安全

**文件**：`SessionManager.h/.cpp`、`SessionStorage.h/.cpp` 🔒

### 4.1 会话 ID 生成器加锁

```cpp
// SessionManager 新增成员 std::mutex rngMutex_;
std::string SessionManager::generateSessionId() {
    std::lock_guard<std::mutex> lock(rngMutex_);   // 新增
    // ... 使用 std::mt19937 rng_ 生成
}
```

**为什么**：`std::mt19937` 非线程安全，多线程并发生成会话 ID 会数据竞争。

### 4.2 内存会话存储加锁

```cpp
// MemorySessionStorage 新增成员 mutable std::mutex mutex_;
void save(...)   { std::lock_guard<std::mutex> lock(mutex_); sessions_[...] = session; }
load(...)        { std::lock_guard<std::mutex> lock(mutex_); ... }
void remove(...) { std::lock_guard<std::mutex> lock(mutex_); sessions_.erase(...); }
```

**为什么/效果**：`std::unordered_map` 并发读写会破坏内部结构。加锁后 save/load/remove 线程安全，多 I/O 线程并发访问会话存储不再有数据竞争。

### 4.3 线程安全修复的粒度与边界

这次修复选择的是最直接的互斥锁方案：先恢复正确性，再考虑细粒度优化。它的价值在于把原项目隐含的“单线程访问 session”假设打掉，让多 I/O 线程或业务线程并发访问时不再破坏内部状态。

但这个方案也有边界：

- `MemorySessionStorage` 当前是一把锁保护整个 `unordered_map`，高并发鉴权时可能成为热点；
- `load()` 里如果发现 session 过期，会在同一把锁下删除条目，这是正确但偏保守的做法；
- 如果未来 session 规模变大，可以考虑读写锁、分段锁，或者将 session 存储迁移到 Redis 等外部组件。

面试中如果被追问“为什么不用读写锁”，可以回答：当前优先修复崩溃和数据竞争；读写锁适合读多写少且临界区足够大的场景，本项目 session 存储操作很短，互斥锁实现简单、风险更低。

---

## 五、LLM 接入网关

**目录**：`AIApps/ChatServer/include/LLMGateway/`、`AIApps/ChatServer/src/LLMGateway/`（全新）🆕

整体调用链：**每用户限流 → 后端限流 → 熔断检查 → 带超时 curl → 失败上报熔断 → fallback 路由降级**。

### 5.1 GatewayTypes.h — 类型与异常

- 异常体系：`GatewayException` 基类，派生 `RateLimitException` / `CircuitOpenException` / `TimeoutException` / `BackendException`；
- `BackendConfig` / `GatewayResult` 等结构体；`nowMs()` 毫秒时间戳工具。

### 5.2 RateLimiter — 令牌桶两级限流

```cpp
class TokenBucket {
    void refill();                 // 按时间比例补充令牌：tokens += elapsed/1000 * rate
    bool tryConsume();             // 令牌≥1 则消费返回 true，否则 false
};
class RateLimiter {
    void checkUserAllowed(int userId);          // 每用户独立令牌桶
    void checkBackendAllowed(const std::string& backendId);  // 每后端独立令牌桶
};
```

- 支持突发（burstSize）；
- 用户级桶 `try_emplace` 懒创建；后端未显式配置则放行（不限流）；
- 不足时抛 `RateLimitException`。

**为什么/效果**：防止单用户刷爆后端、防止打爆下游模型 API 配额；两级粒度分别保护"公平性"与"后端容量"。

### 5.3 CircuitBreaker — 连续失败阈值 + 时间衰减窗口熔断器

```
CLOSED --连续失败次数>=failure_threshold--> OPEN
OPEN   --恢复超时后--> HALF_OPEN
HALF_OPEN --探活次数<half_open_max_calls--> 放行探活
HALF_OPEN --成功次数>=success_threshold--> CLOSED
          └--任意探活失败--> OPEN
```

- `CLOSED` 状态下维护 `consecutiveFailures_` 和 `lastFailureTimeMs_`。
- `reportFailure()`：如果距离上次失败已经超过 `failureResetTimeoutMs_`，先清空过期失败计数；然后累计失败次数，达到 `failureThreshold_` 后进入 `OPEN`。
- `reportSuccess()`：成功不会立即清空失败计数，只有距离最后一次失败超过 `failureResetTimeoutMs_` 后才清零，避免“失败-成功-失败-成功”交替模式绕过熔断。
- `OPEN` 状态下拒绝请求，超过 `recoveryTimeoutMs_` 后，下一次 `allowRequest()` 通过 CAS 尝试进入 `HALF_OPEN`。
- `HALF_OPEN` 状态下使用 `halfOpenCalls_.fetch_add(1)` 限制本轮探活尝试次数，超过 `halfOpenMaxCalls_` 的请求直接拒绝。
- `HALF_OPEN` 下任一失败立即回到 `OPEN`；成功次数达到 `successThreshold_` 后恢复 `CLOSED`，并清空失败计数。

**为什么改成这个策略**：固定窗口失败率更精细，但需要维护请求总数、失败数、窗口轮转、最小样本数和半开在途探活，代码和讲解成本都更高。当前项目的熔断对象是外部 LLM API，主要目标是快速隔离“持续失败或恢复不稳定”的后端；连续失败次数 + 时间衰减窗口已经能覆盖主要故障形态，而且和原子变量实现天然契合，结构更接近常见熔断器写法。

**效果**：实现更轻，热路径主要是原子读写和 `fetch_add`；成功不会立刻清空失败历史，避免偶发成功掩盖持续不稳定；半开阶段通过探活次数限制避免恢复期一次性放开全部请求。

### 5.4 LLMGateway — 网关主体（单例）

- `call()`（非流式）：用户限流 → `tryBackend(主后端)`（后端限流+熔断+HTTP）→ 失败按 `routing` 配置遍历 fallback 后端 → 全部耗尽抛 `BackendException`；
- `callStreaming()`（流式）：限流 + 熔断 + 流式 HTTP，流式不走 fallback（已吐出的 token 无法回收，主动放弃重试）；
- `executeHttpStreaming()`：手写 SSE 行缓冲解析（处理 `data:` 前缀、`\r\n` 边界、`[DONE]`、半行缓存）逐块回调；
- 未初始化/禁用时为直通模式（pass-through）。

### 5.5 ★ 区分"传输层成功"与"业务层失败"（健壮性修复）♻️

**问题**：`curl_easy_perform` 对 HTTP 400/401/429 仍返回 `CURLE_OK`，导致"后端欠费/鉴权失败/限流"被当成成功——网关日志显示 OK、熔断器不计失败、前端只看到空白页。

**改动**：在 `executeHttp` 与 `executeHttpStreaming` 中取出 HTTP 状态码并主动判错：

```cpp
long httpCode = 0;
curl_easy_getinfo(curl, CURLINFO_RESPONSE_CODE, &httpCode);
// ...
if (httpCode >= 400)
    throw BackendException("Backend HTTP " + std::to_string(httpCode) + ": " + extractBackendError(body));

// 流式额外处理：HTTP 200 但整个流无任何 data: 行、body 是错误 JSON
if (!ctx.sawData && !ctx.raw.empty())
    throw BackendException("Backend returned no stream data: " + extractBackendError(ctx.raw));
```

新增 `extractBackendError()` 从 `{"error":{"message":...,"code":...}}` / `{"message":...}` 提取可读错误信息。

**效果**：业务错误 → 抛 `BackendException` → 被 `tryBackend`/`callStreaming` 捕获 → `cb.reportFailure()` **正确计入熔断** + 触发 fallback（非流式）→ 错误信息透传到前端 SSE error 显示，而非静默空白。

### 5.6 GatewayConfig — 配置外置

`gateway_config.json` 外置 backends（url/key/model）、两级限流参数、熔断阈值、半开探活参数、超时、`routing` fallback 链；`loadFromFile()` 运行时加载。

熔断器配置中 `failure_threshold` 表示连续失败多少次后打开熔断，`failure_reset_timeout_ms` 表示失败计数的时间衰减窗口，`half_open_max_calls` 表示 HALF_OPEN 本轮最多允许多少次探活尝试，`success_threshold` 表示半开探活成功多少次后恢复 CLOSED。代码仍兼容读取旧字段，但当前配置文件使用新字段。

### 5.7 网关深度机制与边界

#### 5.7.1 用户级限流为什么只扣一次

`call()` 中用户级限流在进入主后端前执行一次，fallback 尝试不会重复扣用户令牌。原因是从用户视角看，这是一次业务请求；fallback 是服务端内部容错。如果每次 fallback 都扣用户令牌，后端越不稳定，用户越容易被额外限流，不符合公平性。

后端级限流则不同：每尝试一个实际后端，都要检查该后端自己的容量，因为它保护的是下游模型 API 的额度和并发承载。

#### 5.7.2 fallback 只替换模型字段，不重构请求格式

`tryBackend()` 会复制 payload，并且只在请求体包含 `model` 字段时替换 fallback 后端的模型名：

```cpp
json reqBody = payload;
if (!backend.modelName.empty() && reqBody.contains("model")) {
    reqBody["model"] = backend.modelName;
}
```

这说明当前 fallback 更适合同协议后端之间切换，例如 OpenAI-compatible 模型之间切换。RAG 这类请求体没有 `model` 字段，网关不会强行改写它。请求格式仍由上层 `AIStrategy` 决定，网关只负责治理，不理解模型业务语义。

#### 5.7.3 为什么流式调用不做 fallback

非流式调用失败时，前端还没收到任何内容，网关可以尝试 fallback 并返回最终完整响应。

流式调用不同：一旦 `onChunk` 已经把部分 token 推给前端，就无法撤回。此时切换另一个后端续写会造成语义断裂。因此 `callStreaming()` 当前只做限流、熔断、超时和错误上报，不做 fallback。

#### 5.7.4 为什么选择连续失败次数 + 时间衰减窗口

原来的固定窗口失败率可以回答“这一段时间内整体失败比例是否过高”，但实现会牵涉窗口轮转、请求总数、失败总数和最小样本数。对于当前项目的 LLM 后端治理来说，这个精度不是刚需。

当前实现更接近常见熔断器：失败次数达到阈值就 OPEN，但成功不会马上把失败计数清零。只有距离最后一次失败超过 `failureResetTimeoutMs_` 后，成功才会清空失败计数。这样可以防止后端处于“多数失败、偶尔成功”的状态时迟迟不熔断。

HALF_OPEN 限制的是“本轮探活尝试次数”，而不是严格的“同时在途请求数”。`allowRequest()` 进入 HALF_OPEN 后用 `halfOpenCalls_.fetch_add(1)` 计数，超过 `halfOpenMaxCalls_` 就拒绝。这个语义比在途计数简单，也更贴近当前项目需要表达的设计重点。

这里仍然保留了非流式网关的成功上报边界：`tryBackend()` 只负责拿到 HTTP body，`call()` 在 JSON 解析成功后才 `reportSuccess()`；如果 HTTP 2xx 但响应 JSON 不可解析，则 `reportFailure()` 并继续 fallback。这样熔断器统计的是业务调用结果，而不是单纯的传输层结果。

#### 5.7.5 当前策略的取舍

连续失败次数 + 时间衰减窗口比固定窗口失败率更简单，但它不能精确表达“窗口内失败率达到多少”。例如 10 次请求里 5 次失败但不连续时，它不一定会熔断。

这个取舍在当前项目里可以接受：模型 API 的典型故障往往表现为连续 401/429/5xx、超时或返回不可解析错误。相比更复杂的失败率窗口，当前策略更容易实现、测试和讲清楚。

#### 5.7.6 错误提取为什么重要

`extractBackendError()` 不只是为了日志好看。它能把 OpenAI/DashScope 常见错误体中的 `message/code` 提取出来，让上层 SSE error 和日志更可读。否则前端只会看到空白或泛化错误，定位“欠费、鉴权失败、参数错误、限流”会很困难。

---

## 六、网关测试

**文件**：`AIApps/ChatServer/tests/test_gateway.cpp`、`test_gateway.sh`（全新）🆕

**单元测试**（`test_gateway.cpp`）：
- TokenBucket：基础消费、延迟后令牌补充；
- RateLimiter：用户级、后端级、未配置后端放行、多用户独立桶、**10 线程并发访问**；
- CircuitBreaker：初始 CLOSED、连续失败达到阈值后 OPEN、成功不立即清空失败计数、失败计数超过时间窗口后衰减、**OPEN→HALF_OPEN→CLOSED 完整恢复**、HALF_OPEN 探活次数限制、HALF_OPEN 失败重熔断、多实例独立。

**集成测试**（`test_gateway.sh`）：登录拿 cookie → 调用 SSE 聊天接口 → 校验 `data:` 事件与 `[DONE]` 结束帧 → 校验历史接口 → 发送并发 burst 请求辅助观察限流日志；fallback 路由仍保留为手工验证步骤。

**为什么/效果**：限流器、熔断器是纯逻辑且涉及并发，单测能锁定行为正确性；并发用例验证锁的正确性。

### 6.1 测试覆盖的设计意图

网关测试不是简单“跑通接口”，而是把限流和熔断拆成纯逻辑组件验证：

- TokenBucket 测试消费和按时间补充，验证被动 refill 逻辑；
- RateLimiter 测试用户桶、后端桶、多用户隔离和未知后端放行；
- 并发测试用 10 线程同时访问，验证限流器 mutex 保护下不会出现 token 负数或数据竞争；
- CircuitBreaker 测试连续失败阈值、失败计数时间衰减、HALF_OPEN 恢复、探活次数限制和多实例隔离。

### 6.2 仍需补充的测试

当前测试已经覆盖核心治理逻辑，但还可以继续补：

- fallback 链路的自动化测试：主后端失败后是否按顺序尝试备用后端；
- HTTP 400/500 错误体提取测试：验证 `extractBackendError()` 的兼容性；
- 流式异常测试：HTTP 200 但没有 `data:` 行时是否正确抛错；
- 网关级 JSON 解析失败测试：HTTP 2xx 但响应体不可解析时，是否只记一次失败并继续 fallback。

---

## 七、AI 模型接入层重构

**文件**：`AIStrategy.h/.cpp`、`AIFactory.h/.cpp` ♻️

### 7.1 硬编码多策略类 → 配置驱动通用策略

**改动前（原项目）**：4 个硬编码策略类，编译期静态注册：

```cpp
class AliyunStrategy / DouBaoStrategy / AliyunRAGStrategy / AliyunMcpStrategy ...
static StrategyRegister<AliyunStrategy>  regAliyun("1");
static StrategyRegister<DouBaoStrategy>  regDoubao("2");
static StrategyRegister<AliyunRAGStrategy> regAliyunRag("3");
static StrategyRegister<AliyunMcpStrategy> regAliyunMcp("4");
```

每个模型的 url/model 名硬编码在 `.cpp` 里，新增模型需写新类、重新编译。`AIStrategy.cpp` 原有 178 行实现。

**改动后**：合并为通用策略 `GenericAIStrategy`（OpenAI 兼容）+ `AliyunRAGStrategy`，所有参数构造注入，`AIStrategy.cpp` 仅剩 1 行 include：

```cpp
class GenericAIStrategy : public AIStrategy {
    GenericAIStrategy(modelName, apiUrl, apiKey, int maxContext);   // 全部注入
    json buildRequest(...) const override;          // OpenAI 兼容
    std::string parseResponse(...) const override;
    std::string parseStreamChunk(...) const override;  // 新增：流式 delta.content 解析
    bool supportTools() const override { return true; }
    int getMaxContext() const override { return maxContext_; }  // 新增：上下文上限
};
```

基类新增虚接口：`parseStreamChunk()`、`supportTools()`、`getMaxContext()`（替代原 `bool isMCPModel`）。本次流式/MCP/RAG 整合中进一步新增 `enableStreaming(json& payload)` 与 `getExtraHeaders()`：前者把不同后端的流式参数下沉到策略层，后者允许 RAG 这类接口附加专用 HTTP header。

### 7.2 工厂支持运行时配置加载与别名

**文件**：`AIFactory.h/.cpp`，新增：

```cpp
void loadFromConfig(const std::string& configPath);   // 从 model_config.json 注册模型
std::string resolveAlias(const std::string& name) const;  // 别名 → 实际 ID
const std::string& getDefaultModel() const;

// create() 先解析别名再查找
std::shared_ptr<AIStrategy> create(const std::string& name) {
    std::string resolved = resolveAlias(name);
    auto it = creators.find(resolved);
    if (it == creators.end()) throw std::runtime_error("Unknown strategy: " + name);
    return it->second();
}
```

`loadFromConfig` 解析 `default_model` / `aliases` / `models[]`（id/name/api_url/api_key/type/max_context），按 `type` 注册为 `GenericAIStrategy` 或 `AliyunRAGStrategy`。

**为什么/效果**：从"编译期硬编码"变为"运行时配置驱动"，**新增模型只改一行 JSON**（已接入 Qwen / DeepSeek）；别名系统兼容前端旧的数字 ID。

### 7.3 策略层重构的深层意义

#### 7.3.1 策略层负责“协议差异”，网关负责“出口治理”

本项目把模型调用拆成两层：

| 层次 | 职责 | 不负责 |
|---|---|---|
| `AIStrategy` | 构造请求体、解析普通响应、解析流式 chunk、声明上下文窗口、提供额外 header | 限流、熔断、fallback |
| `LLMGateway` | 限流、熔断、超时、HTTP 状态码判断、fallback、SSE data 行拆分 | 判断业务字段、理解 RAG/tool 语义 |

这个边界很重要：网关不应该知道 RAG 的 `output.text` 或 OpenAI 的 `choices[0].delta.content`，否则每接一种模型都要改网关。

#### 7.3.2 当前工厂还不是完全开闭

`loadFromConfig()` 当前根据 `type` 判断创建 `AliyunRAGStrategy` 或 `GenericAIStrategy`：

```cpp
if (type == "rag") { ... } else { ... }
```

这已经把模型参数配置化，但如果未来出现第三种协议策略，仍需要修改工厂代码。更彻底的方案是策略注册表：每种策略注册自己的 `type -> creator`，工厂只查表创建。

#### 7.3.3 配置校验还可以更严格

当前配置读取假设 `models[]` 中必有 `id/name/api_url` 等字段。生产级可以增加 schema 校验：

- 缺少必填字段时启动失败；
- `default_model` 必须能解析到已注册模型；
- alias 不能指向不存在的模型；
- API key 不应为空，除非显式允许本地模型；
- RAG 模型必须带对应接口所需的额外参数。

这样可以避免“服务启动成功，请求时才发现配置错”的问题。

---

## 八、AIHelper 对话核心改造

**文件**：`AIHelper.h/.cpp` ♻️🔒

### 8.1 线程安全加固
- 新增 `mutable std::mutex mutex_` 保护 `messages` 与 `strategy`；
- 早期版本中 `chat()` 持锁调用内部实现 `chatInternal()`；本次流式/MCP/RAG 整合后，改为统一入口 `chat(..., bool enableTools, ChunkCallback onChunk)` 持锁完成普通流式、工具路由、工具执行与最终流式回答；`GetMessages()`/`restoreMessage()` 继续加锁。

### 8.2 全部请求改走网关
**改动前**：`executeCurl(payload)` 内部自行 `curl_easy_init` 直连模型 API。
**改动后**：删除裸 curl，改为委托网关：

```cpp
json AIHelper::executeCurl(const json& payload, int userId, const std::string& modelType) {
    return LLMGateway::instance().call(modelType, payload,
                                       strategy->getApiUrl(), strategy->getApiKey(), userId);
}
```

**效果**：所有模型调用统一经过限流/熔断/降级治理。

### 8.3 统一流式聊天入口 chat(..., enableTools, onChunk) ♻️

**改动前**：AIHelper 同时存在两条业务入口：

```cpp
std::string chat(...);        // 非流式，支持 MCP 两阶段工具调用
void chatStreaming(...);      // 流式，直接单次调用 LLM，不进入 MCP 工具逻辑
```

这导致 `stream` 开关同时承担“是否使用 SSE 返回”和“是否进入 MCP 工具编排”两个含义。用户想体验流式时会绕过 MCP；想使用 MCP 时又必须关闭流式。

**改动后**：删除外部可见的 `chatStreaming()` 分支，统一为一个 SSE 入口：

```cpp
void AIHelper::chat(int userId,
                    std::string userName,
                    std::string sessionId,
                    std::string userQuestion,
                    std::string modelType,
                    bool enableTools,
                    ChunkCallback onChunk);
```

内部拆出三个辅助方法：

```cpp
void prepareStrategy(const std::string& modelType);
AIToolCall routeToolCall(int userId, const std::string& userQuestion,
                         const std::string& modelType, AIConfig& config);
void completeStreaming(int userId, const std::string& modelType,
                       ChunkCallback onChunk, std::string& fullResult);
```

统一入口的编排逻辑为：

```text
enableTools=false 或 strategy->supportTools()==false
    -> 添加用户消息
    -> completeStreaming() 单次 LLM 流式回答
    -> 落库完整 AI 回复

enableTools=true 且模型支持工具
    -> routeToolCall() 第一次非流式 LLM 工具路由
    -> 如需工具则 AIToolRegistry::invoke()
    -> 构造最终回答 prompt
    -> completeStreaming() 第二次 LLM 流式最终回答
    -> 落库原始用户问题与最终 AI 回复
```

**为什么**：让“响应形态”与“是否启用工具”解耦。SSE 成为默认输出通道，`enableTools` 专门控制 MCP 工具编排。

**效果**：
- 普通聊天在关闭工具时保持单次真实流式调用；
- MCP 命中工具后，第二次最终回答也能流式输出；
- 前端不再需要维护 JSON 响应与 SSE 响应两套逻辑；
- `messages` 中只落库原始用户问题和最终 AI 答案，路由 prompt 与工具结果 prompt 都是临时消息。


### 8.4 上下文窗口裁剪 buildRequestWithContext（新增）
当历史消息超过 `maxContext*2+2` 时，**保留最早一轮 + 最近 N 轮**，并插入一条 system 消息告知模型"中间 X 轮被裁剪"：

```cpp
json AIHelper::buildRequestWithContext() {
    int maxContext = strategy->getMaxContext();
    int total = messages.size();
    if (maxContext <= 0 || total <= maxContext*2+2) return strategy->buildRequest(messages);
    // 保留 messages[0],[1] + 最近 recent 条，并插入裁剪提示 system 消息
    ...
}
```

**为什么/效果**：控制送入模型的 token 量、防止超长上下文超限，同时保留首轮意图与最近上下文。

### 8.5 工具调用两阶段加异常安全
两阶段推理中临时压入提示词消息，改动后用 try/catch 在异常时 `messages.pop_back()` 回滚，避免临时消息污染历史。

本次进一步把工具判断 prompt 从“可能直接回答用户”调整为“只输出 JSON 路由决策”，解析结构兼容新旧两种格式：

```json
{"need_tool": true, "tool": "get_time", "args": {}}
```

或：

```json
{"need_tool": false, "tool": "", "args": {}}
```

`AIToolCall` 新增 `rawResponse` 便于调试；`args` 默认初始化为 `json::object()`，避免缺省参数导致后续工具调用处理不稳定。

> 注：MCP 工具注册与执行本体仍沿用原项目的 `AIToolRegistry`，本项目主要改造的是 AIHelper 侧的工具路由、异常安全、流式最终回答与历史落库边界。

### 8.6 AIHelper 编排的深层边界

#### 8.6.1 为什么 `chat()` 持锁范围很大

当前 `AIHelper::chat()` 入口一开始就持有 `mutex_`，直到普通流式或两阶段工具调用完成。这能保证同一会话内的 `messages` 和 `strategy` 不会被并发请求打乱，避免上下文顺序错乱。

代价是：同一个会话内的并发聊天会被串行化。这个取舍是合理的，因为一个会话的上下文天然有顺序依赖；如果允许同会话多条消息同时进入模型，历史消息顺序和落库顺序都会变得难以定义。

#### 8.6.2 工具路由 prompt 为什么必须临时压入再弹出

工具路由需要把“工具选择提示词”放入上下文，让模型输出工具决策。但这个 prompt 不是用户真实消息，也不是最终 AI 回复，不能长期污染会话历史。

因此 `routeToolCall()` 使用：

```cpp
messages.push_back({routePrompt, 0});
try { ... messages.pop_back(); }
catch (...) { messages.pop_back(); throw; }
```

同理，最终回答 prompt 也只是为了让第二次 LLM 生成最终答复，完成后会弹出，最终只落库原始用户问题和最终 AI 答案。

#### 8.6.3 工具失败和路由失败如何降级

当前代码对两类失败做了不同处理：

- `config.json` 加载失败：直接退化为普通单次流式回答；
- 路由调用失败或工具执行失败：把错误文本作为 SSE chunk 返回并落库。

这保证了前端不会一直等待无结果，同时历史中也能看到失败原因。但更严格的产品设计也可以选择“不落库内部错误”，只在前端提示。

#### 8.6.4 当前不是原生 function calling

当前工具链路是 prompt-based：第一次 LLM 输出工具决策 JSON，后端解析并执行工具，再由第二次 LLM 生成最终回答。它没有使用 `assistant.tool_calls`、`tool` role、tool call id，也没有处理流式 tool arguments delta。

所以文档和简历中如果写“支持工具调用”更准确；如果写“MCP”需要说明这是项目内命名或类 MCP 的工具编排，而不是原生 MCP 协议。

---

## 九、ChatServer 主类与 main 装配

**文件**：`ChatServer.h/.cpp`、`main.cpp` ♻️

- **业务线程池**：新增 `std::unique_ptr<http::utils::ThreadPool> businessPool_`（8 工作线程），`getBusinessPool()` 暴露；`initialize()` 中创建。
- **多会话结构**：`chatInformation` 由 `map<userId, AIHelper>` 改为 `map<userId, map<sessionId, shared_ptr<AIHelper>>>`，支持一个用户多个会话；新增 `sessionsIdsMap` 跟踪会话列表。
- **移除单点登录**：删除 `onlineUsers_` / `mutexForOnlineUsers_`（详见第十节登录登出）。
- **连接池容量**：`MysqlUtil::init(..., 5)` → `100`，提升并发承载。
- **DbQueryResult 适配**：`initChatMessage()` 改用 `auto res = mysqlUtil_.executeQuery(sql)`（RAII 自动管理）。
  - 当前实现仍需注意：历史消息逐行恢复时会对每条消息执行 `sessionsIdsMap[user_id].push_back(session_id)`，同一会话多条历史消息可能导致会话列表重复；后续应改为仅在首次创建该 session 时 push，或在恢复完成后去重。
- **启动装配**（`main.cpp`）：
  ```cpp
  StrategyFactory::instance().loadFromConfig(".../model_config.json");   // 注册模型
  GatewayConfig gwConfig;
  if (gwConfig.loadFromFile(".../gateway_config.json")) LLMGateway::instance().init(gwConfig);
  ```
- 注册新路由：`/chat/delete-session`。

### 9.1 装配层状态管理的细节

#### 9.1.1 多会话结构为什么要从一层 map 变成两层 map

原来如果一个用户只对应一个 `AIHelper`，那么同一用户的不同聊天会话会共享同一份 `messages`。这会导致历史上下文混在一起。

改成：

```cpp
map<userId, map<sessionId, shared_ptr<AIHelper>>>
```

后，每个 session 都有自己的 `AIHelper` 和上下文，用户维度只负责管理会话集合。

#### 9.1.2 `sessionsIdsMap` 的重复风险

当前文档已经标出一个边界：历史消息恢复时，如果每条消息都 `push_back(session_id)`，同一会话有多条消息就可能出现重复 sessionId。

更稳妥的方式是：

- 用 `set` 暂存 sessionId，恢复完成后再转 vector；
- 或者插入前判断是否存在；
- 或者在数据库层查询 `distinct session_id`。

#### 9.1.3 连接池容量从 5 到 100 的含义

这不是单纯“越大越好”。容量变大能提高并发登录/历史查询时的可用连接数量，但也会增加 MySQL 侧连接资源占用。这个值应该和：

- 业务线程池大小；
- MySQL `max_connections`；
- 请求中数据库访问耗时；
- LLM 调用是否持有数据库连接；

一起评估。当前改为 100 是为了压测和并发场景下避免过小连接池成为明显瓶颈。

---

## 十、请求处理器

### 10.1 ChatSendHandler / ChatCreateAndSendHandler ♻️

早期改造先把 handler 从同步响应改为“按 `stream` 分支选择 JSON 或 SSE”的异步链路：

```cpp
if (stream) {
    sendSSEHeaders(conn);
    AIHelperPtr->chatStreaming(...);
    sendSSEEnd(conn);
} else {
    std::string ai = AIHelperPtr->chat(...);
    sendJsonResponse(conn, body);
}
```

本次流式/MCP/RAG 整合后，handler 进一步统一为 SSE：

```cpp
bool enableTools = j.value("enableTools", true);
http::HttpResponse::sendSSEHeaders(conn);
AIHelperPtr->chat(userId, username, sessionId, userQuestion, modelType, enableTools,
    [conn](const std::string& chunk) {
        json chunkResp;
        chunkResp["id"] = "chatcmpl-stream";
        chunkResp["object"] = "chat.completion.chunk";
        chunkResp["choices"] = json::array({{
            {"index", 0},
            {"delta", {{"content", chunk}}},
        }});
        conn->getLoop()->runInLoop([conn, chunkBody = chunkResp.dump()] {
            http::HttpResponse::sendSSEChunk(conn, chunkBody);
        });
    });
conn->getLoop()->runInLoop([conn] { http::HttpResponse::sendSSEEnd(conn); });
```

`ChatCreateAndSendHandler` 仍然先生成 `sessionId`，并把它作为第一个 SSE chunk 下发：

```json
{"sessionId":"..."}
```

**为什么**：前端只维护一种流式读取协议，后端通过 `enableTools` 决定是否进入 MCP 编排，而不是再用 `stream` 同时控制“响应形态”和“工具能力”。

**效果**：
- 新会话和旧会话发送消息都统一走 SSE；
- 普通模型、MCP 工具最终回答、RAG 流式输出都包装成一致的 OpenAI 风格 chunk；
- handler catch 块统一把异常发送为 SSE error；
- 旧的非流式 JSON 分支被移除，接口语义更简单。


### 10.2 ChatDeleteSessionHandler（新增）🆕
新增会话删除处理器与 `/chat/delete-session` 路由，完善会话生命周期（创建/列表/历史/删除）。

当前实现会同步删除 MySQL 中该用户该 session 的历史消息，并清理内存中的 `chatInformation` 与 `sessionsIdsMap`。需要补充的风险点：删除 SQL 目前仍直接拼接 `sessionId`，应和注册/登录链路一样改为参数化查询，避免恶意构造 sessionId 带来 SQL 注入风险。

### 10.3 ChatRegisterHandler — SQL 注入修复 ♻️
**改动前**（字符串拼接，存在注入）：

```cpp
std::string sql = "INSERT INTO users (username, password) VALUES ('" + username + "', '" + password + "')";
std::string sql2 = "SELECT id FROM users WHERE username = '" + username + "'";
```

**改动后**（参数化查询）：

```cpp
mysqlUtil_.executeUpdate("INSERT INTO users (username, password) VALUES (?, ?)", username, password);
auto result = mysqlUtil_.executeQuery("SELECT id FROM users WHERE username = ?", username);
```

`isUserExist()` 同样参数化；整体逻辑包进 try/catch。**效果**：消除注册链路 SQL 注入风险。

### 10.4 ChatLoginHandler / ChatLogoutHandler — 移除单点登录限制 ♻️
**改动前**：登录时若 `onlineUsers_[userId]` 已在线则返回 403；登出时 `onlineUsers_.erase(userId)`。
**改动后**：删除该限制，允许同一用户多端并发会话。

> 说明：这是"移除限制"，简历如提及应表述为"取消单点登录约束以支持多端并发会话"，而非"实现多点登录"。

### 10.5 Handler 层的异步与错误路径

#### 10.5.1 Handler 为什么只捕获值，不捕获请求引用

`ChatSendHandler` 在把任务投递到业务线程池前，会把 `userId/username/sessionId/userQuestion/modelType/enableTools` 等必要数据按值捕获。这是为了避免异步任务执行时，原始 `HttpRequest` 或栈上局部变量已经失效。

这是 C++ 异步编程里非常关键的生命周期边界：跨线程任务不能依赖当前栈帧继续存在。

#### 10.5.2 SSE 错误如何返回

业务线程中如果 `AIHelper::chat()` 抛出异常，handler 会包装成：

```json
{"error":{"message":"..."}}
```

并通过 `sendSSEError()` 返回给前端。这样即使流式链路中途失败，前端也能收到结构化错误，而不是连接静默断开。

#### 10.5.3 当前可继续修复的安全点

`ChatDeleteSessionHandler` 当前删除 SQL 仍然直接拼接 `sessionId`，文档已经标出风险。这个点应该优先改成参数化 SQL，因为 sessionId 虽然通常由服务端生成，但只要接口接收前端传入，就不应该信任。

---


## 十一、本次流式/MCP/RAG 整合改造

### 11.1 问题背景：`stream` 开关语义污染 ♻️

**改动前**：前端有一个“流式”开关，后端根据 `stream` 字段选择两条完全不同的业务路径：

```text
stream=false
    -> ChatSendHandler / ChatCreateAndSendHandler
    -> AIHelper::chat()
    -> chatInternal()
    -> supportTools() 判断
    -> MCP 工具路由 / 工具执行 / 第二次 LLM
    -> JSON 完整响应

stream=true
    -> ChatSendHandler / ChatCreateAndSendHandler
    -> AIHelper::chatStreaming()
    -> LLMGateway::callStreaming()
    -> executeHttpStreaming()
    -> 单次 LLM 流式响应
```

**问题**：`stream` 本来只应该表示“客户端是否希望流式接收响应”，但实际变成了“是否流式 + 是否进入 MCP 工具编排”的共同开关。结果是：

- 想要真实流式体验时，会绕过 MCP；
- 想要 MCP 工具能力时，必须放弃流式体验；
- 业务语义和接口字段含义不匹配。

**改动后**：废弃前端“流式开关”，改为“MCP工具”开关，请求字段使用 `enableTools`：

```json
{
  "question": "现在几点？",
  "modelType": "aliyun-qwen",
  "enableTools": true
}
```

SSE 成为默认响应形态，`enableTools` 只表达是否允许工具编排。

**效果**：接口语义重新对齐：响应怎么返回由服务端统一 SSE 负责，是否启用工具由 `enableTools` 负责。

### 11.2 前端 AI.html：流式开关 → MCP 工具开关 ♻️

**文件**：`AIApps/ChatServer/resource/AI.html`

**改动前**：前端通过 `streamToggle` 决定走 `sendStreaming()` 还是普通 `fetch()`：

```js
const stream = streamToggle.checked;
if (stream) {
    await sendStreaming('/chat/send', { question, modelType, sessionId, stream: true });
} else {
    const r = await fetch('/chat/send', { body: JSON.stringify({ question, modelType, sessionId }) });
}
```

**改动后**：删除普通 JSON 分支，统一调用 `sendStreaming()`；UI 开关改为 `toolToggle`，请求体传 `enableTools`：

```js
const enableTools = toolToggle.checked;
const content = await sendStreaming(
    '/chat/send',
    { question, modelType, sessionId: currentSessionId, enableTools }
);
```

新会话接口 `/chat/send-new-session` 同样统一走 SSE，并通过首帧 `sessionId` 建立本地会话。

**为什么**：前端不应该理解后端内部是否要两次 LLM 或是否要执行工具，只需要处理一个稳定的事件流协议。

**效果**：
- 前端删除 JSON/流式两套分支；
- UI 语义从“流式”改成“MCP工具”；
- 所有 AI 回复都在同一套流式渲染逻辑中显示。

### 11.3 AIConfig：工具路由 prompt 结构化 ♻️

**文件**：`AIApps/ChatServer/resource/config.json`、`AIApps/ChatServer/include/AIUtil/AIConfig.h`、`AIApps/ChatServer/src/AIUtil/AIConfig.cpp`

**改动前**：工具判断 prompt 允许模型在“不需要工具”时直接回答用户。这会让第一次 LLM 调用既承担“路由判断”，又承担“最终回答”，职责混杂。

**改动后**：prompt 明确要求模型只做工具路由，不回答用户问题：

```json
{"need_tool":true,"tool":"工具名","args":{"参数名":"参数值"}}
```

或：

```json
{"need_tool":false,"tool":"","args":{}}
```

`parseAIResponse()` 新增新格式解析，并兼容旧格式：

```cpp
if (j.contains("need_tool") && j["need_tool"].is_boolean()) {
    result.isToolCall = j["need_tool"].get<bool>();
    if (result.isToolCall) {
        result.toolName = j.value("tool", "");
        result.args = j.value("args", json::object());
    }
    return result;
}

if (j.contains("tool") && j["tool"].is_string() && !j["tool"].get<std::string>().empty()) {
    result.isToolCall = true;
    result.toolName = j["tool"].get<std::string>();
    ...
}
```

`AIToolCall` 新增：

```cpp
json args = json::object();
std::string rawResponse;
```

**为什么**：把第一次 LLM 调用收敛成“控制面”的工具路由，最终回答统一交给后续流式回答阶段。

**效果**：工具路由输出更稳定，解析失败时可降级为不使用工具；调试时可以通过 `rawResponse` 查看模型原始路由结果。

### 11.4 AIHelper：统一编排普通流式、MCP 与最终回答 ♻️

**文件**：`AIApps/ChatServer/include/AIUtil/AIHelper.h`、`AIApps/ChatServer/src/AIUtil/AIHelper.cpp`

**改动前**：外部有两个入口：

```cpp
std::string chat(...);        // 非流式，支持 MCP
void chatStreaming(...);      // 流式，不支持 MCP
```

**改动后**：统一为：

```cpp
void chat(..., bool enableTools, ChunkCallback onChunk);
```

新增私有方法：

```cpp
void prepareStrategy(const std::string& modelType);
AIToolCall routeToolCall(int userId, const std::string& userQuestion,
                         const std::string& modelType, AIConfig& config);
void completeStreaming(int userId, const std::string& modelType,
                       ChunkCallback onChunk, std::string& fullResult);
```

核心流程：

```text
关闭工具 / 模型不支持工具
    -> 直接 completeStreaming()

启用工具 / 模型支持工具
    -> routeToolCall() 非流式路由
    -> 如需工具则执行 AIToolRegistry::invoke()
    -> 构造 finalPrompt
    -> completeStreaming() 流式最终回答
```

**异常安全**：路由 prompt 和最终 prompt 都是临时压入 `messages`，异常时用 `pop_back()` 回滚，最终只落库原始用户问题与最终答案。

**效果**：
- 普通流式和 MCP 流式复用同一套最终回答发送逻辑；
- 工具失败时可以把错误作为 SSE chunk 返回并落库；
- AIHelper 的职责从“两个入口各自处理”变成“统一业务编排”。

### 11.5 AIStrategy：把流式协议差异下沉到策略层 ♻️

**文件**：`AIApps/ChatServer/include/AIUtil/AIStrategy.h`

**改动前**：AIHelper 直接写死：

```cpp
payload["stream"] = true;
```

这适合 OpenAI-compatible 接口，但不适合 DashScope RAG 应用接口。

**改动后**：基类新增：

```cpp
virtual void enableStreaming(json& payload) const {
    payload["stream"] = true;
}

virtual std::vector<std::pair<std::string, std::string>> getExtraHeaders() {
    return {};
}
```

`GenericAIStrategy` 使用默认实现；`AliyunRAGStrategy` 重写：

```cpp
std::vector<std::pair<std::string, std::string>> getExtraHeaders() override {
    return {{"X-DashScope-SSE", "enable"}};
}

void enableStreaming(json& payload) const override {
    payload["parameters"]["incremental_output"] = true;
}
```

**为什么**：不同模型后端的流式开启方式不同，应该由模型策略自己表达，而不是让 AIHelper 硬编码所有后端细节。

**效果**：OpenAI-compatible 与 DashScope RAG 共用 `completeStreaming()`，但各自设置正确的请求体和 header。

### 11.6 LLMGateway：流式请求支持额外 Header + 兼容 data:{...} ♻️

**文件**：`AIApps/ChatServer/include/LLMGateway/LLMGateway.h`、`AIApps/ChatServer/src/LLMGateway/LLMGateway.cpp`

**改动前**：`callStreaming()` / `executeHttpStreaming()` 只能发送通用 header：

```http
Authorization: Bearer <key>
Content-Type: application/json
```

SSE parser 只识别：

```text
data: {...}
```

也就是 `data:` 后必须有空格。

**改动后**：流式调用签名增加 `extraHeaders`：

```cpp
void callStreaming(...,
                   ChunkCallback onChunk,
                   const std::vector<std::pair<std::string, std::string>>& extraHeaders = {});
```

底层 curl 追加策略给出的 header：

```cpp
for (const auto& h : extraHeaders) {
    std::string headerLine = h.first + ": " + h.second;
    headers = curl_slist_append(headers, headerLine.c_str());
}
```

SSE parser 改为兼容 `data:` 后可选空格：

```cpp
if (line.rfind("data:", 0) == 0) {
    std::string data = line.substr(5);
    if (!data.empty() && data.front() == ' ') {
        data.erase(0, 1);
    }
    if (data == "[DONE]") continue;
    ctx->sawData = true;
    ctx->onChunk(data);
}
```

**为什么**：DashScope RAG 应用流式响应需要 `X-DashScope-SSE: enable`，且实际返回 `data:{...}`，旧 parser 会误判为“没有 stream data”。

**效果**：RAG SSE 可以被网关正确识别，再由 `AliyunRAGStrategy::parseStreamChunk()` 从 `output.text` 中提取文本，最终包装成前端统一的 `choices[0].delta.content`。

### 11.7 Handler 与前端统一 SSE 验证结果

本次改动后实际验证过以下链路：

| 场景 | 请求特征 | 结果 |
|---|---|---|
| 普通单次流式 | `aliyun-qwen + enableTools=false` | 多段 `choices[0].delta.content`，最后 `[DONE]` |
| MCP 开启但无需工具 | `aliyun-qwen + enableTools=true + 普通知识问题` | 先工具路由，再第二次 LLM 流式回答 |
| MCP 工具命中 | `aliyun-qwen + enableTools=true + 现在几点` | 路由命中 `get_time`，执行工具后流式最终回答 |
| RAG 流式 | `aliyun-rag + enableTools=false` | DashScope `output.text` 被转换为统一 OpenAI 风格 SSE chunk |

**取舍**：当前方案没有全量迁移原生 tool calling，而是在保留 prompt-MCP 的基础上做低成本演进。优点是改动范围可控，能快速把普通流式、MCP 最终回答流式、RAG 流式统一起来；缺点是 MCP 开启时即使无需工具，也会先做一次路由调用。后续如果时间允许，可以继续演进到 OpenAI-compatible 原生 `tools/tool_calls` 流式解析。

### 11.8 统一 SSE 的本质：解耦“响应形态”和“工具能力”

这次改造前，`stream` 字段同时控制两件事：

- 前端希望流式还是完整 JSON；
- 后端是否进入工具编排路径。

这是语义污染。统一 SSE 后，响应形态固定为 SSE，工具能力由 `enableTools` 单独表达。这样接口语义变成：

| 字段/协议 | 负责的语义 |
|---|---|
| SSE | 响应如何返回 |
| `enableTools` | 是否允许工具编排 |
| `modelType` | 使用哪个模型策略 |

这个拆分让前端不用维护两套响应解析逻辑，也让后端可以自由决定一次 LLM、两次 LLM、工具调用或 RAG，而不改变前端协议。

### 11.9 为什么第一次工具路由不能流式给前端

当前工具路由依赖第一次 LLM 输出完整 JSON，例如：

```json
{"need_tool":true,"tool":"get_time","args":{}}
```

在完整 JSON 出来之前，后端无法可靠判断：

- 是否需要工具；
- 工具名是什么；
- 参数是否完整；
- 这段输出是否应该展示给用户。

如果把第一次调用的 token 直接流给前端，就可能把“工具路由中间结果”暴露给用户。统一 SSE 方案选择只流式输出最终回答，是用一次额外 LLM 调用换取用户体验一致性。

### 11.10 RAG 适配为什么放在策略层而不是网关层

DashScope RAG 的差异包括：

- 请求体不是 OpenAI chat completions 格式；
- 流式开关是 `parameters.incremental_output = true`；
- 需要额外 header `X-DashScope-SSE: enable`；
- 文本增量在 `output.text`。

这些都属于“模型协议差异”，不属于网关治理。如果写进网关，网关就会同时承担治理和业务解析，后续接新模型会越来越臃肿。因此本次把差异放在 `AliyunRAGStrategy`，网关只负责拆 SSE `data:` 行和错误处理。

### 11.11 当前统一 SSE 的剩余边界

- 流式中途失败后，已经发送给前端的内容无法回滚；当前只能发送 SSE error。
- 工具开启但无需工具时仍然会多一次路由调用，有额外 token 和延迟。
- 当前不支持原生 tool call delta 增量解析。
- SSE header 当前在 handler 业务线程中发送，chunk/end 通过 `runInLoop()` 发送；生产级可以统一投递到 I/O 线程。
- 如果前端断开连接，上游 curl 调用是否立即中断仍需要更完整的取消机制。

---
## 十二、安全与工程化

- **密钥治理** 🔒：含真实 API Key 的 `gateway_config.json` / `model_config.json` 取消 git 跟踪，仅提交 `*.example.json` 模板；用 `git filter-repo` 重写全历史清除 3 个泄漏 key 并 force-push 覆盖远端。
- **.gitignore**：补充 `build-debug/`、`build-release/`、误提交的测试二进制、vim swap 文件规则。
- **压测/脚本**：`login.lua`（wrk 登录压测）、`create_users.sh`（批量注册测试用户）。
- **文档**：重写 `README.md`（基于原项目对比描述）；新增 `调试学习.md`（GDB 多线程调试实战）。
- **空目录清理**：移除未落地的 `include/RAG/hnswlib`、`src/RAG`、`src/test` 占位目录。

---

## 十三、沿用原项目、未实质改动的部分

以下为原项目（Kama-HTTPServer / CppAIService）已有能力，本项目**直接沿用、未做实质修改**（仅个别文件补充中文注释）。简历中应作为"平台能力"描述，不计入个人改动：

| 模块 | 说明 |
|---|---|
| **RabbitMQ 异步落库**（`MQManager`） | 与原版逐字节相同，沿用 |
| **MCP 工具机制**（`AIToolRegistry`、`AIConfig`） | 工具注册/调用本体为原版；本项目仅补注释、修乱码，并在 AIHelper 侧接入网关与流式 |
| **图像识别**（`ImageRecognizer`，ONNX + OpenCV） | 实现与原版相同，沿用 |
| **语音 ASR/TTS**（`AISpeechProcessor`） | 实现与原版相同，`.h` 仅参数换行排版 |
| **HTTP 框架本体** | 路由（Router）、SSL、中间件（Middleware/CORS）、Session 核心、HttpContext 解析状态机（仅补注释）等为原项目框架 |

仅做格式化/注释的文件：`DbException.h`（命名空间缩进）、`HttpContext.h/.cpp`、`AISessionIdGenerator.h`、`AIConfig.*`、`AIToolRegistry.cpp`（修复乱码注释）。

---

## 十四、深度追问与改进边界

这一节不是新增改动，而是把前面所有模块可以继续深入解释的点集中列出来，方便复盘和面试追问。

### 14.1 数据库与连接池

- 为什么 `ResultSet` 必须和 `PreparedStatement` 绑定生命周期？
- 为什么 `delete PreparedStatement` 不等于释放 MySQL 服务端 prepared statement 句柄？
- `unique_ptr + custom deleter` 和 `shared_ptr` 的语义差异是什么？
- LIFO 为什么有连接收敛效果？
- 健康检查为什么不能持锁做 `ping()`？
- 获取连接超时后，上层应该如何返回错误或降级？

### 14.2 网关治理

- 用户级限流和后端级限流为什么要分开？
- 为什么 fallback 不应该重复扣用户令牌？
- 为什么流式响应不做 fallback？
- 为什么从固定窗口失败率改成连续失败次数 + 时间衰减窗口？
- 为什么成功不应该立即清空失败计数？
- HALF_OPEN 为什么需要限制探活尝试次数？`half_open_max_calls` 和 `success_threshold` 分别控制什么？
- 为什么 HTTP 400/500 要被视为后端失败，而不能只看 `CURLE_OK`？

### 14.3 AI 编排与流式响应

- 为什么 `stream` 开关会造成业务语义污染？
- 为什么 `enableTools` 应该独立于 SSE 响应形态？
- 当前工具调用为什么不是原生 function calling / MCP？
- 为什么工具路由第一次调用不能直接流给前端？
- 为什么只落库原始用户问题和最终回答，不落库中间工具 prompt？
- RAG 协议差异为什么应该放在策略层，而不是网关层？

### 14.4 当前最值得继续完善的点

| 优先级 | 改进点 | 原因 |
|---|---|---|
| 高 | `ChatDeleteSessionHandler` 删除 SQL 参数化 | 当前仍有 SQL 注入风险边界 |
| 高 | SSE header 也统一投递到 I/O 线程 | 收敛跨线程发送行为 |
| 中 | 网关 fallback 增加自动化测试 | 当前更多是手工验证 |
| 中 | 线程池增加队列上限和排队超时 | 防止慢请求无限积压 |
| 低 | 策略工厂改为注册表机制 | 更彻底符合开闭原则 |
| 低 | 上下文裁剪改为 token 级或摘要记忆 | 提升长对话质量 |

---

> 本文档对应代码状态：清理与密钥重写后的 `main` 分支，叠加网关业务错误检测、连接池获取超时，以及本次统一 SSE / MCP 工具开关 / RAG 流式接入改造。
