# AI服务平台系统复盘：从压测暴露问题到 AI 服务链路统一

> 这份文档用于辅助复盘项目演进过程。它不是面试逐字稿，而是帮助自己重新理解：问题是怎么暴露的、为什么要改、具体怎么改、方案背后有什么取舍。
>
> 阅读方式：先看每一阶段的“问题”和“修改”，再看“思考深化”。后者更适合在面试追问时展开。

## 目录

1. [项目起点与改造动机](#1-项目起点与改造动机)
2. [压测阶段：先确认性能数据是否可信](#2-压测阶段先确认性能数据是否可信)
3. [数据库访问层：prepared statement 泄漏与 RAII](#3-数据库访问层prepared-statement-泄漏与-raii)
4. [连接池重构：热连接、所有权与超时失败](#4-连接池重构热连接所有权与超时失败)
5. [LLM 网关治理：限流、熔断与降级](#5-llm-网关治理限流熔断与降级)
6. [异步响应：避免 LLM 慢调用阻塞 I/O 线程](#6-异步响应避免-llm-慢调用阻塞-io-线程)
7. [模型接入层：从硬编码走向配置化](#7-模型接入层从硬编码走向配置化)
8. [上下文裁剪与 SSE 流式响应](#8-上下文裁剪与-sse-流式响应)
9. [工具调用、RAG 与统一 SSE 的取舍](#9-工具调用rag-与统一-sse-的取舍)
10. [零散改动与最终复盘框架](#10-零散改动与最终复盘框架)

---

## 1. 项目起点与改造动机

### 原项目复杂度

我最开始接触这个开源项目时，是希望把它看懂并作为简历项目。但项目本身并不小：

- 底层是基于 muduo 的 HTTP 框架，包含请求解析、响应构建、中间件洋葱模型、静态/动态路由、SSL、session、数据库封装等能力。
- 上层是一个 AI 推理服务助手，包含 AI 聊天、图像识别、多会话、多模型适配、工具调用、远程 RAG、语音链路、消息异步落库和容器化交付。

只读懂原项目并不能让我真正掌控它。我对另一个分布式缓存项目更有掌控感，所以决定用类似方式推进这个项目：先压测、记录数据、暴露问题，再围绕问题逐步改造。

### 改造主线

整个项目不是一开始就规划成完整架构，而是从实际问题一步步推出来：

1. 压测暴露 session 会话层线程安全问题。
2. login 压测暴露 prepared statement 服务端句柄泄漏。
3. 修复数据库资源后，继续重构连接池。
4. 接入多个模型后，引入网关治理。
5. 面对 LLM 慢调用，引入异步响应。
6. 为了扩展模型，重构策略和工厂。
7. 为了统一体验，最终整合 SSE、工具调用和 RAG。

---

## 2. 压测阶段：先确认性能数据是否可信

### 2.1 menu 压测触发崩溃

我最先用 wrk 对静态路由和正则路由做压测。简单压测 `menu` 页面时，服务端直接崩溃；换成单线程压测后又没有问题。

这个现象说明问题大概率不是业务逻辑本身，而是多线程访问共享状态导致的数据竞争。继续排查后发现，`menu` 虽然是静态页面，但访问前会经过 session 鉴权，因此仍然会进入会话层。

### 2.2 session 会话层线程安全

问题集中在两类共享状态：

- session id 生成器内部使用随机数状态，多线程并发访问不安全。
- 内存 session 存储基于 `unordered_map`，并发读写会破坏容器结构。

修复方式是对 session id 生成和 session 存储的 `save/load/remove` 加锁。

### 思考深化：加锁不是终点

加锁解决了正确性，但也引入了同步开销。这里还可以继续追问：

- 当前是一把锁保护整个 session 存储，未来高并发下会不会成为瓶颈？
- session 访问是读多还是写多？读写锁是否更合适？
- 是否可以按 session id 分段加锁，降低锁竞争？

这一阶段给我的启发是：压测首先是正确性验证工具，其次才是性能展示工具。单线程能跑、多线程崩，说明系统里仍然藏着单线程假设。

---

## 3. 数据库访问层：prepared statement 泄漏与 RAII

### 3.1 login 压测出现固定成功次数

修复 session 线程安全后，我继续压测 login 接口。为了让压测更真实，我先批量注册 1000 个账号，再用 wrk lua 脚本按登录流程执行压测。

一开始我只看到压测能出结果，但进一步分析后发现：只有固定数量的请求成功，数量接近 `16382`。这不能作为真实性能数据，因为后续失败请求没有完整走完业务链路。

服务器日志出现 MySQL 报错：

```text
Can't create more than max_prepared_stmt_count statements (current value: 16382)
```

### 3.2 根因：服务端 prepared statement 句柄泄漏

原代码每次数据库请求都会创建 `PreparedStatement`，但在连接归还连接池时没有显式 `close()`，导致 MySQL 服务端 prepared statement 句柄不断累积。

这里还有一个更深的问题：查询结果由裸 `sql::ResultSet*` 返回，而 `ResultSet` 的生命周期依赖创建它的 `PreparedStatement`。如果 statement 先析构，结果集就可能悬垂。

### 3.3 修改方案

我把查询结果和 statement 生命周期绑定起来：

- 为 `PreparedStatement` 定义自定义 deleter，析构时先 `close()` 再 `delete`。
- 引入 `DbQueryResult`，同时持有 `PreparedStatement` 和 `ResultSet`。
- `DbQueryResult` 禁止拷贝、允许移动，明确资源所有权。
- 结果集析构时先释放 `ResultSet`，再释放 `PreparedStatement`。

### 思考深化：这是 C++ 资源管理问题

这个问题最值得总结的不是 MySQL 的 `16382` 上限，而是 C++ 资源管理的典型陷阱：

- 客户端对象释放不等于服务端资源释放。
- 资源释放顺序不能依赖调用者记忆。
- 裸指针很难表达资源所有权和生命周期依赖。

这个修复可以上升为一个原则：数据库句柄、文件描述符、curl handle、连接池连接这类资源，都应该优先用 RAII 表达获取、持有和释放，而不是把 `close/delete` 分散在业务路径中。

---

## 4. 连接池重构：热连接、所有权与超时失败

prepared statement 泄漏修复后，login 压测请求能正常走完整链路。继续回看连接池实现，又暴露出几个结构性问题。

### 4.1 原连接池的问题

| 问题 | 影响 |
|---|---|
| FIFO 队列取连接 | 每次倾向取最冷连接，冷连接更可能失活 |
| 健康检查线程持锁 `ping()` | `ping()` 是慢 I/O，会阻塞业务线程拿连接 |
| 健康检查扰动 LIFO 顺序 | `swap + pop_back` 会把尾部热连接搬到冷区，后台探活会破坏冷热分层 |
| 连接使用 `shared_ptr` 表达 | 连接借出是独占语义，shared ownership 不匹配 |
| 获取连接无限等待 | 连接耗尽时线程永久堆积，故障无法快速暴露 |

### 4.2 修改方案

我做了四类重构：

1. **FIFO 改 LIFO**  
   用 `deque` 明确维护“头冷尾热”：业务取连接从尾部 `pop_back()`，归还连接 `push_back()`，优先复用最近归还的热连接。

2. **健康检查锁外执行，并保持冷热语义**  
   检查线程先从冷端保序摘出待检查连接，释放锁后再 `ping()`，检查完插回原冷区位置。连接条目维护 `lastReturned` 和 `lastChecked`：业务归还只更新前者，后台探活只更新后者。

3. **shared_ptr 改 unique_ptr**  
   使用带 `PoolDeleter` 的 `unique_ptr` 表达连接独占借用，析构时自动归还连接池。

4. **无限等待改超时等待**  
   获取连接超过一定时间仍失败就抛异常，避免线程无限堆积。

### 思考深化：LIFO 不只是“取热连接”

LIFO 的更深价值是连接收敛：

- LIFO 会让少数活跃连接反复被使用。
- 长期不用的连接会沉到 `deque` 头部，更容易被识别为空闲或多余连接。
- FIFO 会让所有连接轮流活跃，反而不利于判断哪些连接可以回收。

这里还有一个容易被忽略的点：健康检查不能为了 O(1) 摘取而使用 `swap + pop_back`。在 LIFO 连接池中，尾部恰好是最热连接；把尾部元素换到冷端，会直接破坏“头冷尾热”的顺序。后台 `ping()` 也不能更新业务归还时间，否则冷连接会被伪装成热连接。

所以更合理的模型是双时间戳：`lastReturned` 表示业务使用热度，`lastChecked` 表示后台探活节奏。两者分离后，健康检查既能避免重复探活同一个冷连接，也不会污染业务复用顺序。这个结构天然支持后续缩容：优先回收 `deque` 头部且 `lastReturned` 足够久远的连接。

### 思考深化：所有权语义要和实际使用匹配

连接池借出的连接在同一时刻只应该有一个业务调用方持有，因此 `unique_ptr` 比 `shared_ptr` 更准确。它减少了控制块分配和引用计数开销，也让“借出/归还”的生命周期更清楚。

### 思考深化：超时等待是取舍

超时等待避免了线程永久阻塞，但也引入了超时时间选择问题：

- 太短：可能误杀本来马上能拿到连接的请求。
- 太长：仍然会造成线程堆积。

所以它的意义不是让请求永远成功，而是把资源耗尽从“无声拖死”变成“可观测、可失败、可降级”。

---

## 5. LLM 网关治理：限流、熔断与降级

### 5.1 为什么需要网关层

原本模型调用更接近裸 curl。单模型演示时可以工作，但接入多个模型后会出现治理问题：

- 不知道哪个用户把哪个后端模型打爆。
- 不同模型后端没有独立容量控制。
- 后端异常时请求仍然继续打过去。
- 主模型失败后不能自动降级到备选模型。
- HTTP 400/500 可能被误判成 curl 成功。

因此我加入了一层 LLMGateway，把模型调用变成统一治理出口。

### 5.2 网关调用流程

整体链路是：

```text
每用户限流 -> 每后端限流 -> 熔断状态检查 -> 带超时 curl -> 失败上报熔断 -> fallback 降级
```

具体能力包括：

- **限流**：每用户和每后端维护令牌桶。
- **熔断**：连续失败次数 + 时间衰减窗口触发三态熔断。
- **降级**：主模型失败后按配置调用 fallback 模型。
- **配置化**：从配置文件读取后端、限流、熔断、fallback 策略。
- **错误分类**：区分传输层成功和业务层失败。

### 5.3 令牌桶限流

令牌桶没有使用独立补充线程，而是在请求到来时根据距离上次请求的时间和补充速率被动补充令牌。

这样做的好处是减少额外线程开销；代价是当前限流仍然是单机限流。如果多实例部署，每个实例都有自己的桶，全局额度会被放大。

### 5.4 熔断器演进

我最初考虑连续失败计数，但不能让一次成功立即清空失败记录。否则大部分请求失败、偶尔成功一次，就可能让熔断器迟迟打不开。

最终方案是连续失败次数 + 时间衰减窗口：

- CLOSED 状态累计连续失败次数。
- 成功不会立即清空失败计数。
- 距离最后一次失败超过 `failure_reset_timeout_ms` 后，成功才清空失败计数。
- 失败次数达到 `failure_threshold` 后进入 OPEN。
- 恢复时间后进入 HALF_OPEN 探活。
- HALF_OPEN 用 `half_open_max_calls` 限制本轮探活尝试次数。
- HALF_OPEN 连续成功达到 `success_threshold` 后恢复 CLOSED，失败则重新 OPEN。

### 思考深化：熔断器要避免两个误判

1. **偶发成功误判**  
   如果成功立即清空失败次数，后端在“多数失败、少数成功”的状态下可能迟迟不熔断。因此需要失败计数的时间衰减窗口，而不是成功立即归零。

2. **half-open 流量误判**  
   half-open 不应该一下子放开全部请求，否则下游没恢复时会被再次打爆。当前实现限制本轮探活尝试次数，成功达到阈值才恢复，任一失败立即回到 OPEN。

3. **慢性不稳定误判**  
   连续失败策略对“连续失败”敏感，但对“失败率很高但不连续”的情况不够敏感。更生产级的方案应该使用滑动时间窗口或环形桶统计请求成功率/失败率，并配合最小请求数，比如“最近 10 秒内请求数达到 50 且失败率超过 50% 才熔断”。这种策略更稳，但也会带来更多状态维护、并发更新和测试成本。

因此当前熔断器可以定位为轻量级本地熔断：它适合当前项目阶段，能覆盖 LLM API 连续 401/429/5xx、超时、不可解析响应等主要故障；如果要演进到生产级服务治理，下一步应考虑滑动窗口成功率、慢请求比例和更完整的观测指标。

### 5.5 curl 的 CURLE_OK 陷阱

`curl_easy_perform` 返回 `CURLE_OK` 只说明传输层完成，不代表业务成功。HTTP 400、401、429、500 都可能仍然是 `CURLE_OK`。

所以我加入了 HTTP 状态码判断：只要状态码大于等于 400，就让网关视为失败并上报熔断器。这个细节让熔断统计更接近真实后端健康度。

---

## 6. 异步响应：避免 LLM 慢调用阻塞 I/O 线程

### 6.1 原同步模型的问题

原项目中 chat 操作是同步阻塞的。用户发起 LLM 请求后，handler 要等待模型响应完成才能返回。

LLM 调用通常是秒级慢任务，如果直接阻塞 muduo I/O 线程，会影响同一 event loop 上其他连接的处理能力。

### 6.2 修改方案

我引入了业务线程池和 deferred response：

1. I/O 线程收到请求。
2. handler 将 response 标记为 `deferred`。
3. `HttpServer::onRequest()` 发现 deferred 后直接返回，不在当前栈帧发送响应。
4. handler 捕获必要上下文，将 LLM 调用投递到业务线程池。
5. 业务线程完成后，通过 `conn->getLoop()->runInLoop()` 切回原 I/O 线程发送响应。

### 思考深化：异步化的边界

异步化不只是“丢进线程池”：

- Muduo 是 one loop per thread，一个连接天然归属于某个 I/O 线程。
- 跨线程操作连接需要谨慎，最终发送应回到连接所属 event loop。
- `runInLoop()` 的底层不是“魔法切线程”，而是把任务挂到该连接所属 `EventLoop` 的待执行队列里，再通过 wakeup fd（典型实现是 `eventfd`）唤醒阻塞在 `epoll_wait()` 的原 I/O 线程；I/O 线程醒来后执行 pending functors，`conn->send()` 因而回到正确线程上下文。
- 业务线程执行期间客户端可能断开，回调里需要考虑连接生命周期。
- 当前代码通过捕获连接指针延长生命周期，更稳妥的生产级方案可以考虑 `weak_ptr` 和取消机制。

---

## 7. 模型接入层：从硬编码走向配置化

### 7.1 原问题

原代码中模型接入是硬编码的。新增、删除或替换模型时，需要改 C++ 代码、重新编译、重新部署。

这带来几个问题：

- 模型 URL、API key、请求格式和业务代码耦合。
- 新增模型可能引入已有逻辑风险。
- 后端模型异常时无法灵活切换配置。

### 7.2 修改方案

我区分了两类策略：

- **普通模型策略**：适配大多数 OpenAI-compatible 格式模型。
- **RAG 模型策略**：适配阿里云远程 RAG 应用 API。

工厂模式从 JSON 配置读取模型字段，根据模型类型创建对应策略。新增、替换或删除模型时，主要修改配置文件即可。

### 思考深化：配置化还不等于完全开闭

当前策略和工厂已经降低了模型替换成本，但如果工厂内部仍然通过 `if-else` 或 `switch` 判断类型，那么新增第三类策略时仍要改工厂代码。

更彻底的方式是注册机制：每个策略把自己的 type 和构造函数注册到工厂，工厂只负责查表创建对象。

---

## 8. 上下文裁剪与 SSE 流式响应

### 8.1 上下文裁剪

为了节省 token 消耗，也为了让 LLM 更聚焦用户需求，我加入了上下文轮次裁剪。

当前策略是：

- 保留最初的提示词或首轮信息。
- 保留最近固定轮次对话。
- 将裁剪后的上下文组装进 LLM 请求。

### 思考深化：裁剪策略的隐含假设

这种方案简单稳定，但隐含假设是“越近的对话越重要”。

在闲聊场景里通常成立，但在任务型对话里，用户最早给出的约束可能比最近几轮更重要。后续可以继续演进为：

- 基于 token 数量裁剪。
- 对历史对话做摘要记忆。
- 抽取关键事实长期保留。

### 8.2 SSE 流式响应改造

SSE 是我做过改动幅度最大的部分，覆盖了：

- HTTP 框架层：支持 SSE header、chunk、error、end。
- 网关层：新增 `callStreaming` 和 `executeHttpStreaming`。
- 策略层：增加流式 chunk 解析接口。
- 业务 handler：将模型输出逐 chunk 推送给前端。
- 前端：从完整响应显示改为流式渲染。

一开始我只是做了一个流式开关：开关打开走新流式路径，关闭仍走原始整段响应。后来发现这只是效果演示，无法匹配全链路改造成本，于是决定统一聊天响应为 SSE。

---

## 9. 工具调用、RAG 与统一 SSE 的取舍

### 9.1 统一 SSE 后暴露的新问题

如果所有聊天都走 SSE，那么普通模型、工具调用和 RAG 都要适配流式响应。

原来的问题是：

- 流式开关打开时，只走普通模型单次调用路径。
- 工具调用路径仍然是非流式。
- RAG 模型也没有进入统一流式路径。
- `stream` 字段同时承担“响应形态”和“是否进入工具能力”的含义。

### 9.2 工具调用不是原生 function calling，也不是 MCP

这里需要明确概念边界：当前项目中的工具调用更接近 **prompt-based tool use**。

它不是原生 function calling，也不是 MCP 协议。原因不只是“名字不同”，而是实现层次不同：当前项目第一次 LLM 调用返回的本质仍是一段普通文本，只是 prompt 强约束它输出工具决策 JSON；后端再把这段文本解析为“是否需要工具、工具名、参数”，必要时执行工具，最后再做第二次模型调用生成最终回答。

原生 function calling 则不同。它会在请求里显式声明 tools 和参数 schema，模型返回时通过结构化的 `tool_calls` / `function_call` 字段表达工具调用，工具结果也会通过专门的消息角色继续回传。换句话说，原生 function calling 把工具调用能力做进了 API 协议层，而当前项目的工具调用能力主要建立在 prompt 约束和业务层解析上。

这带来几个直接后果：

- 原生 function calling 更容易支持流式增量 tool call；
- 当前项目需要先让 LLM 完整返回一段文本；
- 后端再解析这段文本，判断是否需要工具、工具名和参数；
- 因此第一次 LLM 调用天然不适合直接向前端流式转发。

如果提前把第一次调用的 token 推给前端，用户可能看到的是工具路由中间结果、半截 JSON，甚至是模型偏离格式要求后的解释文本，而不是最终回答。


### 9.3 最终统一方案

最终方案是重新引入“是否使用工具”的开关，但不再使用“是否流式”的开关：

| 场景 | 调用路径 | 代价 |
|---|---|---|
| 不启用工具 | 单次 LLM 流式回答 | 延迟和 token 成本低 |
| 启用工具且命中工具 | 第一次非流式路由 -> 工具执行 -> 第二次流式最终回答 | 多一次 LLM 调用 |
| 启用工具但未命中工具 | 第一次非流式路由 -> 第二次流式最终回答 | 体验一致，但有额外成本 |
| RAG | RAG 策略构造请求并解析流式响应 | 需要适配特殊协议 |

### 9.4 为什么启用工具时强制两次调用

即使命中不到工具，也强制做第二次 LLM 调用，是为了保证前端始终只看到最终回答。

这是一个明确取舍：

- **不开工具**：优先低延迟、低 token 成本。
- **开工具**：优先响应形态一致性和最终回答体验。

消息落库也遵循这个边界：工具判断和工具结果属于执行轨迹，不作为用户可见历史保存；数据库只保存原始用户输入和最终 LLM 响应。

### 9.5 RAG 流式适配

RAG 模型需要单独适配：

- 请求格式不同。
- 流式开启参数不同。
- 响应解析路径不同。

所以我在 RAG 策略类中定义阿里云 RAG 所需的请求报文格式和流式响应解析逻辑，再由统一 SSE 链路输出给前端。

---

## 10. 零散改动与最终复盘框架

### 10.1 其他改动

除主线之外，还有一些补充改动：

- 增加会话记录删除功能。
- 移除单点登录限制。
- 增加网关单元测试。
- 修复注册链路 SQL 注入风险。

### 10.2 最终复盘框架

回看整个项目，我后续复盘每个改动时都会固定追问三个问题。

#### 问题一：我的方案引入了什么新代价？

示例：

- 加锁解决正确性，但引入锁竞争。
- 超时等待避免线程无限堆积，但需要选择合理超时时间。
- 统一 SSE 改善体验，但增加错误处理和断连处理复杂度。
- 工具调用保证最终回答体验，但增加 token 成本和延迟。

#### 问题二：这个具体问题能抽象成什么原则？

示例：

- prepared statement 泄漏 -> RAII 与资源生命周期管理。
- shared_ptr 误用 -> 所有权语义要匹配实际使用模式。
- 连续失败计数熔断缺陷 -> 状态判断要基于窗口和比例，而不是瞬时事件。
- 健康检查持锁 ping -> 慢 I/O 不应该放在临界区内。
- 健康检查扰动 LIFO -> 后台维护逻辑不能破坏业务路径依赖的冷热顺序。

#### 问题三：生产级或更大规模下是否仍成立？

示例：

- 当前连接池是单机连接池，分布式部署时需要重新考虑实例级资源管理。
- 当前令牌桶是单机限流，多实例下需要分布式限流。
- 当前熔断状态是单机状态，多实例下是否共享要看治理目标。
- 当前工具调用是 prompt-based，未来可以演进到原生 tools/tool_calls 协议。

### 10.3 一句话总结

这个项目的核心不是简单“加 AI 接口”，而是从压测暴露的问题出发，逐步把原本能演示的教学项目改造成一个更接近真实服务的 C++ AI 服务：数据库资源安全、连接池优化、异步 HTTP、网关治理、多模型配置、SSE 流式响应、工具调用和 RAG 接入都被纳入了统一思考。
