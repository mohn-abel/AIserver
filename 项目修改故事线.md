# 项目修改故事线：从教学 Demo 到可治理的 C++ AI 服务

> 这份文档用“故事线”的方式讲项目演进，适合复盘、面试讲解和 README 之外的技术阐述。
> 它不替代 `项目改动详解.md`，后者保留逐文件、逐模块的改动清单；本文重点讲每个阶段为什么发生、遇到了什么问题、最后如何解决。

---

## 0. 起点：一个能跑通的教学项目，但还不是稳定服务

最初的项目基于 Kama-HTTPServer / CppAIService。它已经具备一个教学项目该有的骨架：HTTP 路由、登录注册、聊天页面、MySQL 存储、RabbitMQ 异步落库、一些 AI 能力入口。

但我真正开始把它当成“可展示的后端项目”推进时，发现它和一个能长期运行的服务之间还有明显距离：

- 数据库资源生命周期不够严谨，短期演示看不出问题，长期运行会暴露句柄泄漏和悬垂指针。
- HTTP handler 是同步响应模型，不适合秒级 LLM 调用。
- 模型 API 直接在业务里 curl，没有限流、熔断、fallback 和统一错误处理。
- 模型接入方式硬编码，新增模型需要改代码。
- 流式响应、MCP 工具、RAG 三类 AI 能力各走各的路径，业务语义逐渐混乱。

所以整个项目的演进不是“堆功能”，而是先修地基，再做异步化，再接网关，最后统一 AI 业务编排。

---

## 1. 第一阶段：先修数据库地基

### 场景

聊天服务的所有关键数据都会落到 MySQL：用户、会话、历史消息。只要服务不是一次性跑完退出，数据库访问层的资源管理就必须可靠。

### 问题

原项目的查询接口返回裸 `sql::ResultSet*`。这个写法在演示中可能刚好能跑，但它隐藏了两个工程问题。

第一个问题是 `ResultSet` 的生命周期依赖 `PreparedStatement`。函数里局部的 `PreparedStatement` 析构后，返回出去的 `ResultSet*` 就可能变成悬垂指针。

第二个问题更隐蔽：`PreparedStatement` 释放时如果没有显式 `close()`，MySQL 服务端的 prepared statement 句柄不会及时释放。连接池里的连接是长生命周期的，句柄会不断累积，最终把 MySQL 的 prepared statement 上限打满。

### 方法

我把查询结果和 statement 绑定成一个 RAII 对象：

- `StmtDeleter` 负责析构时先 `close()` 再 `delete`。
- `DbQueryResult` 同时持有 `PreparedStatement` 和 `ResultSet`。
- 禁止拷贝，只允许移动，确保所有权清晰。

### 结果

数据库查询从“调用方自己小心使用裸指针”，变成“结果对象自己管理生命周期”。这一步没有增加业务功能，但它让后续所有历史消息读取、登录注册、会话恢复都站在更稳的地基上。

---

## 2. 第二阶段：连接池从“能用”改成“抗抖动”

### 场景

接入 LLM 后，请求耗时不再是普通 HTTP 的毫秒级。业务线程会等待模型返回，数据库连接也会被更频繁地竞争。连接池如果在某些周期性任务中持锁做慢操作，就会放大成用户可感知的延迟抖动。

### 问题

原连接池健康检查会持锁遍历连接并执行 `ping()`。`ping()` 是网络 I/O，持锁期间其他业务线程无法拿连接。

此外，获取连接时是无限等待。如果连接耗尽，线程会一直挂着，故障不会快速暴露，而是慢慢把线程池拖死。

### 方法

我做了三个调整：

1. 健康检查改成“摘出、锁外探测、放回”。锁只保护连接池容器，不包住网络 I/O。
2. 连接池从 FIFO 改成 LIFO，让最近使用过的热连接优先复用。
3. `getConnection()` 增加超时等待，默认 5 秒拿不到连接就抛异常。

同时，查询结果对象持有连接，结果析构时自动归还连接池，避免连接被提前归还或忘记归还。

### 结果

这一阶段解决的是服务稳定性和尾延迟问题。健康检查不再周期性卡住业务线程；连接耗尽时能快速失败；连接归还也不再依赖调用方记忆。

---

## 3. 第三阶段：让 HTTP 框架支持慢任务和流式输出

### 场景

LLM 调用天然慢。如果 handler 在 I/O 线程里直接等待模型返回，整个 Reactor 的响应能力都会被拖低。更进一步，用户希望边生成边看结果，HTTP 框架必须支持 SSE。

### 问题

原 HTTP 框架是同步响应模型：handler 填好 `HttpResponse`，函数返回后框架立刻发送。它没有办法表达“这个请求我要晚点再回复”，也没有工具函数支持持续发送 SSE chunk。

### 方法

我增加了通用线程池和 deferred response 机制：

- `ThreadPool` 承载阻塞的 LLM 调用。
- `HttpResponse` 保存 `TcpConnectionPtr`，并能标记 `deferred`。
- `HttpServer` 发现 response 被标记为 deferred 后，不在当前调用栈发送响应。
- 业务线程完成后，通过 `conn->getLoop()->runInLoop()` 切回 I/O 线程发送。
- `HttpResponse` 增加 SSE header、chunk、error、end 的发送方法。

### 结果

HTTP 框架从“同步短请求框架”升级为“能承载 LLM 慢任务和 SSE 流式输出的框架”。这一步是后面所有流式聊天、MCP 最终回答流式、RAG 流式的基础。

---

## 4. 第四阶段：补齐会话线程安全

### 场景

业务线程池引入后，会话模块不再只被 I/O 线程访问。登录、鉴权、聊天、历史查询都可能并发访问 session。

### 问题

两个点存在数据竞争：

- 会话 ID 生成器内部使用 `std::mt19937`，它不是线程安全的。
- 内存会话存储用 `unordered_map`，并发读写会破坏容器状态。

### 方法

- 给 session id 生成过程加锁。
- 给 `MemorySessionStorage` 的 `save/load/remove` 加锁。

### 结果

会话层不再依赖单线程假设，多线程 handler 可以安全共享 session 存储。

---

## 5. 第五阶段：建立 LLMGateway，把模型调用纳入治理

### 场景

当项目只接一个模型时，业务代码里直接 curl 看起来足够。但一旦接入多个模型、fallback、限流和流式输出，模型调用就需要一个统一出口。

### 问题

裸 curl 有几个典型问题：

- 不知道哪个用户打爆了请求。
- 不同后端没有独立容量控制。
- 后端持续失败时仍然被请求。
- 主后端失败后不能降级到备选模型。
- HTTP 400/401/429 可能被误判成 curl 成功。
- 流式响应的 SSE 解析需要处理半行、`data:`、`[DONE]` 和异常 JSON。

### 方法

我新增 `LLMGateway`：

- `RateLimiter` 做用户级和后端级令牌桶限流。
- `CircuitBreaker` 做三态熔断。
- `GatewayConfig` 把后端、超时、限流、熔断、fallback 路由放进 JSON。
- `call()` 支持非流式 fallback。
- `callStreaming()` 支持流式请求，但不做 fallback，因为已经吐出的 token 无法回滚。
- `executeHttp()` 和 `executeHttpStreaming()` 统一处理 curl、HTTP 状态码和错误提取。

我还修复了传输层和业务层成功的混淆：只要 HTTP 状态码大于等于 400，就主动抛出 `BackendException`，让熔断器和 fallback 能正确工作。

### 结果

模型调用变成一个可治理的出口：有超时、有错误分类、有熔断、有 fallback、有流式解析。业务层不用再关心 curl 细节，只关心“我要调用哪个模型”。

---

## 6. 第六阶段：模型接入从硬编码走向配置驱动

### 场景

项目需要接入 Qwen、DeepSeek、DashScope RAG，后续还可能扩展更多模型。如果每个模型都写一个 C++ 类并硬编码 URL/API key，维护成本会越来越高。

### 问题

原项目的策略类是静态注册的，模型配置散在代码里。新增模型意味着改代码、重新编译。MCP 能力、上下文窗口、响应解析也没有统一的能力表达。

### 方法

我把策略层收敛成：

- `GenericAIStrategy`：OpenAI-compatible 模型统一走它。
- `AliyunRAGStrategy`：DashScope RAG 应用接口单独适配。

`AIFactory` 从 `model_config.json` 加载模型配置，按 `type` 创建 strategy。

基类逐步增加能力接口：

- `parseResponse()` 解析普通响应。
- `parseStreamChunk()` 解析流式 chunk。
- `supportTools()` 表达工具支持能力。
- `getMaxContext()` 控制上下文窗口。
- `enableStreaming()` 设置策略自己的流式参数。
- `getExtraHeaders()` 提供策略自己的 HTTP header。

### 结果

新增模型主要改配置文件。业务层只传 `modelType`，策略层负责请求格式、响应解析、流式参数和额外 header。

---

## 7. 第七阶段：AIHelper 成为聊天业务编排核心

### 场景

AIHelper 持有一个会话的历史消息，是聊天业务最核心的类。它需要处理上下文、模型调用、工具调用、流式输出和落库。

### 问题

最初 AIHelper 的职责逐渐分裂：

- 普通 `chat()` 走非流式，支持 MCP。
- `chatStreaming()` 走流式，但不支持 MCP。
- 工具 prompt 临时压入历史，异常时可能污染上下文。
- 长上下文没有裁剪。
- 模型请求没有统一走网关。

### 方法

第一步，我先给 AIHelper 加锁，保护 `messages` 和 `strategy`。然后把模型调用全部改成走 `LLMGateway`。接着加入上下文裁剪：保留最早一轮和最近 N 轮，中间被裁剪时插入 system 提示。

在 MCP 工具链路中，我把临时 prompt 的 push/pop 包在 try/catch 里，保证异常时回滚。

最后，在本次统一 SSE 改造里，我把外部入口收敛为：

```cpp
void chat(..., bool enableTools, ChunkCallback onChunk);
```

它内部根据 `enableTools` 和 `strategy->supportTools()` 决定直接流式回答，还是先做工具路由，再执行工具，最后流式输出最终回答。

### 结果

AIHelper 从多个分裂入口，变成一个统一的聊天编排器。普通模型、MCP、RAG 都能进入同一套最终输出协议。

---

## 8. 第八阶段：第一次发现 stream 与 MCP 语义冲突

### 场景

流式响应做好后，前端有一个“流式”开关。用户打开它，能看到 token 一段段出现；关闭它，则等待完整 JSON。看起来很自然。

但当 MCP 工具加入后，这个开关暴露了语义问题。

### 问题

后端实际链路是：

```text
stream=true
    -> 直接 chatStreaming()
    -> 单次 LLM 流式
    -> 不进入工具判断

stream=false
    -> chatInternal()
    -> 支持工具路由、工具执行、第二次 LLM
    -> 完整 JSON 返回
```

也就是说，`stream` 不只控制响应形态，还间接控制是否启用 MCP。用户想要工具，就要牺牲流式；用户想要流式，就要放弃工具。

### 方法选择

我讨论过三个方案：

1. 完整方案 B：只要启用工具能力，所有请求都先路由，再第二次生成最终回答。这样最终回答都能流式，但普通问题也会增加一次 LLM 调用。
2. 原生 tool calling：第一次流式请求直接带 tools，模型需要工具时输出 tool_calls，不需要工具时直接流式回答。这是业内更优方案，但要改消息结构、tool call delta 解析和多步状态机。
3. 低成本机会主义流式：保留 prompt-MCP，不全量迁移原生 tool calling；对外统一 SSE，工具能力用 `enableTools` 单独控制。

考虑当前时间成本和校招 ROI，我选择第三种。

### 结果

这一步明确了项目的产品语义：

- SSE 是默认响应通道。
- MCP 是一个可开关的编排能力。
- 不在短期内强行迁移原生 tool calling。

---

## 9. 第九阶段：统一 SSE 与 enableTools

### 场景

确定方案后，需要改前端、handler 和 AIHelper，让它们都围绕新的语义工作。

### 方法

前端：

- 删除“流式”开关。
- 新增“MCP工具”开关。
- 所有消息发送都调用 `sendStreaming()`。
- 请求体携带 `enableTools`。

handler：

- 删除 JSON 响应分支。
- 新旧会话发送都统一 SSE。
- 新会话首帧发送 `sessionId`。
- 内容统一包装成 OpenAI 风格 `choices[0].delta.content`。

AIHelper：

- 删除外部 `chatStreaming()` 入口。
- 新增 `routeToolCall()` 和 `completeStreaming()`。
- 关闭工具时直接单次流式。
- 开启工具时先非流式路由，再流式最终回答。

### 结果

普通流式、MCP 路由、工具执行、最终回答都被统一到一个 SSE 通道里。前端不再知道后端内部到底是一次 LLM、两次 LLM 还是 RAG，只负责消费事件流。

---

## 10. 第十阶段：RAG 流式接入遇到协议细节问题

### 场景

统一 SSE 后，普通 OpenAI-compatible 模型已经能流式返回。下一步是让 DashScope RAG 应用也进入同一套 SSE 协议。

### 问题

RAG 接口不是 OpenAI-compatible chat completions，它的流式开启方式不同：

- 需要 HTTP header：`X-DashScope-SSE: enable`
- 请求体参数是：`parameters.incremental_output = true`
- 流式数据内容在：`output.text`

第一次接入后，服务端拿到了 DashScope 的 SSE，但网关报错“Backend returned no stream data”。实际原因是 DashScope 返回：

```text
data:{...}
```

而原 parser 只识别：

```text
data: {...}
```

### 方法

我把差异下沉到 `AliyunRAGStrategy`：

```cpp
void enableStreaming(json& payload) const override {
    payload["parameters"]["incremental_output"] = true;
}

std::vector<std::pair<std::string, std::string>> getExtraHeaders() override {
    return {{"X-DashScope-SSE", "enable"}};
}
```

同时让 `LLMGateway::callStreaming()` 支持 `extraHeaders`，并让底层 curl 追加这些 header。

最后放宽 SSE parser：

```cpp
if (line.rfind("data:", 0) == 0) {
    std::string data = line.substr(5);
    if (!data.empty() && data.front() == ' ') data.erase(0, 1);
    ...
}
```

### 结果

RAG 返回的 `output.text` 被 `AliyunRAGStrategy::parseStreamChunk()` 提取后，再被 handler 包装成统一的 OpenAI 风格 SSE chunk。前端无需区分它来自 Qwen、MCP 还是 RAG。

---

## 11. 第十一阶段：验证关键链路

最终我用本地服务验证了四条关键链路：

1. `aliyun-qwen + enableTools=false`：普通单次 LLM 真实流式输出。
2. `aliyun-qwen + enableTools=true + 普通知识问题`：先工具路由，再第二次 LLM 流式输出。
3. `aliyun-qwen + enableTools=true + 现在几点`：路由命中 `get_time`，工具执行后流式最终回答。
4. `aliyun-rag + enableTools=false`：DashScope RAG SSE 正常解析，并统一转换为 `choices[0].delta.content`。

这说明新的响应模型已经覆盖普通聊天、MCP 工具和 RAG 三个主要场景。

---

## 12. 当前架构的取舍

现在的方案不是业内最完整的原生 tool calling 架构。它保留了 prompt-MCP 的路由模式，因此开启 MCP 后，普通问题也会多一次路由调用。

但这个取舍是有意的：

- 原生 tool calling 需要重构消息结构，支持 `assistant.tool_calls`、`tool` role、tool call id、流式 tool args delta 和多步状态机。
- 当前项目是 C++ 自研 HTTP/LLM 网关，没有 SDK 帮忙兜底，迁移成本较高。
- 以校招项目展示为目标，统一 SSE + MCP 独立开关 + RAG 流式的收益更高，风险更低。

后续如果继续演进，可以把 `GenericAIStrategy` 扩展为原生 tools 协议：

```text
第一次流式 LLM 请求携带 tools
    -> 不需要工具：直接输出 delta.content
    -> 需要工具：流式收集 tool_calls args
    -> 执行工具
    -> 第二次流式生成最终回答
```

这会进一步减少“不需要工具时的额外路由调用”，但属于下一阶段优化。

---

## 13. 最终故事总结

这个项目不是简单地“加了几个 AI 接口”。它的主线是：

1. 先修数据库和连接池，解决长期运行稳定性。
2. 再让 HTTP 框架支持异步响应和 SSE，为 LLM 慢调用铺路。
3. 建立 LLMGateway，把模型 API 调用纳入限流、熔断、超时、fallback 和错误治理。
4. 把模型接入层配置化，让 Qwen、DeepSeek、RAG 可以按 model id 接入。
5. 改造 AIHelper，让普通聊天、MCP 工具和 RAG 最终都走统一编排。
6. 最后统一前端 SSE 协议，用 `enableTools` 解耦工具能力和响应形态。

最终，它从一个教学 Demo 演进成了一个具备稳定数据库访问、连接池优化、异步 HTTP、LLM 网关治理、多模型配置、多会话、MCP 工具编排和 RAG 流式接入的 C++ AI 服务。
