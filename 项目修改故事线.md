# 项目修改故事线：从压测暴露问题到统一 AI 服务链路

> 这份文档用“故事线”的方式复盘项目演进。
> 它不替代 `项目改动详解.md`：后者负责逐文件记录“改了什么”，本文负责讲“为什么会改、问题如何暴露、方案如何一步步收敛”。
>
> 说明：文档中沿用项目里已有的“MCP工具”命名，但这里的实现本质是 **prompt-based tool routing / tool use**，不是原生 MCP 协议，也不是 OpenAI 原生 function calling。工具注册和执行本体主要沿用原项目，本项目重点改造的是 AIHelper 侧的工具路由、网关接入、流式最终回答和历史落库边界。

---

## 0. 起点：先把项目从“看懂”变成“能被我解释清楚”

这个项目最开始吸引我，是因为它不只是一个简单 HTTP Server。底层是基于 muduo 的 HTTP 框架，包含请求解析、响应构建、中间件、路由、SSL、session、数据库封装等能力；上层又做了 AI 聊天、图像识别、多会话、多模型适配、工具调用、远程 RAG、语音链路、消息异步落库和容器化交付。

但这也带来一个问题：如果只是把原项目看懂，它仍然不像“我自己的项目”。所以我的第一步不是立刻堆新功能，而是借鉴另一个分布式缓存项目的经验，对这个项目做压测和链路观察。压测的目的也不是先拿一个漂亮 QPS，而是把低并发演示下隐藏的问题暴露出来。

这条修改线最后形成了一个比较自然的顺序：

1. 先用压测暴露会话线程安全和数据库资源泄漏。
2. 修复 prepared statement 生命周期问题后，继续重构连接池。
3. 接入 LLMGateway，把模型调用从裸 curl 收敛为可限流、可熔断、可降级的统一出口。
4. 引入业务线程池和 deferred response，让慢 LLM 调用不阻塞 I/O 线程。
5. 重构模型策略和工厂，把模型接入从硬编码改为配置驱动。
6. 最后统一 SSE、工具调用和 RAG 的响应链路，让前端和业务语义重新对齐。

---

## 1. 压测先暴露了 session 线程安全问题

### 现象

我先用 wrk 对静态路由和正则路由做压测。最早压测 `menu` 这类看起来很简单的页面时，服务端直接崩溃；换成单线程场景后又没有问题。

这个现象说明问题大概率不在具体业务逻辑，而在多线程访问共享状态上。继续排查后发现，`menu` 虽然是静态页面，但访问前仍要经过 session 鉴权，所以它会进入会话层。

### 根因

会话层里有两个多线程风险点：

- session id 生成器内部使用 `std::mt19937`，并发访问不是线程安全的。
- `MemorySessionStorage` 使用 `unordered_map` 保存 session，并发读写会破坏容器状态。

### 修改

我给 session id 生成过程加锁，并给 `MemorySessionStorage::save/load/remove` 加锁。这个改动在 `项目改动详解.md` 的“四、会话层线程安全”里有逐文件记录。

### 思考

这个问题不能只总结成“加锁解决”。加锁解决的是正确性，但锁本身也可能成为新的性能瓶颈。当前实现采用的是直接互斥锁，足以修复原项目在多线程压测下的崩溃；如果继续演进，可以再按访问模式考虑读写锁或按 session id 分段加锁。

这一步给我的启发是：压测首先是正确性验证工具，其次才是性能展示工具。单线程能跑、多线程崩，说明项目里仍然有单线程假设没有被打破。

---

## 2. login 压测暴露 prepared statement 泄漏

### 现象

修复 session 线程安全后，我继续压测 login 接口。为了让压测更接近真实链路，我先批量注册 1000 个账号，再用 wrk 的 lua 脚本按登录流程请求。

一开始压测能出结果，但进一步看成功数时发现一个异常：只有固定数量的请求成功，数量接近 16382。这个数字不能直接当性能结果，因为后续失败请求并没有正常走完整条登录链路。

服务端日志里出现了 MySQL 报错：

```text
Can't create more than max_prepared_stmt_count statements (current value: 16382)
```

### 根因

原数据库访问层每次请求都会创建 `PreparedStatement`，但没有显式 `close()` 释放 MySQL 服务端 prepared statement 句柄。连接池里的连接是长生命周期的，所以这些服务端句柄会不断累积，最终打满 MySQL 的上限。

同时，原查询接口返回裸 `sql::ResultSet*`。`ResultSet` 的生命周期依赖创建它的 `PreparedStatement`，如果 statement 在函数结束时析构，返回出去的结果集就有悬垂风险。

### 修改

我把查询结果改成 RAII 对象：

- `StmtDeleter` 在析构时先 `close()` 再 `delete`，释放服务端资源。
- `DbQueryResult` 同时持有 `PreparedStatement` 和 `ResultSet`。
- `DbQueryResult` 禁止拷贝、允许移动，保证所有权明确。
- `executeUpdate` 也统一使用带 deleter 的 statement 指针。

### 思考

这个 bug 的本质不是“MySQL 有 16382 这个上限”，而是 C++ 资源生命周期没有被类型表达出来。prepared statement 既有客户端对象，又有服务端句柄；只 `delete` 不 `close()`，服务端资源不会及时释放。

所以这个修复可以上升成一个原则：数据库句柄、文件描述符、curl handle、连接池连接这类资源，不应该依赖业务代码记得手动释放，而应该用 RAII 把获取、持有和释放绑定到对象生命周期里。

---

## 3. 连接池从“能用”改成“更符合高并发访问”

### 触发

prepared statement 泄漏修复后，login 压测请求可以正常走完整链路。我又继续回看连接池实现，发现它虽然能工作，但在高并发和长时间运行下有几个明显问题。

### 问题

第一个问题是健康检查线程。原实现会每隔一段时间拿锁遍历连接并 `ping()`，而 `ping()` 是网络 I/O。持锁做慢 I/O 会让业务线程在 `getConnection()` 上等待，形成周期性延迟尖峰。

第二个问题是 FIFO 队列。先进先出意味着每次取到的是最久没被使用的冷连接，冷连接更可能已经被 MySQL 断开，所以业务线程取连接时还要额外面对活性检查和重连成本。

第三个问题是连接所有权。原实现使用 `shared_ptr` 表达连接池连接，但业务从连接池借出连接时，同一时刻其实只有一个调用方独占使用它，用共享所有权表达独占借用并不贴切，还会带来控制块分配和引用计数开销。

第四个问题是无限等待。连接耗尽时业务线程会一直挂起，故障不会快速暴露，而是慢慢堆积线程。

### 修改

我做了几类改造：

- 健康检查改成“摘出、锁外 ping、放回”的三段式，锁只保护连接池容器。
- 连接池容器从 FIFO 改为 `vector` LIFO，优先复用最近归还的热连接。
- 连接附加最后归还时间戳，健康检查跳过近期活跃连接，并限制每轮检查数量。
- 池中连接和 `MysqlUtil::QueryResult` 持有的连接改为带 `PoolDeleter` 的 `unique_ptr`。
- `getConnection()` 从无限等待改成带超时等待，默认 5 秒获取不到连接就抛异常。

### 思考

LIFO 的价值不只是“拿热连接”。它还有连接收敛效果：少数活跃连接会被反复使用，长期不用的连接会沉在池底，更容易被识别为冷连接或多余连接。FIFO 会让所有连接轮流被使用，反而让每个连接看起来都“最近活跃”。

`unique_ptr` 的价值也不只是少一次控制块分配，而是所有权语义更准确。连接池借出的连接在同一时刻只应该有一个业务方持有；结果对象析构时通过 deleter 自动归还连接，也避免了忘记归还或提前归还。

超时等待则体现了另一个取舍：它不会让请求无限挂起，但需要选择合适的超时时间。太短会误杀，太长又会让线程继续堆积。当前实现先把故障从“无声拖死”改成“可观测、可失败”，后续再根据压测和线上指标调参。

---

## 4. 建立 LLMGateway，把模型调用纳入治理

### 背景

原项目里模型调用更接近裸 curl。单模型演示时这能工作，但当项目接入多个模型后，就会出现治理问题：哪个用户在打爆后端、哪个模型需要限流、后端连续失败时是否要隔离、主模型失败后是否能降级，这些问题业务代码很难分散处理。

我做过分布式缓存项目，所以比较自然地想到把模型调用收敛成一层网关治理。

### 修改

我新增了 `LLMGateway` 相关模块，整体链路是：

```text
每用户限流 -> 每后端限流 -> 熔断状态检查 -> 带超时 curl -> 失败上报熔断 -> fallback 降级
```

具体包括：

- `RateLimiter`：用户级和后端级令牌桶限流。
- `CircuitBreaker`：连续失败次数 + 时间衰减窗口的三态熔断器。
- `GatewayConfig`：从 JSON 加载后端、超时、限流、熔断和 fallback 路由。
- `call()`：非流式调用，支持 fallback。
- `callStreaming()`：流式调用，支持限流、熔断和超时，但不做 fallback，因为 token 一旦吐给前端就无法回滚。
- `executeHttp()` / `executeHttpStreaming()`：统一封装 curl、HTTP 状态码判断和错误提取。

### 熔断器演进

我一开始考虑过简单连续失败次数熔断，但它有一个问题：如果每次成功都立刻清空失败计数，那么“失败-成功-失败-成功”的交替模式会让熔断迟迟打不开。

一版实现曾经改成固定时间窗口失败率，但它需要维护窗口请求数、失败数、最小样本数、窗口轮转和更复杂的半开探活计数。后来我发现这个复杂度对当前项目并不划算：LLM 后端的典型故障通常是连续 401/429/5xx、超时或返回不可解析结果，用连续失败次数就足够表达主要风险。

所以最终收敛为更接近常见熔断器的方案：

- CLOSED 状态累计连续失败次数。
- 成功不会立即清空失败计数。
- 距离最后一次失败超过 `failure_reset_timeout_ms` 后，成功才清空失败计数。
- 失败次数达到 `failure_threshold` 后进入 OPEN。
- 恢复超时后进入 HALF_OPEN 探活。
- HALF_OPEN 用 `half_open_max_calls` 限制本轮探活尝试次数。
- HALF_OPEN 下失败立即回 OPEN，成功次数达到 `success_threshold` 后回 CLOSED。

这个版本比固定窗口失败率少了很多状态变量，也更贴近我在另一个项目里用原子变量实现熔断器的经验：状态、失败次数、半开探活次数都用 atomic 表达，请求热路径不需要全程加锁。

当然，这不是最强的熔断策略。更生产级的方案通常会用滑动时间窗口或环形桶统计最近一段时间的请求成功率/失败率，并加上最小请求数保护。例如“最近 10 秒至少 50 个请求，失败率超过 50% 才熔断”。这种方案能识别非连续但比例很高的失败，也能避免小样本误判。当前项目选择连续失败次数 + 时间衰减窗口，是为了在代码复杂度、讲解成本和实际收益之间做折中。

### 业务错误识别

我还修复了一个容易忽略的问题：`curl_easy_perform` 返回 `CURLE_OK` 只代表传输层完成，不代表业务成功。HTTP 400、401、429、500 这类状态码仍然可能被 curl 当成传输成功。

因此网关会主动读取 HTTP 状态码，只要状态码大于等于 400，就抛出 `BackendException`，让熔断器和 fallback 能按真实业务失败工作。

### 思考

网关层的意义不是“包一层 HTTP 请求”，而是把不可控的外部模型 API 变成可治理资源。当前实现适合单机服务内的保护；如果部署到多实例，还需要继续考虑分布式限流、熔断状态是否共享、配置热更新和观测告警。

---

## 5. 异步响应：让慢 LLM 调用不阻塞 I/O 线程

### 背景

LLM 调用是秒级慢任务。如果 handler 在 muduo I/O 线程里同步等待模型返回，就会拖慢整个 Reactor 的响应能力。原 HTTP 框架是同步模型：handler 填好 `HttpResponse` 后，`HttpServer::onRequest()` 立刻发送响应。

### 修改

我加了通用线程池和 deferred response 机制：

- `ThreadPool` 承载阻塞的 LLM 调用。
- `HttpResponse` 保存 `TcpConnectionPtr`，并能标记 `deferred`。
- `HttpServer::onRequest()` 在回调后检查 `response.isDeferred()`，如果为 true 就不在当前栈帧发送。
- `ChatSendHandler` / `ChatCreateAndSendHandler` 标记 deferred 后，把业务逻辑投递到业务线程池。
- 业务线程完成后，通过 `conn->getLoop()->runInLoop()` 切回原 I/O 线程发送 SSE chunk 或结束帧。

同时 `HttpResponse` 增加了 SSE header、chunk、error、end 的发送方法，为后续统一流式响应打基础。

### 思考

这里的关键不是“开一个线程池”这么简单，而是要尊重 muduo 的 one loop per thread 模型。当前实现中，handler 在业务线程里先发送 SSE header，后续 chunk 和结束帧通过 `runInLoop()` 投递回连接所属的 event loop 执行，这和源码里的 `ChatSendHandler` / `ChatCreateAndSendHandler` 行为一致。更严格的生产级写法可以把 header 也统一投递到 I/O 线程，进一步收敛跨线程发送边界。

再往下一层看，`runInLoop()` 的意义不是“换个线程执行”这么抽象，而是：`TcpConnection` 天然绑定一个固定的 `EventLoop`；业务线程拿到 `conn->getLoop()` 后，把发送 lambda 放进这个 loop 的待执行队列，并通过 wakeup fd（Muduo 典型实现里是 `eventfd`）触发一次唤醒。原 I/O 线程原本阻塞在 `epoll_wait()`，收到这个 wakeup fd 的可读事件后返回，随后执行 pending functors，于是 `conn->send()` 最终仍然发生在连接所属的原 I/O 线程里。

当前代码通过捕获 `TcpConnectionPtr` 延长连接生命周期，避免业务线程回调时连接对象已经释放。更进一步的生产级处理可以考虑 weak_ptr、客户端断开后的取消、任务超时和流式中断清理。

---

## 6. 模型接入层从硬编码改为配置驱动

### 背景

原项目的模型策略更偏硬编码：不同模型对应不同 C++ 类，URL、model 名、API key 等配置散在代码里。新增或替换模型需要改代码、重新编译，也会把请求格式和业务 handler 耦合在一起。

### 修改

我把模型策略收敛为两类：

- `GenericAIStrategy`：适配 OpenAI-compatible 的普通模型，如 Qwen、DeepSeek 等。
- `AliyunRAGStrategy`：单独适配阿里云 DashScope RAG 应用接口。

`AIFactory` 从 `model_config.json` 加载模型配置，读取 `default_model`、`aliases` 和 `models[]`，按 `type` 创建对应策略。业务层只传 `modelType`，具体请求体构造、普通响应解析、流式 chunk 解析、上下文窗口大小、额外 header 都由策略层负责。

### 思考

这一步把模型替换从“修改 C++ 代码”变成“修改配置文件”。但它还不是最彻底的开闭原则：如果未来出现第三类非 OpenAI-compatible 后端，工厂里仍然可能需要新增 type 分支。更完整的演进方向是策略自注册或注册表机制，让新增策略只注册自己的 type 和构造函数。

---

## 7. AIHelper 从分裂入口收敛为聊天编排核心

### 背景

AIHelper 持有一个会话的历史消息，是聊天业务最核心的类。随着网关、上下文裁剪、工具调用和 SSE 加入，它不能再只是简单地拼请求、调模型、返回字符串。

### 修改

我对 AIHelper 做了几类收敛：

- 用互斥锁保护 `messages` 和 `strategy`。
- 所有模型请求都通过 `LLMGateway`，不再在 AIHelper 内部裸 curl。
- 增加 `buildRequestWithContext()`，按模型配置的上下文轮次裁剪历史。
- 工具路由和最终回答的临时 prompt 用 try/catch 回滚，避免异常污染历史。
- 最终外部入口统一为 `chat(..., bool enableTools, ChunkCallback onChunk)`。

当前核心流程是：

```text
enableTools=false 或模型不支持工具
    -> 添加用户原始问题
    -> completeStreaming() 单次 LLM 流式回答
    -> 落库完整 AI 回复

enableTools=true 且模型支持工具
    -> routeToolCall() 第一次非流式 LLM 工具路由
    -> 如需工具则 AIToolRegistry::invoke()
    -> 构造最终回答 prompt
    -> completeStreaming() 第二次 LLM 流式最终回答
    -> 只落库原始用户问题和最终 AI 回复
```

### 上下文裁剪思考

当前裁剪策略是保留最早一轮和最近 N 轮，并插入 system 提示说明中间对话被裁剪。这个方案简单、稳定、可预测，但它隐含假设“最近上下文更重要”。如果是任务型对话，最开始给出的约束可能也很关键。后续可以继续演进到按 token 裁剪、摘要记忆或重要事实抽取。

---

## 8. SSE 统一的真正矛盾：响应形态和工具能力被混在一起

### 原问题

我最开始只是给普通 LLM 调用做了一条流式响应路径，前端通过“流式开关”决定走 SSE 还是普通 JSON。这个阶段能展示流式效果，但它和工具调用、RAG 并没有真正统一。

当时的链路大致是：

```text
stream=true
    -> chatStreaming()
    -> 单次 LLM 流式
    -> 不进入工具判断

stream=false
    -> chat()
    -> 工具路由 / 工具执行 / 第二次 LLM
    -> 完整 JSON 返回
```

这就导致 `stream` 字段同时承担两个含义：既表示“是否流式响应”，又间接决定“是否启用工具能力”。用户想要工具，就要放弃流式；用户想要流式，就绕过工具。

### 方案收敛

我最终把响应形态和工具能力拆开：

- SSE 成为聊天接口默认响应形态。
- 前端去掉“流式开关”，改成“工具开关”。
- 请求字段使用 `enableTools`，只表达是否允许工具编排。
- handler 删除旧 JSON 分支，新会话和旧会话都统一走 SSE。
- 新会话首帧发送 `sessionId`，后续内容统一包装成 OpenAI 风格 `choices[0].delta.content`。

### 工具调用的边界

这里必须讲清楚：当前工具调用不是原生 function calling，也不是原生 MCP 协议。它的第一次 LLM 调用是 prompt-based 路由，让模型只输出类似下面的 JSON：

```json
{"need_tool": true, "tool": "get_time", "args": {}}
```

或：

```json
{"need_tool": false, "tool": "", "args": {}}
```

这说明当前第一次返回的本质仍然是普通文本，只是被 prompt 强约束成 JSON 形式；后端不是在读协议字段，而是在读一段需要自己解析的模型输出。后端必须在完整拿到这段文本后，才能判断是否需要工具、工具名和参数。

原生 function calling 则不同：请求里会显式声明 tools 和参数 schema，模型返回时通过结构化的 `tool_calls` / `function_call` 字段表达工具调用，客户端再按协议执行工具，并用专门的消息角色把工具结果回传。也就是说，原生 function calling 的工具调用能力属于 API 协议层，而不是 prompt 约束层。

这带来几个直接差异：

- prompt-based 更依赖模型“听话”，更容易遇到 JSON 不合法、字段名漂移、混入解释文本等问题；
- 当前项目没有 `assistant.tool_calls`、`tool` role、tool call id 这些协议语义，状态推进主要靠业务层自己维护；
- prompt-based 的第一次路由结果通常要完整返回后才能解析，因此不适合直接向前端流式透传；
- 原生 function calling 更容易支持流式 tool call 增量输出和参数拼接。

所以当前项目在 `enableTools=true` 时，通常会形成“两次模型调用”：第一次非流式做工具路由决策，第二次再流式输出最终回答。这是用一次额外调用换取统一前端体验和较低改造成本，而不是原生协议级 tools/tool_calls 方案。

如果未来迁移到 OpenAI-compatible 原生 `tools/tool_calls`，可以进一步减少“不需要工具时也多一次路由调用”的成本，但代价是要同步重构消息结构、tool role、tool call id、流式 args delta 和多步状态机，不适合作为当前阶段的低成本改造。

---

## 9. RAG 流式接入：协议差异下沉到策略层

### 问题

普通 OpenAI-compatible 模型流式响应后，RAG 还不能直接复用同一套逻辑。阿里云 DashScope RAG 应用接口和普通 chat completions 不一样：

- 流式开启需要 header：`X-DashScope-SSE: enable`
- 请求体需要：`parameters.incremental_output = true`
- 返回文本在：`output.text`
- 实际 SSE 里可能返回 `data:{...}`，而不是 `data: {...}`

如果这些差异写死在 AIHelper 或 handler 里，后续每接一种模型都会继续污染业务层。

### 修改

我把差异下沉到 `AliyunRAGStrategy`：

- 重写 `enableStreaming()`，设置 `parameters.incremental_output = true`。
- 重写 `getExtraHeaders()`，返回 `X-DashScope-SSE: enable`。
- 重写 `parseStreamChunk()`，从 `output.text` 提取增量文本。

同时 `LLMGateway::callStreaming()` 支持策略传入的额外 header，SSE parser 也放宽为兼容 `data:` 后可选空格。

### 结果

RAG 的原始流式响应先由网关识别，再由 RAG 策略解析，最后由 handler 包装成前端统一消费的 OpenAI 风格 SSE chunk。前端无需关心这段内容来自普通模型、工具最终回答还是 RAG。

---

## 10. 其他配套改动

除了主线改造，还有几类配套修改：

- `ChatServer` 中用户会话结构从单用户单 AIHelper 扩展为用户到多 session 的映射。
- 连接池初始化容量从 5 调整到 100，配合压测和并发聊天场景。
- 新增 `/chat/delete-session`，补齐会话删除能力。
- 注册链路改为参数化 SQL，修复 SQL 注入风险。
- 移除单点登录限制，允许同一用户多端并发会话。
- 新增网关单元测试，覆盖令牌桶、熔断器、窗口轮转、半开恢复和并发访问等逻辑。
- 清理密钥和工程文件，避免真实 API key 进入 git 历史。

需要注意的是，RabbitMQ 异步落库、图像识别、语音 ASR/TTS、工具注册与执行本体等能力主要来自原项目，本项目应把它们作为平台已有能力描述，而不是全部算作个人改动。

---

## 11. 最终架构和复盘结论

这条故事线不是“我一开始就规划好了完整架构”，而是从压测和真实问题一步步推出来的：

1. 压测 `menu` 暴露 session 会话层线程安全问题。
2. 压测 login 暴露 prepared statement 服务端句柄泄漏和结果集生命周期问题。
3. 修复数据库访问层后，继续重构连接池，处理健康检查持锁慢 I/O、FIFO 冷连接、shared_ptr 语义不匹配和无限等待。
4. 接入多模型后，引入 LLMGateway，把模型 API 纳入限流、熔断、超时、fallback 和错误分类治理。
5. 面对 LLM 慢调用，引入业务线程池和 deferred response，并通过 `runInLoop()` 回到 I/O 线程发送。
6. 模型策略从硬编码改为配置驱动，普通模型和 RAG 分别适配。
7. AIHelper 统一上下文裁剪、网关调用、工具路由、流式最终回答和落库边界。
8. 最后统一 SSE，用 `enableTools` 解耦工具能力和响应形态，并让普通聊天、工具最终回答、RAG 都进入同一套前端协议。

如果用一句话总结：这个项目的核心不是简单“加了 AI 接口”，而是把一个能演示的教学项目，沿着压测暴露的问题和 LLM 服务化的约束，改造成一个具备数据库资源安全、连接池优化、异步 HTTP、模型网关治理、多模型配置、工具编排和 RAG 流式接入的 C++ AI 服务。

后续继续深化时，我会固定追问三个问题：

- 这个方案引入了什么新代价，例如锁竞争、超时误杀、额外 token、流式断连处理。
- 这个具体 bug 能抽象成什么原则，例如 RAII、所有权语义、慢 I/O 不持锁、失败率熔断。
- 如果放到更大规模或多实例部署下是否仍然成立，例如分布式限流、熔断状态共享、配置热更新和可观测性。
